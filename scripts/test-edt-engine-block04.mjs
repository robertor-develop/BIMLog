import{spawnSync}from"node:child_process";const commands=[291,292,293,294,295].map(build=>`test:edt-engine-build${build}`);for(const command of commands){const result=spawnSync("pnpm",["--filter","@workspace/api-server","run",command],{stdio:"inherit",shell:process.platform==="win32"});if(result.status!==0)process.exit(result.status??1);}console.log("EDT_ENGINE_BLOCK04_RESULT=PASS");
