# Lens Next Build 11A — adapter regression gate repair

Date: 2026-09-03

Starting commit: `ac3418925817d5f5d1447082631702ebf0d3010c`

Last known passing adapter checkpoint: `ecdb46fa645729c67d90629e7daaf14be69a18bc` (`v1.05.N10-P04`, both adapters recorded PASS 52/52).

## Exact pre-mutation failure

The single `XmlExportWritesValidatedFile` method was invoked through an F-rooted diagnostic host beside the actual Navisworks 2021 test assembly. The diagnostic host changed no tracked source and printed the complete exception chain:

1. `System.Reflection.TargetInvocationException` from the diagnostic invocation of `BIMLogLensNext.Native.Tests.AdapterContractTests.XmlExportWritesValidatedFile`.
2. `System.Reflection.TargetInvocationException` at the test's reflection call to `LensNextDockPanelControl.ExportViewpointsXmlCore` (`AdapterContractTests.cs:491`).
3. `System.InvalidOperationException`: `Navisworks could not export viewpoints XML: [path/file name too long]`, thrown by `LensNextDockPanelControl.InvokeComContractMethod` while invoking the fake COM export contract.
4. Root `System.IO.PathTooLongException`, thrown by `System.IO.File.InternalWriteAllText` in `AdapterContractTests.SuccessfulIoState.DriveIOPlugin`.

Reflected product method: `BIMLogLensNext.Native.LensNextDockPanelControl.ExportViewpointsXmlCore` in `BIMLogLensNext.Native2021.dll`.

Test target: `BIMLogLensNext.Native.Tests.AdapterContractTests+SuccessfulIoState.DriveIOPlugin` in `BIMLogLensNext.Native2021.Tests.exe`.

The generated test temporary XML path was exactly 260 characters. Its components were:

- 155-character test application base under the Build 11 worktree;
- `xml-export-test-` plus a 32-character GUID for the scratch directory;
- `.viewpoints.xml.` plus another 32-character GUID plus `.tmp.xml`, generated by the production export core.

The directory creation itself succeeded. The fake exporter failed only when writing the resulting 260-character temporary filename under .NET Framework legacy path limits.

## Classification

`A. TEST/HARNESS DEFECT`

The N10-P04 checkpoint records this adapter test family passing. A direct diff from that checkpoint through the Build 11 starting commit shows no change to the implicated XML export method, test method, or shared output-path configuration. The behavioral dependency that changed was the test execution location: the Build 11 worktree/output prefix is long enough to exhaust the fixed filename budget created by the harness's verbose scratch-directory prefix.

This does not establish a product regression. The fake exporter, reflected production method, plugin name, XML validation, destination-preservation behavior, and assembly layout are unchanged.

## Minimum correction

Only the two test scratch-directory constructions in `AdapterContractTests.cs` were shortened from `xml-export-test-<GUID>` to `<GUID>`. Uniqueness, isolation, cleanup, existing-destination preservation, production temporary-file behavior, and all asserted XML behavior remain unchanged. No production source or project/output layout changed.

An attempted test-project output relocation was rejected during validation because older tests intentionally resolve source relative to the established output layout. It was removed before commit and is absent from the final diff.

## Verification

- Isolated `xml_export_writes_validated_file`: PASS.
- Complete Navisworks 2021 adapter suite: PASS 54/54.
- Build 11 focused/core suite: PASS 77/77.
- Platform atomic-create source contract: PASS.
- Create/capture UI source contract: PASS.
- Open Working View reconstruction and platform-first source contracts: PASS.
- Complete `scripts/test-lens-next-n10-p05-regression.ps1`: `REGRESSION_GATE_RESULT=PASS`.

The regression script required ordinary NuGet network access for restore. Its initial sandboxed attempt failed with `NU1301`; the authorized network-capable rerun passed every gate. No Replit, database, deployment, package, customer environment, or fallback artifact was accessed or changed.
