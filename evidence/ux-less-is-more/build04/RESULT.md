# Build 4 — Job Intake progressive disclosure

Result: PASS locally.

- Preserves the existing two-minute Quick Setup as the default and the complete Advanced Setup as the authoritative detailed workflow.
- Quick Setup now leads with required Setup readiness and places optional, work, and financial progress behind one bilingual disclosure.
- Advanced Setup continues to display the full readiness breakdown and every existing stage, field, action, persistence path, activation rule, and report.
- Uses native button semantics with `aria-expanded` and `aria-controls`.
- Frontend TypeScript: PASS.
- Product behavior changed: YES, Intake presentation hierarchy only.
- Intake data, autosave, activation, contracts, APUs, assignments, permissions, database, schema, and deployment changed: NO.
