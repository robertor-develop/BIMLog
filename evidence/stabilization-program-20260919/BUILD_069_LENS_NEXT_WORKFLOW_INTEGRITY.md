# Build 069 — Lens Next workflow integrity

- Create remains capture-first and one atomic Platform commit; retries retain idempotency identity and never create a local Saved Viewpoint as a side effect.
- Reconciliation remains confirmation → Platform pull → exact managed local upload. A GUID disagreement produces a non-executable manual conflict.
- Working View remains Platform-package load → exact Native apply. Repair remains explicit Native capture → Platform save → readback before apply.
- A stale refresh response is proven unable to overwrite the accepted version, issue-set digest, or visual-state digest; it stops in conflict state.
- Existing publishing regressions continue to prove identical retry replay, divergent retry conflict, stale-version refusal, and audit-failure transaction rollback.
