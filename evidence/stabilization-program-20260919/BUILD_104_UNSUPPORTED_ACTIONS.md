# Build 104 — Unsupported actions removed

- Removed the active Meeting form panel that advertised unavailable recording upload and AI autofill.
- Public Features and Pricing expose the implemented Windows Sync Agent only; no Mac installer action is advertised.
- No Native, installer, package, schema, database, or provider configuration changed.
- Focused contract: `pnpm --filter @workspace/api-server run test:block21-build104`.
