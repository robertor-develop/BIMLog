# Build 068 — Lens Next Platform browser completion

- One machine-readable capability contract binds the issue list/detail, grouping, filters, cards/table presentation, responsive and keyboard behavior.
- Captured-image states remain truthful (`missing`, `loading`, `loaded`, `error`); reference attachments remain PDF/JPG/PNG with the existing 5 MB limit.
- RFI and Submittal links remain same-project authoritative links and do not mutate the linked records.
- Production explicitly says a capture is not interactive 3D. The unmounted prototype is not promoted or represented as a production capability.
- The focused acceptance checks desktop/mobile CSS, focus visibility, reduced motion, negative image states, and the complete selected-issue surface.
- The complete historical suite exposed one stale P33 literal in the Build 30 release-candidate test. It now reads the canonical release identity and fails on a real generated-identity mismatch instead of failing whenever Platform advances normally.
- The same run exposed a stale Build 4 source-shape assertion that rejected the already-accepted explicit historical model-continuity flag. The assertion now accepts that governed optional argument while preserving every reconstruction-boundary check.
