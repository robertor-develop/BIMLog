# C002 — preserve and classify recovery candidates

The read-only recovery inventory records 25 dirty/untracked source files across three explicitly scoped F-rooted checkouts with byte hashes, matching current-file hashes where present, and per-file disposition. It is not a claim that every old contract is semantically equivalent. A repeat check proves the inventoried source files remained unchanged. Nothing is deleted, moved, overwritten, merged or installed.

Reviewed decisions:
- Project26 Procore dry-run source directly wraps previewProcoreRfiCsv and hardcodes one target, digest and 43-row fixture. Current procore-rfi-import owns the generic importer. Preserve the wrapper as historical test evidence; do not import it into production.
- Old Sidebar diff converts the APU button into a Link with a Calculator icon and explicit action discriminants. Current Sidebar and live commercial navigation already expose the canonical FinancialApuWorkspace link. Do not reintroduce the old sidebar or duplicate APU route.
- Old generic-APU template contracts describe fixed blueprint/variant imports and independent-review evidence. Current company-pricing-template-contract, generic-apu-engine, financial-contract-apu-binding and approved-budget services own the corresponding modern behaviors. Keep old contracts/tests for exact requirement comparison during C011–C020; no missing runtime behavior is proven solely by absent filenames.
- Historical database-safety edits are preserved, not applied over the current safety mechanism.
- Untracked historical evidence, temp folders and unrelated repository files remain at their original locations; this bounded source inventory does not certify their semantic completeness or authorize cleanup.

Local Lens manifest/binary identities are retained in C001 evidence. Existing rollback bundle directories and H-root packages remain untouched. Exact Ruben package and connected restoration proof are still field evidence, not fabricated by a directory listing.

This is the plan's preservation/disposition option, not a recovered feature or completed template-engine claim. No production change, dependency change, database connection, credentials, native mutation or source deletion.
