Fix 1 — AI Assist on submittal description. The AI Assist button is taking the user's plain language description and adding generic template content on top of it instead of replacing it with a professional formal submittal description. Change the behavior completely. When the user clicks AI Assist — take whatever text they typed, send it to the Claude API with this instruction: "The user has written the following description of a submittal in plain informal language. Rewrite it as a professional, formal construction submittal description suitable for submission to an architect or engineer. Keep all the technical content and intent exactly as the user described. Do not add generic template sections, checklists, or boilerplate. Just rewrite their actual content professionally." Replace the entire description field content with the AI-generated rewrite. Do not append to existing content — replace it entirely.
Fix 2 — Related RFI field. Change the Related RFI ID number input to a dropdown that loads all open RFIs for this project from the API. Show each RFI as its number and subject — for example RFI-0001: Structural penetration clearance L3. If there are no RFIs show a message saying No RFIs on this project yet. Allow the user to clear the selection.
Fix 3 — Procurement Before Approval warning. When the user selects On Order, Delivered to Site, or Installed and the submittal status is not yet Approved — show a prominent amber warning banner immediately below the procurement status radio buttons that says: Warning — Procurement Before Approval: Materials have been ordered or delivered before this submittal has been formally approved. This creates significant liability risk. Proceed only if you have written authorization from the architect or engineer. This warning must appear immediately when the radio button is selected — not after saving.
Fix 4 — AI Compliance Check. The button currently spins but does nothing visible. Fix it so after the AI check completes it shows the results directly below the button — a checklist showing each of the 7 rejection cause categories with a green check, amber warning, or red flag, plus a Submission Risk Score of Low, Medium, or High. If the submittal has not been saved yet show a message saying Save the submittal first to run the AI compliance check.
Fix 5 — Distribution list pre-population. When the submittal form opens automatically add the current logged-in user's email to the distribution list. They should always be on their own distribution list.
Fix 6 — Export fixes — same as RFI fixes already requested. The Export All button on the submittal list and log views must export the SUBMITTAL LOG as a summary table — one row per submittal showing submittal number, title, type, status, submitted by, submitted to, date submitted, date required, days outstanding, ball in court. Available in PDF landscape, Excel, and Word. Individual submittal rows must have their own Export PDF and Export Word buttons that export that single submittal as a formatted document. The Word export for individual submittals must include a fillable Official Review Response section with these fields — Review Decision dropdown Approved, Approved as Noted, Revise and Resubmit, Rejected, Not Required — Reviewer name and company — Date of review — Compliance notes text area — Rejection reason if applicable — Signature line — Authorized by with date.
Fix 7 — Reference document fields guidance. Next to the Drawing Number, Drawing Title, and Spec Section fields add a small search icon button. When clicked it searches files already uploaded to this project in BIMLog and shows a dropdown of matching files. When a file is selected it auto-populates the field. If no files exist show a message saying No files uploaded to this project yet — upload files in the Files tab to link them here.
Then rebuild and redeploy. Do not change anything else.