This is a critical build. Do not touch anything else that is currently working. Build all of this in one go.
PART 1 — Generated response documents
When a user saves a response to an RFI on the BIMLog platform — automatically do the following:
Step 1 — Generate the response PDF exactly as currently built.
Step 2 — Save this generated PDF as a file record in the files table. Use these values: fileName built from the project naming convention — take the convention separator, use the project code for field Project Code, use the responding party's originator code for Originator, use the discipline from the RFI reference information if available otherwise use the first allowed discipline value, use the level from the RFI if available otherwise use the first allowed level, set Type to RP for response, set Status to S2, set Sequence to the RFI number zero-padded to 4 digits. Example: NYC-270-BTC-MEP-L3-RP-0001-S2.pdf. Set uploadedByUserId to the responding user. Set projectId to the current project. Set status to active. Set source to system-generated. Add a new nullable column called source to the files table with values user-uploaded or system-generated. Add a nullable column called linked_rfi_id integer referencing rfis id so we know which RFI this document belongs to.
Step 3 — Store the actual PDF bytes. Save the generated PDF to a server-side storage path at uploads/projects/[projectId]/rfi-responses/[fileName]. Create this directory structure if it does not exist.
Step 4 — Add a download endpoint. Add GET /api/v1/projects/:projectId/files/:fileId/download which serves the file from storage as a download attachment.
Step 5 — Show the response document in the RFI detail panel. After a response is saved — show a section called Generated Response Document with the file name, the date generated, and a Download button that calls the download endpoint.
Step 6 — Show system-generated documents in the Files tab. In the files list — show system-generated documents with a different badge — a blue BIMLog Auto badge instead of the uploader avatar. This makes it clear the document was created by the platform not manually uploaded.
PART 2 — Document integrity system — build everything that was never built
Step 1 — Add these columns to the files table and run migration immediately: file_hash text nullable, file_size_bytes integer nullable, document_relationship text nullable, document_relationship_declared_at timestamp nullable, version_diff_status text nullable, hash_comparison_note text nullable, file_type_tier text nullable, source text nullable default user-uploaded, linked_rfi_id integer nullable references rfis id.
Step 2 — Create rfi_view_events table: id serial primary key, rfi_id integer not null references rfis id, user_id integer not null references users id, user_full_name text not null, user_company_name text not null, viewed_at timestamp not null default now, event_type text not null default viewed.
Step 3 — SHA-256 hashing on every upload. In the file upload route — after receiving the file name — calculate a SHA-256 hash of the file name string using Node.js crypto module. Store it in file_hash. Also store the file name length as file_size_bytes as a proxy until real file bytes are supported. Determine file_type_tier based on extension: tier1 for pdf dxf ifc jpg jpeg png, tier2 for dwg xlsx xls, tier3 for rvt rfa nwd nwf nwc. Store it.
Step 4 — Duplicate detection. After calculating the hash — search the files table for any existing file in this project with the same file_hash. If found — do not save — return 409 with JSON: status duplicate_detected, existingFile with id name uploadedByName createdAt, message This file appears identical to an existing file.
Step 5 — Declaration required. The upload endpoint must require a document_relationship field in the request body. Valid values: created, modified, reference, supporting. If missing — return 400 with message Document relationship declaration is required. Store it with the current timestamp in document_relationship_declared_at.
Step 6 — Declaration panel in Files tab. Before the upload button activates — show 4 large selectable cards the user must choose from: I created this document (blue border, document icon), I modified an existing document (amber border, edit icon), I am referencing an existing document without changes (gray border, link icon), This is a supporting document (green border, paperclip icon). The Upload button is disabled until one is selected. Show a note: Your declaration is permanently recorded in the audit trail.
Step 7 — RFI view tracking. When the RFI detail panel opens — call POST /api/v1/projects/:projectId/rfis/:rfiId/view which inserts a record into rfi_view_events. Show a Viewed by section in the RFI detail panel listing everyone who opened it with their name, company, and timestamp.
Step 8 — Audit Certificate endpoint. Add GET /api/v1/projects/:projectId/rfis/:rfiId/audit-certificate which generates a PDF containing: certificate header with BIMLog branding and UUID, RFI identification, party information, ball-in-court history table showing each party and how many days they held it, document submission record showing every file with its hash and declaration, view and access record showing who opened it and when, response attempt record, and the legal disclaimer text exactly as follows — LEGAL DISCLAIMER: This Audit Certificate was generated by BIMLog, an automated data recording and reporting platform operated by IgniteSmart LLC. The information contained in this certificate is derived from an immutable append-only audit trail maintained by BIMLog. File integrity is verified by SHA-256 cryptographic hash where technically possible. IgniteSmart LLC certifies that this audit record has not been modified since original entry and that all timestamps reflect the actual date and time of each recorded event in UTC. This certificate does not constitute a legal determination of liability or fault. It is an informational record of platform activity. Users are solely responsible for the accuracy of information they submit to BIMLog. For questions contact legal@ignitesmart.ai.
Step 9 — Add Audit Certificate button to RFI detail panel header with a shield icon. When clicked call the audit-certificate endpoint and download the PDF.
Step 10 — Also fix the 4 RFI export issues from before in the same build: log PDF and Word in landscape orientation with multi-page support, individual RFI PDF no blank pages, individual RFI Word with full response section checkboxes and signature line.
Run the database migration first. Then build all routes. Then build the frontend. Then TypeScript check. Then rebuild and redeploy. Show me confirmation that all 5 checks from before now pass.