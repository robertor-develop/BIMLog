Four targeted fixes only. Do not change anything else.
Fix 1 — Word export checkboxes not rendering. The realCheckbox function using SymbolRun exists but the output still shows plain text. Find every place in the Word export where cost impact, schedule impact, and attachments are written as text rows or table cells. Replace each one with a Paragraph containing two children — first the realCheckbox function call with the checked boolean, second a TextRun with a space and the label. Do not put checkboxes inside TableCell elements — put them as standalone Paragraphs outside any table. The sections should look like this pattern: new Paragraph containing realCheckbox of whether value matches followed by TextRun with the label. Do this for every single option — No Cost Impact, Cost Increase TBD, Cost Increase Known, Cost Decrease, No Schedule Impact, Increase in Calendar Days, Decrease in Calendar Days, See marked up drawings, See attached documents, See attached calculations, Other. Add a blank Paragraph spacer between each section. After the Attachments section add a Signature section with a Paragraph saying Authorized by followed by 40 underscore characters, then a Paragraph saying Date followed by 20 underscore characters.
Fix 2 — Audit certificate PDF company column overflow. In the audit certificate PDF generation find the view events table. The Company column text BIMCapital Partners is overflowing into adjacent rows. Fix this by adding explicit maxWidth and ellipsis truncation to the company column text call. Set the company column x position and width so that company text is limited to 120 points wide. Use the PDFKit text options width 120 and ellipsis true on the company text draw call. Also add lineBreak false to prevent text from wrapping to the next line within a row.
Fix 3 — Close RFI button not visible. The isProjectAdmin check uses currentMember role equals project_admin. The problem is currentMember may be undefined or the role value in the database may be different from the string project_admin. Add a console.log debug line temporarily — log currentMember and its role to the browser console so we can see what value is coming back. Also add a fallback — if the members list is still loading or currentMember is undefined show the Close RFI button anyway if the logged in user created the RFI. Additionally change the button from being inside the status dropdown area to being a standalone red button in the RFI detail panel header row next to the Audit Cert button. Make it always visible in the header for now so we can confirm it renders — we will add the role restriction back after confirming it shows.
Fix 4 — Official response PDF placeholder text. The response PDF shows literal asterisk markdown text like RESPONSE TO RFI-0001 and Project 270 Park Avenue and RFI Number and Date of Response Current Date as unformatted strings. Find the response PDF generation function — it is separate from the main RFI PDF and the audit certificate. Look for where the response text is being drawn and find any template literal strings that contain markdown formatting like double asterisks or square bracket placeholders. Strip all double asterisk characters from the response text before drawing it to the PDF. Replace any occurrence of Current Date in square brackets with the actual formatted date using new Date toLocaleDateString. Replace any occurrence of RFI Number in square brackets with rfi.number. Replace any occurrence of Project in square brackets with the actual project name.
Then run TypeScript check on both frontend and backend. Then rebuild and redeploy. Show exact output of these 4 checks:
Check 1 — Word checkboxes are standalone Paragraphs not in TableCells: grep -n "realCheckbox|No Cost Impact|See marked up|Authorized by|underscore" artifacts/api-server/src/routes/rfis.ts | head -15
Check 2 — Audit cert company column has width and ellipsis: grep -n "ellipsis|lineBreak.*false|company.*120|120.*company|width.*120" artifacts/api-server/src/routes/rfis.ts | head -10
Check 3 — Close RFI button in header not gated by role temporarily: grep -n "Close RFI|closeRfi|red.*button|header.*close|close.*header" artifacts/bimlog/src/pages/project/RfisTab.tsx | head -10
Check 4 — Response PDF strips markdown asterisks: grep -n "replace.asterisk|strip.markdown|Current Date|\\|markdown|responseText.*replace" artifacts/api-server/src/routes/rfis.ts | head -10
Show exact output of all 4 checks. Nothing else.