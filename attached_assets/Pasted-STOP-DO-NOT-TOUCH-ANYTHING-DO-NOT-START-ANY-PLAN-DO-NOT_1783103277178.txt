STOP. DO NOT TOUCH ANYTHING. DO NOT START ANY PLAN. DO NOT APPROVE OR PUBLISH ANYTHING. DO NOT MODIFY PRODUCTION NEON. First, in plain text, tell me what you already did for the helium dev-sync (the db-dev-sync step) and whether it ran — before you change anything.

Context and goal, plain English. My publish preview keeps generating DROP CONSTRAINT and DROP INDEX statements on every publish, even on doc-only changes. The reason: five database objects are created by the api-server at runtime in app.ts but are NOT declared in the drizzle schema. So when drizzle-kit push syncs the helium dev database from the schema, helium never gets these objects, and Replit's publish diff (helium versus prod Neon) sees that prod has them and dev does not, and generates drops for them every time. This is safe for now because they are only constraints and indexes so no rows are deleted, and app.ts recreates them idempotently on boot. But I want the drift to stop.

The five objects, so you know exactly what must exist in the schema. These are their real definitions from app.ts; do not change their shape. A foreign key named lens_viewpoints_supersedes_id_fk on lens_viewpoints.supersedes_id referencing lens_viewpoints.id (app.ts near line 212). A partial unique index named rfi_ball_in_court_open_unique on rfi_ball_in_court_history (rfi_id) WHERE to_date IS NULL (app.ts near line 166). An index named clashes_project_fingerprint_idx on clashes (project_id, fingerprint) (app.ts near line 146). An index named lens_viewpoint_reports_project_idx on lens_viewpoint_reports (project_id) (app.ts near line 293). An index named lens_viewpoint_events_viewpoint_idx on lens_viewpoint_events (viewpoint_id) (app.ts near line 305).

The goal is to declare these five in the drizzle schema so the schema matches what the app actually creates, then re-sync the helium dev database from the schema so dev matches prod, so future publish previews stop generating drops for them. I am not telling you how to implement it — choose the cleanest way in the drizzle schema files yourself. Do not delete any data. Do not publish.

When done, print all of this back as plain text, not in a collapsed box: which schema files you changed and the five declarations you added; the result of re-running the helium sync; a check that helium now contains all five objects (query information_schema / pg_indexes / pg_constraint and list them); and what the publish diff would now generate (it should no longer list those five drops). If anything about deleting a TABLE, a COLUMN, or row data appears at any point, STOP immediately and tell me before doing anything.