Fix these 4 things. Do not touch anything else that is currently working.
Fix 1 — RFI Log PDF and Word cut off. The log export in both PDF and Word is getting cut off on the right side because there are too many columns for portrait orientation. Switch the log PDF and log Word export to landscape orientation. Also if the data has more rows than fit on one page — continue on the next page. Do not cut off. The log should be as many pages as needed to show all RFIs — same as the Excel which shows everything correctly.
Fix 2 — Individual RFI PDF blank pages. The individual RFI PDF export has 6 pages but most are blank with only a name appearing on page 5. Fix the PDF generation so it only renders pages that have actual content. No blank pages. A standard RFI with moderate content should be 2 to 3 pages maximum. Find every place in makeRfiPdf where a new page is added and make sure content actually follows before adding a page break. Remove all unconditional addPage calls that are not followed by real content.
Fix 3 — Individual RFI Word document — add the missing response section options. In the Word export for individual RFIs the Official Response section must include these specific fields with checkboxes and fillable lines: Response text area, Answered By name and company, Date of Response, Cost Impact checkboxes — No Cost Impact, Cost Increase TBD, Cost Increase Known with amount field, Cost Decrease, Schedule Impact checkboxes — No Schedule Impact, Increase in Calendar Days with number of days field, Decrease in Calendar Days with number of days field, Attachments section with checkboxes for See marked up drawings, See attached documents, See attached calculations, Other with fillable line, and a Signature line with Authorized by and date. This section must appear whether or not a response has been saved in the system.
Fix 4 — Suggested compliant names position in Files tab. The suggested compliant names section currently appears at the bottom of the page after the error breakdown. Move it to appear immediately below the file name breakdown — right after the FILE NAME BREAKDOWN section and the colored token chips showing which fields failed. It should be the very next thing the user sees after understanding what went wrong — not at the bottom after scrolling. Also add the AI Suggest button that calls the Claude API to suggest a name — this was in the original instruction but is missing from the current build.
Then rebuild and redeploy. Do not change anything else.