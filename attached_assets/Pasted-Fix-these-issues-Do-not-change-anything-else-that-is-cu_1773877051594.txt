Fix these issues. Do not change anything else that is currently working.
Fix 1 — Declaration panel upload error. The Files tab shows the 4 declaration cards — Created, Modified, Reference, Supporting — but when a file is dropped and uploaded the API returns an error saying document_relationship is required. This means the selected declaration value is not being passed in the upload request body. Find the upload handler in FilesTab.tsx — check that documentRelationship state is being included in the mutate call. The mutate call must include documentRelationship as a required field. Also the Upload button must be disabled until a declaration card is selected. Show a clear message above the drag and drop zone that says Select your document relationship above before uploading.
Fix 2 — RFI response model overhaul. The current RFI model only allows one official response and the same person who created the RFI can respond to it. This needs three changes. First — add a conflict of interest warning. When the submitted-by user or company is the same as the user adding a response — show an amber warning that says Warning: You are responding to your own RFI. This has been flagged in the audit trail as a potential conflict of interest. Log this event in the activity log permanently. Second — allow multiple response attempts. The RFI should allow additional responses to be added after the first one. Each response gets a sequence number — Response 1, Response 2, Response 3. All responses are shown in chronological order in the RFI detail panel. The original responder and any project member with write access can add responses. Third — add a Close RFI button visible only to Project Admin role. When clicked it marks the RFI as closed regardless of response status and logs who closed it and when in the activity trail. The Audit Certificate must include all responses and who closed it.
Fix 3 — RFI Word export missing response section. The current Word export has no checkboxes. Add the full Official Response section with these exact elements — checkboxes for Cost Impact: No Cost Impact, Cost Increase TBD, Cost Increase Known with amount field, Cost Decrease — checkboxes for Schedule Impact: No Schedule Impact, Increase in Calendar Days with number field, Decrease in Calendar Days with number field — checkboxes for Attachments: See marked up drawings, See attached documents, See attached calculations, Other with fillable line — Answered By name field, company field, Date of Response field — Signature line with Authorized by and date. All checkboxes must be actual Word checkbox form fields not bullet points.
Fix 4 — Add Review Response must auto-scroll. When the user clicks Add Review Response button in the submittal detail panel — do not just show the form at the bottom where the user cannot see it. Immediately scroll the detail panel to the Review Response section smoothly. Use scrollIntoView with behavior smooth on the review response container ref. The user must visually land on the form the moment they click the button.
Fix 5 — AI Draft Rejection rewrites not appends. The AI Draft Rejection button in the submittal review response section is adding generic template content instead of rewriting the actual rejection reason professionally. Fix it the same way we fixed AI Assist — send the existing rejection reason text to Claude with instruction to rewrite it as a professional formal rejection notice specifying exactly what needs to be corrected and why. Replace the field content entirely. Do not append.
Fix 6 — AI Pre-Submission Check on new submittal form does nothing. The Run AI Compliance Check button in Section 7 of the new submittal form spins but shows no results. Check the POST endpoint for ai-check. The issue is likely that the submittal does not exist yet when the button is clicked — the form has not been saved. Change the behavior — when Run AI Compliance Check is clicked on an unsaved form, collect all the current form field values — title, spec section, category, manufacturer, model number, description — and send them directly to the Claude API without requiring a saved submittal ID. Show the results inline in the form immediately below the button. The results must show each of the 7 rejection cause categories with a green check amber warning or red flag, plus the Submission Risk Score Low Medium or High. Do not require saving first.
Fix 7 — Drawing Number and Drawing Title need guidance. These are free text fields with no validation. Add a note below each field in small gray text that says: For reference only — enter the drawing number exactly as it appears on the project drawing set. This does not validate against uploaded files. Also the search icon button next to these fields should search through files already uploaded to the project and suggest matching file names. If no files exist show No files uploaded yet.
Fix 8 — Submittal category must show all options. The new submittal form shows only Other in the category dropdown. Show all categories: Shop Drawing, Product Data, Sample, Mockup, Calculation, Certificate, Warranty, Operation Manual, As-Built, Closeout, Other. Do not require GC approval for standard categories — the approval workflow for custom categories is a Phase 2 feature.
Fix 9 — Email field validation. On the submittal form in Section 2 Submitted By and Section 3 Submitted To — validate the email field format on blur. If the email does not match a valid email pattern show a red border and message Invalid email address — please check before submitting. Do not allow form submission with an invalid email.