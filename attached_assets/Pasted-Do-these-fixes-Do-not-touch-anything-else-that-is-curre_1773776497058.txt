Do these fixes. Do not touch anything else that is currently working.
Fix 1 — OAuth contact buttons. Change the 3 contact buttons to all be the same style — blue background white text same as the Get Started button. Change the text and links to exactly these three: Button 1 — Email us — opens mailto:info@ignitesmart.ai — add a small copy icon next to the email address info@ignitesmart.ai that copies the email to clipboard when clicked. Button 2 — Call or text us — Bolivia +591 71054305 — opens tel:+59171054305. Button 3 — Call or text us — USA +1 332 900 9180 — opens tel:+13329009180. Button 4 — WhatsApp us — opens https://wa.me/59171054305.
Fix 2 — Validate and Download Get Started button. When the user clicks Get Started on the Validate and Download option it should close the modal and redirect the user directly to the Files tab of the current project where they can upload a file immediately.
Fix 3 — Download for Windows and Download for Mac buttons. These should not send an email. Instead when clicked they should download the actual Electron sync agent app. The sync agent was already built at artifacts/sync-agent. Package it as a downloadable zip file and serve it from the API at GET /api/v1/downloads/sync-agent-windows and GET /api/v1/downloads/sync-agent-mac. The buttons should trigger these download endpoints directly. If packaging takes too long — as a temporary solution serve the sync-agent source folder as a zip file that the user can download and run with npm install and npm start.
Fix 4 — Add Integration button. Add a clearly visible Add Integration button or card at the bottom of the Integrations tab page. It should be a large dashed border card with a plus icon that says Add Integration — Connect any platform your team uses. When clicked it opens a modal showing a grid of platforms to choose from — Procore, Autodesk ACC, OneDrive, SharePoint, Revit, Navisworks, Speckle, MS Project, Google Drive, PowerBI, Excel, IFC — plus an Other field where they can type any platform name and contact us. Selecting any platform from the grid opens the ConnectModal for that platform.
Fix 5 — Help button back navigation. When the user is inside a project and clicks Help — the Setup Guide page should remember which project they came from. The Back button on the Setup Guide page should say Back to Project and return them to the exact project and tab they were on before clicking Help — not back to the dashboard.
Then rebuild and redeploy. Do not change anything else.