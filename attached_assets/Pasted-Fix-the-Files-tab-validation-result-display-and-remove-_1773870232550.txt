Fix the Files tab validation result display and remove the inline Name Generator. Do not change any other part of the app.
Fix 1 — Remove the inline Name Generator completely from FilesTab.tsx. When a user needs to generate a compliant name they use the Name Generator tab in the sidebar. Delete the entire inline Name Generator section from the Files tab.
Fix 2 — Remove the Simulate File Upload text input section completely. The drag and drop zone is the only upload interface. Delete the text input, its label, its subtitle, and the Test Upload button entirely.
Fix 3 — Restructure the validation result so it shows in this exact order top to bottom:
First — a clean white or very light blue container with no red background showing: the rejected file name in monospace gray, then below it a blue bold heading that says Suggested Compliant Name, then the suggested name as a large prominent blue chip that copies to clipboard on click, then next to it the Ask AI button in purple and the Customize Name button in outline blue. Below those buttons one line of small gray helper text that says Built from your active naming convention — click to copy or use the Name Generator tab for full control.
Second — below that first container, separated by a divider, a collapsible section that is collapsed by default. The toggle link says Show why this file was rejected with a small chevron icon. When expanded it shows the red field-by-field error breakdown — each field that failed, the value submitted, and the allowed values as chips. This section can be red. But it is hidden by default.
Fix 4 — The outer validation result container background must be white or very light amber — NOT red. Only the field error details inside the collapsed section are red.
Fix 5 — Fix the Ask AI button. It is currently showing AI suggestion failed. The suggest-name endpoint is failing. Fix it so if the Anthropic API call fails for any reason — instead of showing an error toast — it falls back to building a smart suggestion by scanning the rejected file name for any words or substrings that match allowed convention values, using those matched values for those fields and the first allowed value for unmatched fields. Always return a suggestion. Never show a failure toast.
Then rebuild and redeploy. Do not change anything else.