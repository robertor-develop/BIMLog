Modify ONLY the file artifacts/bimlog/src/pages/Dashboard.tsx. Do not create any new files. Do not touch App.tsx. Do not touch Navbar.tsx. Do not touch any routing. Do not touch any project pages. Do not touch any sidebar. Only Dashboard.tsx changes.
The Dashboard page must keep everything that currently exists — the Navbar at the top, the Your Projects heading, the New Project button, the project cards grid, all existing hooks and data fetching. Do not remove or replace anything that currently works.
ADD this content above the existing Your Projects section in this exact order top to bottom:
SECTION 1 — Platform stats bar. A row of 5 cards using the same CSS classes and styling as the existing project stat cards. The 5 cards are: Card 1 — label "Active Projects", value is the count of projects from the existing projects array. Card 2 — label "Total Files", value is the sum of fileCount across all projects if that field exists, otherwise 0. Card 3 — label "Open RFIs", value is the sum of openRfiCount across all projects if that field exists, otherwise 0. Card 4 — label "Pending Submittals", value is the sum of pendingSubmittalCount across all projects if that field exists, otherwise 0. Card 5 — label "Compliance Rate", value is calculated as the percentage of files that are valid across all projects — if no file data available show 0%. Show these 5 cards in a single horizontal row with the same spacing and styling as the stat cards inside project pages.
SECTION 2 — Two column row. Left column heading "Needs Attention" with an amber warning icon. In this column show a list built from the existing projects array — for each project check if it has openRfiCount greater than 0 and show it as an amber alert row saying "[Project Name] — Has open RFIs" with a "Go to Project" button that navigates to /projects/[id]/rfis using setLocation from wouter. If no projects need attention show a green row saying "All projects on track". Right column heading "Recent Activity" with a clock icon. Fetch activity from GET /api/v1/projects/[id]/activity for each project in the projects array — combine all results, sort by createdAt descending, take the top 10 entries. Show each entry as a row with a colored action badge, the user name, the project name, and a relative timestamp like "2 hours ago". If no activity exists show "No recent activity".
SECTION 3 — Full width section heading "Your Projects" — this is the EXISTING project cards grid exactly as it currently is. Keep it exactly as is. Just move it below Sections 1 and 2.
Use only hooks and imports that already exist in Dashboard.tsx. If you need to fetch activity for multiple projects use Promise.all with the existing fetch pattern already used in the file. Do not add any new npm packages. Do not add any new API endpoints. Use only what already exists.
Then rebuild and redeploy. Show exact output of these checks:
Check 1 — Section 1 stats exist: grep -n "Active Projects|Total Files|Open RFIs|Pending Submittals|Compliance Rate" artifacts/bimlog/src/pages/Dashboard.tsx | head -10
Check 2 — Section 2 Needs Attention exists: grep -n "Needs Attention|Recent Activity|Go to Project|All projects on track" artifacts/bimlog/src/pages/Dashboard.tsx | head -10
Check 3 — No new files created: find artifacts/bimlog/src/components/layout -name "MasterSidebar*"
Check 4 — App.tsx not modified: git diff --name-only HEAD | grep "App.tsx"
Show exact output of all 4 checks. Nothing else.