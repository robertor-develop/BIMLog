Fix the file upload system in FilesTab.tsx. Do not change the backend. Do not change anything else in the app.
Fix 1 — The upload mutate call is not sending documentRelationship. Find the mutate call around line 456 that does body: JSON.stringify({ fileName }). Change it to include all required fields: body: JSON.stringify({ fileName, documentRelationship, fileSize: file?.size || 1024, fileType: file?.type || "application/octet-stream", fileContent }). The documentRelationship state is already set when the user clicks a declaration card — it just isn't being passed to the API. This is why the upload silently fails or passes without validation.
Fix 2 — After a successful upload OR a failed validation, the file list must refresh. The invalidateQueries calls on lines 473 and 474 exist but they only fire on success. Move them so they also fire when the API returns a 422 validation error — meaning the file was rejected for naming convention violation. When rejected, the file SHOULD appear in the file list with status "rejected" so the user can see it. Check that the API actually saves rejected files to the database with status "rejected" — if it does not, the list will always be empty after a rejection.
Fix 3 — The "No files uploaded yet" empty state is showing even when files exist or after an upload attempt. Find where the empty state renders (around line 389-391). It should only show when families.length === 0 AND !isLoading. Make sure this condition is correct and that after any upload attempt — success or rejection — the query is invalidated and the list re-renders.
Fix 4 — Customize Name must NOT navigate away. Remove the setLocation navigation. Instead when the user clicks Customize Name — open the Name Generator inline as a modal or slide-over panel directly in the Files tab. If building a modal is complex — as a simpler fix, open the Name Generator in a new browser tab using window.open(\/projects/${projectId}/name-generator`, '_blank')` so the user does not lose their upload context.
Fix 5 — Ask AI must show its result. After the fetch to suggest-name completes — the result is being set in state (aiSuggestedName) but confirm it is rendering visibly. Find where aiSuggestedName is rendered and make sure it appears as a purple chip below the blue suggested name chip. If the state variable exists but the JSX rendering it is missing or conditional on a value that is never true — add the rendering now.
Then run TypeScript check on the frontend only. Then rebuild and redeploy. Show exact output of these checks:
Check 1 — documentRelationship is now in the mutate body: grep -n "documentRelationship|JSON.stringify" artifacts/bimlog/src/pages/project/FilesTab.tsx | head -10
Check 2 — invalidateQueries fires on both success and rejection: grep -n "invalidateQueries|onError|onSuccess|422|rejected" artifacts/bimlog/src/pages/project/FilesTab.tsx | head -15
Check 3 — Customize Name opens new tab not navigates: grep -n "window.open|setLocation|Customize|name-generator" artifacts/bimlog/src/pages/project/FilesTab.tsx | head -10
Check 4 — aiSuggestedName renders in JSX: grep -n "aiSuggestedName|AI badge|purple|ai-chip" artifacts/bimlog/src/pages/project/FilesTab.tsx | head -10
Show exact output of all 4 checks. Nothing else.