Connect SendGrid for automated email notifications. Use the SendGrid API key already stored in the environment variables. Do not hardcode any API key. The from address is notifications@ignitesmart.ai.
Install the SendGrid Node.js library: npm install @sendgrid/mail
Create a shared email service at artifacts/api-server/src/lib/email.ts that exports a sendEmail function taking to, subject, html, and optional replyTo parameters. Use the SENDGRID_API_KEY environment variable. If the environment variable is not set log a warning and skip sending without throwing an error.
Create professional HTML email templates for each notification type. All emails must have the BIMLog by IgniteSmart branding — navy header with BIMLog logo text, clean white body, navy footer with © 2026 BIMCapital Partners INC and info@ignitesmart.ai. All emails must be bilingual — detect the recipient's language preference from their notification_preferences column and send in EN or ES accordingly.
Build these 10 email triggers:
Trigger 1 — Project invitation. When POST /api/v1/projects/:projectId/invitations is called — send an email to the invited email address with subject "You have been invited to join [Project Name] on BIMLog" — body shows project name, the role they have been assigned, who invited them, and a prominent button "Accept Invitation & Create Account" linking to the registration page with their email pre-filled.
Trigger 2 — RFI assigned. When a new RFI is created — send email to the submitted_to_email with subject "New RFI Assigned: [RFI Number] — [Subject]" — body shows RFI number, subject, project name, submitted by, date required, and a View RFI button linking directly to the RFI.
Trigger 3 — RFI response overdue. Create a background job that runs every hour using setInterval. Check all open RFIs where date_required is in the past and status is not closed or responded. For each overdue RFI send one email to submitted_to_email with subject "OVERDUE: RFI [Number] response required" — body shows days overdue, RFI details, and a View RFI button. Mark the RFI with a last_overdue_notification_sent timestamp to avoid sending duplicate emails — only send once per 24 hours per RFI.
Trigger 4 — Submittal assigned for review. When a new submittal is created — send email to submitted_to_email with subject "New Submittal for Review: [Submittal Number] — [Title]" — body shows submittal number, title, spec section, project name, submitted by, date required, and a View Submittal button.
Trigger 5 — Submittal response overdue. Same background job as RFIs — check all pending submittals where date_required is in the past. Send email to submitted_to_email with subject "OVERDUE: Submittal [Number] review required." Same 24-hour deduplication logic.
Trigger 6 — File naming violation. When a file upload is rejected for naming convention violation — send email to the uploader's email and to all project admins with subject "Naming Violation Detected: [File Name] — [Project Name]" — body shows the file name that was rejected, which fields failed, the project name, and a link to the Files tab.
Trigger 7 — Procurement Before Approval. When a submittal is saved with procurement_status of on_order, delivered, or installed and status is not approved — send email immediately to all project admins with subject "ALERT: Procurement Before Approval — [Submittal Number]" — body shows the submittal details, what was procured, and a View Submittal button.
Trigger 8 — Rapid approval flagged. When rapid_approval_flag is set to true on a submittal — send email to all project admins with subject "WARNING: Rapid Approval Detected — [Submittal Number]" — body explains that the submittal was approved in under 60 seconds and may not have been properly reviewed.
Trigger 9 — New team member added. When POST /api/v1/projects/:projectId/members is called — send email to the new member's email with subject "You have been added to [Project Name] on BIMLog" — body shows project name, their role, who added them, and a Go to Project button.
Trigger 10 — Password reset. Add POST /api/v1/auth/forgot-password endpoint that accepts an email address, generates a secure reset token using crypto.randomBytes(32).toString('hex'), stores it with a 1-hour expiry in a new column called password_reset_token text nullable and password_reset_expires timestamp nullable on the users table, and sends an email with subject "Reset your BIMLog password" — body has a prominent Reset Password button linking to /reset-password?token=[token]. Add GET /reset-password route on the frontend and POST /api/v1/auth/reset-password endpoint on the backend that validates the token and updates the password.
Also add a Forgot Password link on the login page that calls the forgot-password endpoint.
Respect user notification preferences — before sending any notification except invitation and password reset — check the recipient's notification_preferences column. If they have disabled that notification type do not send the email.
Then run TypeScript check on both frontend and backend with zero errors. Then rebuild and redeploy. Show exact output of these checks:
Check 1 — SendGrid email service exists: grep -rn "sendgrid|SendGrid|SENDGRID_API_KEY|sendEmail" artifacts/api-server/src/lib/ | head -10
Check 2 — All 10 triggers exist: grep -rn "sendEmail|sendgrid" artifacts/api-server/src/routes/ | head -20
Check 3 — Password reset endpoint exists: grep -n "forgot-password|reset-password|password_reset_token" artifacts/api-server/src/routes/auth.ts | head -10
Check 4 — Background job for overdue notifications: grep -n "setInterval|overdue|last_overdue|background.*job" artifacts/api-server/src/routes/rfis.ts artifacts/api-server/src/routes/submittals.ts | head -10
Check 5 — Forgot password link on login page: grep -n "forgot.*password|ForgotPassword|reset.*password" artifacts/bimlog/src/pages/AuthPages.tsx | head -5
Show exact output of all 5 checks. Nothing else.