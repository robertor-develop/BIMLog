Fix these issues in the RFI system. Do not change anything else that is currently working.
Fix 1 — Project Address field. The project address in the RFI form should auto-populate from the project data already in the database — not show browser cached addresses. Pull the project address from the projects table and pre-fill the Project Address field automatically. The user should not need to type it. Make it editable but pre-filled.
Fix 2 — Submitted To section. When the user selects a company in the Submitted To dropdown and the person they need does not exist in the project team — show an Add person button that opens a small inline form to add name email and phone. This person gets added to the RFI distribution and notification list but does NOT get added as a project member. They are external contacts for this RFI only.
Fix 3 — Reference Information fields. The Drawing Number, Drawing Title, Spec Section, Detail Number, and Note Number fields currently show the word list as a placeholder. Fix the placeholders to say something meaningful like e.g. A-101, e.g. Floor Plan Level 3, e.g. 23 00 00 etc. Then add a small search button next to each field that searches files already uploaded to this project in BIMLog and lets the user select one to auto-populate the field. If no files are found show a message that says No documents found for this project. Upload files to the Files tab to link them here. Log when a user submits an RFI without linking reference documents — record this in the activity log as RFI submitted without reference documents attached so it appears in the audit trail and AI reports.
Fix 4 — Distribution list. Allow the user to add external people to the distribution list who are not project members. Show an Add external contact button below the existing team member checkboxes. When clicked show a small inline form with Name, Email, and optionally Phone fields. These external contacts receive RFI notifications by email but are not added as project members. They are saved on this RFI only.
Fix 5 — Response documentation. In the RFI response section add an Attach response documents field. This should allow the user to either paste a URL to an external document, type a file name that exists in BIMLog files, or upload a reference. The response document reference is saved with the RFI response and shown in the RFI detail view and PDF export.
Fix 6 — PDF export blank pages. The exported PDF is 14 pages with 12 blank pages. Fix the PDF generation to only output pages that have content. Remove all blank pages from the export. The PDF should be maximum 2 to 3 pages for a standard RFI.
Fix 7 — Export All options. When the user clicks Export All on the RFI list or log view — show a modal asking which format they want: PDF, Excel, or Word. PDF exports all RFIs as individual pages in one combined PDF document. Excel exports a properly formatted spreadsheet with columns auto-sized, a header row with the BIMLog and project name, and each RFI on its own row with all fields. Word exports a properly formatted document with each RFI as a section with proper headings and formatting. Name the exported file RFI-Log-[ProjectCode]-[Date].[extension].
Fix 8 — Individual RFI export from list. Add an Export PDF button on each RFI row in both the list view and the log view so the user can export any individual RFI to PDF without opening it first.
Then rebuild and redeploy. Do not change anything else.