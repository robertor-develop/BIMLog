Fix these issues in the RFI system. Do not change anything else.
Fix 1 — Export All Word for list and log views. The Word export from the Export All button must export the RFI LOG as a formatted table — exactly like the Excel export but in Word format. One table with all RFIs as rows showing RFI number, subject, status, priority, submitted by, submitted to, date requested, date required, days outstanding, ball in court, schedule impact. It must NOT export individual full RFI documents. Fix this now.
Fix 2 — Export All PDF cut off. The PDF log export is getting cut off on the right side because there are too many columns for letter size paper. Fix this by using landscape orientation for the PDF log export. Set the page to landscape so all columns fit without being cut off.
Fix 3 — List vs Log difference. List view and Log view exports must be different. List export shows the card-style summary — RFI number, subject, status, priority, ball in court, days outstanding. Log export shows the full register table — all columns including submitted by company, submitted to company, submitted to person, forwarded date, answered date, schedule impact days. Make sure the Excel and PDF and Word exports for list and log are clearly different documents reflecting their different column sets.
Fix 4 — Individual RFI row layout. The PDF and Doc buttons on each RFI row in the log view are getting cut off off screen. Fix the table layout so all columns and buttons are visible without horizontal scrolling. Either reduce the column widths, hide less important columns by default with a show/hide columns toggle, or move the action buttons to a fixed right column that is always visible.
Fix 5 — Word response section. In the individual RFI Word export the response section must have a proper formatted text box with a border that says OFFICIAL RESPONSE at the top. Inside the text box include these labeled fields with space to fill in: Response text — a large multi-line area. Answered by — name and company. Date of response. Cost impact — checkbox options No Cost Impact, Cost Increase TBD, Cost Increase Known with amount field, Cost Decrease. Schedule impact — checkbox options No Schedule Impact, Increase in Calendar Days with number field, Decrease in Calendar Days. Attachments — a line that says See attached documents and See marked up drawings with checkboxes. Signature line — Authorized by with a signature line.
Fix 6 — Auto-generated response document. When a user saves a response on the BIMLog platform — automatically generate a formatted PDF response document and make it available for download. This document should appear in the RFI detail panel under a section called Response Documents with a Download Response PDF button. The response PDF must include the original RFI header, the full question, and the complete official response with all fields. Name the response document using the project naming convention — take the discipline, level, and type from the RFI reference information if available, otherwise use default values. The file name format should be PROJECTCODE-ORIGINATOR-DISCIPLINE-LEVEL-RESPONSE-RFINUMBER-DATE.pdf. Store the generated PDF reference in the database linked to the RFI. Show it in the RFI detail panel so the user can download it at any time.
Fix 7 — Response document naming convention. Response documents generated by BIMLog must follow the project naming convention. When generating the response PDF automatically apply the convention separator and use the field values from the RFI context. If the convention has a Status field use S2 for official response documents. If it has a Type field use RP for response or report. Log the auto-generated response document in the activity log and in the file intelligence system so the AI can reference it.
Then rebuild and redeploy. Do not change anything else.