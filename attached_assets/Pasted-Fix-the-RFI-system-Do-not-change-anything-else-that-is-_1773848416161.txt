Fix the RFI system. Do not change anything else that is currently working.
Fix 1 — PDF blank pages. The PDF export is printing a blank page between every content page. This is a page break bug. Find the makeRfiPdf function in the API server and fix the page overflow logic. Every time a new page is added make sure the y position resets to the top margin correctly. Test the export and confirm zero blank pages before deploying.
Fix 2 — Export All button behavior. The Export All button on the RFI list and log page should export the RFI LOG as a document — meaning a summary list of all RFIs with their key fields in a table format. It should NOT export the full content of each individual RFI. The exported log should show RFI number, subject, status, priority, submitted by, submitted to, date requested, date required, days outstanding, ball in court, schedule impact — one row per RFI. Available in PDF, Excel, and Word format as currently built.
Fix 3 — Individual RFI export. Each RFI row in both list and log view should have a small Export PDF button that exports THAT individual RFI as a full formatted PDF document. This is already partially built — make sure it works correctly and exports only that one RFI with zero blank pages.
Fix 4 — RFI Response text field. In the RFI detail panel response section — add a large text area labeled Official Response where the user can type the formal response to the RFI. This field is required before the status can be changed to Responded. Below the text area add an AI Assist button. When clicked — send the full RFI content including subject, question, reference information, and cost and schedule impact to the Claude API and ask it to draft a professional formal RFI response. Insert the generated text into the response text area. The user can edit it before saving.
Fix 5 — Auto-generate response document. When the user clicks Save Response and the Official Response text is filled in — automatically do all of these things: Save the response text to the database. Generate a formatted PDF response document that includes the original RFI header information, the full question, and the official response with the answering party name company and date. Store this PDF reference in the database linked to the RFI. Send an email notification to everyone on the distribution list with the response summary and a link to view the full RFI. Log this entire event in the activity log with full details for AI reporting. The email sending should use the existing email system if configured or log the email as pending if not yet configured.
Fix 6 — Word export with response section. When a single RFI is exported to Word — include a clearly marked Response Section at the bottom of the document with the official response text if one exists. If no response exists yet — include a blank Response section with fields for Response, Answered By, Date, Cost Impact, and Schedule Impact so the recipient can fill it in manually and return it. This makes the Word export useful for external parties who respond outside the system.
Then rebuild and redeploy. Do not change anything else.