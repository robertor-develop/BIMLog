Fix the RFI exports completely. This is the final definitive fix. Do not use LibreOffice conversion anywhere. Remove it entirely. Here is exactly what each button must do.
PDF BUTTON — Individual RFI PDF using PDFKit only. No LibreOffice. No docx conversion. Build a clean PDFKit PDF with these exact sections in order. Section 1 — navy header bar full width with REQUEST FOR INFORMATION on the left and the RFI number on the right in white text. Section 2 — two column info grid showing Project, Status, Priority, Date Requested, Date Required, Submitted By, Submitted To, Drawing Number, Spec Section — each label in small gray uppercase, value in dark text below it. Section 3 — full width section with heading DESCRIPTION OF QUESTION in a navy bar. Below it the question text in plain text — strip ALL markdown formatting before rendering — remove every double asterisk, every single asterisk, every hash symbol, every bracket. Just plain clean text. Section 4 — full width section with heading OFFICIAL RESPONSE in a green bar. Show the response text in plain text — same markdown stripping. Below the response text show a 4-column row with Answered By, Date of Response, Cost Impact, Schedule Impact — each as label and value. Section 5 — IMPACT AND ATTACHMENTS in a navy bar. Two columns side by side. Left column shows Cost Impact options as text checkboxes using the unicode character 2610 for unchecked and 2611 for checked — No Cost Impact, Cost Increase TBD, Cost Increase Known, Cost Decrease. Right column shows Schedule Impact options same way — No Schedule Impact, Increase in Calendar Days with the number, Decrease in Calendar Days. Below that a full width Attachments section with the same checkbox style. Section 6 — footer with BIMLog by IgniteSmart, the RFI number, and today's date. This PDF must be clean professional and have NO placeholder tokens, NO markdown asterisks, NO bracket text anywhere. Replace every instance of [Current Date] with the actual formatted date. Replace every instance of [RFI Number] with the actual RFI number. Replace every instance of [Project] with the actual project name.
WORD BUTTON — Individual RFI Word document using docx library only. No LibreOffice. Same content as the PDF in the same order. For the checkboxes use SymbolRun with char A8 for unchecked and FC for checked with symbolfont Wingdings and size 20. Each checkbox option is a standalone Paragraph with two children — the SymbolRun and a TextRun with a space and the label text. The signature section at the bottom has Name, Title, Company, Date each as a Paragraph with the label followed by 40 underscore characters. Strip all markdown from all text fields before inserting into the Word document.
RESP PDF BUTTON — Remove this button entirely. Delete it from the RFI detail panel header. It serves no purpose distinct from the PDF button and causes confusion.
RESP PDF ENDPOINT — Remove the endpoint entirely from the backend. Clean it up.
RFI LOG PDF — PDFKit only, landscape Letter, 11 columns matching Excel exactly — RFI Number, Subject, Status, Priority, Submitted By, Submitted To, Date Requested, Date Required, Days Outstanding, Ball In Court, Schedule Impact. Column widths must sum to 720 points. Subject column gets the most space. All text truncated with ellipsis if too long. No column cut off. No text overflow into adjacent cells. lineBreak false on every cell. Font size 8 for data rows.
RFI LOG WORD — Same 11 columns as PDF log. Landscape orientation. Table with header row in navy background white text. Data rows alternating white and light gray. Font size 9. All columns visible.
MARKDOWN STRIPPING — Create one shared helper function called stripMarkdown that takes a string and returns the string with all of the following removed — double asterisks, single asterisks, hash symbols at start of line, square brackets and their contents only when they contain placeholder text like Current Date or RFI Number or Project, leading and trailing whitespace on each line. Call this function on every text field before rendering in both PDF and Word exports.
Then run TypeScript check on both frontend and backend with zero errors. Then rebuild and redeploy. Show me exact output of these checks:
Check 1 — stripMarkdown function exists: grep -n "stripMarkdown|strip.*markdown|markdown.*strip" artifacts/api-server/src/routes/rfis.ts | head -5
Check 2 — Resp PDF button removed from frontend: grep -n "Resp.*PDF|resp.*pdf|respPdf|RespPDF" artifacts/bimlog/src/pages/project/RfisTab.tsx | head -5
Check 3 — LibreOffice removed from exports: grep -n "libreoffice|LibreOffice|execSync|convert-to" artifacts/api-server/src/routes/rfis.ts | head -5
Check 4 — PDF uses unicode checkboxes 2610 2611: grep -n "2610|2611|☐|☑|unicode.*check|check.*unicode" artifacts/api-server/src/routes/rfis.ts | head -5
Check 5 — Word uses SymbolRun Wingdings: grep -n "SymbolRun|Wingdings|FC|A8" artifacts/api-server/src/routes/rfis.ts | head -5
Check 6 — RFI log has 11 columns summing to 720: grep -n "720|11.*column|column.*11|Ball In Court|Schedule Impact" artifacts/api-server/src/routes/rfis.ts | head -10
Show exact output of all 6 checks. Nothing else.