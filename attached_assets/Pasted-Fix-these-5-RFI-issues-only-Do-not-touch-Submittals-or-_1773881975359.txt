Fix these 5 RFI issues only. Do not touch Submittals or Files. Do not change anything else.
Fix 1 — RFI Word export real checkboxes. The current Word export lists cost impact, schedule impact, and attachments as plain text. Replace them with actual Word checkbox form fields. In the docx library use the following pattern for each checkbox row — create a Paragraph with two runs: first run is a SymbolRun with char "A8" for unchecked using font Wingdings size 20, second run is a TextRun with a space and the label text. When the value matches the saved RFI data use char "FC" for checked instead of "A8". Apply this pattern to all Cost Impact options — No Cost Impact, Cost Increase TBD, Cost Increase Known with amount on same line, Cost Decrease. Apply to all Schedule Impact options — No Schedule Impact, Increase in Calendar Days with number field on same line, Decrease in Calendar Days with number field on same line. Apply to all Attachment options — See marked up drawings, See attached documents, See attached calculations, Other with blank line after. Add a Signature section at the very bottom with two rows — Authorized by with a long underline and Date with a short underline — both created using repeated underscore characters as a TextRun.
Fix 2 — Audit Certificate PDF column overflow. In the audit certificate PDF the Company column text is overflowing and overlapping the next row. Fix the column widths in the view events table. The table has 4 columns — number, timestamp, user, company. Set fixed column widths — number 30 points, timestamp 140 points, user 160 points, company 160 points. Truncate any text that exceeds the column width with ellipsis. Also ensure the certification statement box at the bottom has enough padding and the text does not overflow the box boundaries.
Fix 3 — Add Response button visible and clear. In the RFI detail panel the user cannot figure out how to add a second response. Add a clearly visible button directly below the last response in the Responses section. The button should say Add Response with a plus icon, styled in blue outline, full width of the responses container. When clicked it opens an inline form directly below — a text area for the response, a Submit Response button in blue, and a Cancel button. Do not scroll anywhere. The form appears inline immediately below the Add Response button. After submitting the new response appears above the Add Response button in the list. The Add Response button is always visible at the bottom of the responses section as long as the RFI is not closed.
Fix 4 — Close RFI button visible for project admin. The Close RFI button is currently not visible even for project admin. Find where the isProjectAdmin check was added and verify the Close RFI button renders when isProjectAdmin is true and the RFI status is not already closed. If the button is not rendering — add it explicitly to the RFI detail panel header row next to the existing export buttons — PDF, Word, Resp PDF, Audit Cert. Style it as a red outline button that says Close RFI. When clicked show a confirmation dialog — Are you sure you want to close this RFI? This cannot be undone. On confirm set the status to closed and log the event in the activity trail with who closed it and when.
Fix 5 — Official Response PDF layout cleanup. The response PDF currently shows placeholder text in double asterisks like RESPONSE TO RFI-0001 and Project 270 Park Avenue and RFI Number and Date of Response Current Date as literal text instead of real values. Fix the response PDF generation to replace all placeholder tokens with actual values from the RFI record. Replace Project with the actual project name. Replace RFI Number with the actual RFI number. Replace Date of Response with the actual response date formatted as Month Day Year. Remove all double asterisk markdown formatting from the output — no bold asterisks, no markdown headers. The PDF should render clean professional text only.
Then run TypeScript check on both frontend and backend. Then rebuild and redeploy. Show me exact output of these 5 checks:
Check 1 — Word checkboxes use SymbolRun: grep -n "SymbolRun|Wingdings|FC|A8|checkbox" artifacts/api-server/src/routes/rfis.ts | head -10
Check 2 — Audit certificate column widths fixed: grep -n "columnWidth|column.*width|30|140|160|truncate|ellipsis" artifacts/api-server/src/routes/rfis.ts | head -10
Check 3 — Add Response button in frontend: grep -n "Add Response|addResponse|add.*response|response.*add|inline.*form|responseForm" artifacts/bimlog/src/pages/project/RfisTab.tsx | head -10
Check 4 — Close RFI button rendering: grep -n "Close RFI|closeRfi|isProjectAdmin|red.*outline|close.*button" artifacts/bimlog/src/pages/project/RfisTab.tsx | head -10
Check 5 — Response PDF placeholder tokens replaced: grep -n "projectName|project.name|rfiNumber|rfi.number|responseDate|Current Date" artifacts/api-server/src/routes/rfis.ts | head -10
Show exact output of all 5 checks. Nothing else.