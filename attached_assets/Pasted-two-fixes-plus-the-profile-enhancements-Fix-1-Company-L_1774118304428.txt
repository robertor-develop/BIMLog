two fixes plus the profile enhancements:
Fix 1 — Company Logo and Profile Photo must be file uploads not URL inputs. Replace the Company Logo URL text input with a file upload button that accepts jpg and png. When the user selects a file convert it to base64 and store it in company_logo_url. Show a preview of the uploaded logo below the button. Do the same for the profile photo — add a clickable upload area around the avatar circle that lets the user pick a photo. Convert to base64 and store in avatar_url. Show the uploaded photo in the avatar circle immediately.
Fix 2 — Add these sections to the Profile page after the Performance Score section and before Personal Information:
Section A — My Projects. A list of all projects this user is a member of. Each row shows project name, project code, the user's role on that project, and a Go to Project button that navigates to that project. Fetch from GET /api/v1/projects.
Section B — Pending Items. Three sub-sections. First — RFIs awaiting my response: fetch all RFIs across all user projects where submitted_to_email matches the user's email and status is open or in_review. Show RFI number, subject, project name, days outstanding, and a View button. Second — Submittals awaiting my review: fetch all submittals where submitted_to_email matches and status is pending. Show submittal number, title, project name, days outstanding, and a View button. Third — Files flagged for my attention: fetch all files where content_verification_result is possible_mismatch or clear_mismatch and the user is a project admin on that project. Show file name, project name, flag reason, and a View button.
Section C — Recent Activity. Last 10 activity log entries across all projects where the user_id matches the current user. Show timestamp, action type badge, details, and project name. Fetch by calling GET /api/v1/projects for each project then GET activity for each, merge and sort by timestamp descending, take top 10.
Then rebuild and redeploy. Show exact output of these checks:
Check 1 — File upload for avatar exists: grep -n "fileInput|file.*upload|upload.*avatar|avatar.*upload|FileReader|base64.*avatar" artifacts/bimlog/src/pages/Profile.tsx | head -10
Check 2 — File upload for company logo exists: grep -n "logo.*upload|upload.*logo|FileReader.*logo|base64.*logo|company.*file" artifacts/bimlog/src/pages/Profile.tsx | head -10
Check 3 — My Projects section exists: grep -n "My Projects|myProjects|user.*projects|projects.*user|Go to Project" artifacts/bimlog/src/pages/Profile.tsx | head -10
Check 4 — Pending Items section exists: grep -n "Pending Items|pendingRfis|pendingSubmittals|awaiting.*response|awaiting.*review" artifacts/bimlog/src/pages/Profile.tsx | head -10
Check 5 — Recent Activity section exists: grep -n "Recent Activity|recentActivity|activity.*profile|profile.*activity" artifacts/bimlog/src/pages/Profile.tsx | head -10
Show exact output of all 5 checks. Nothing else.