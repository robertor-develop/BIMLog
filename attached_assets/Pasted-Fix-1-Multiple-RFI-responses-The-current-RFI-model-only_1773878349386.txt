Fix 1 — Multiple RFI responses. The current RFI model only allows one response. Change it to allow multiple responses in sequence. Add a new table called rfi_responses with columns: id serial primary key, rfi_id integer not null references rfis id, response_number integer not null, response_text text not null, responded_by_user_id integer not null references users id, responded_by_name text not null, responded_by_company text not null, created_at timestamp not null default now, is_conflict_of_interest boolean default false. Run migration immediately. Update the RFI detail panel to show all responses in chronological order — each one showing the response number, responder name, company, timestamp, and response text. Any project member with write access can add a response. When adding a response — if the responding user's company matches the submitted_by_company on the RFI — set is_conflict_of_interest to true and show the amber conflict warning on that specific response. The existing response field on the rfis table stays for backward compatibility but new responses use the rfi_responses table. Show all rfi_responses in the Viewed By and Response section of the Audit Certificate.
Fix 2 — Close RFI restricted to Project Admin only. Currently the Close RFI button shows for any canWrite user. Change it so it only shows for users with the project_admin role. Check the user's role from the project members data. Only project_admin can close an RFI. All other roles cannot see the Close button even if they have write access.
Fix 3 — RFI Word export checkboxes must be real Word checkbox fields. Currently cost impact and schedule impact are written as plain text rows in a table. Replace them with actual Word checkbox form fields using the docx library. For Cost Impact — add 4 checkbox rows: No Cost Impact, Cost Increase TBD, Cost Increase Known with an Amount field next to it, Cost Decrease. For Schedule Impact — add 3 checkbox rows: No Schedule Impact, Increase in Calendar Days with a number field next to it, Decrease in Calendar Days with a number field next to it. For Attachments — add 4 checkbox rows: See marked up drawings, See attached documents, See attached calculations, Other with a fillable line. Each checkbox must be a real docx CheckBox element — not a unicode character, not a bullet point. Use the docx library's CheckBox component or the equivalent field code. Add a Signature section at the bottom with Authorized by underline field and Date underline field.
Then run TypeScript check on both frontend and backend. Then rebuild and redeploy. Show me exact output of these checks:
Check 1 — rfi_responses table exists: SELECT EXISTS (SELECT FROM information_schema.tables WHERE table_name = 'rfi_responses');
Check 2 — Multiple response endpoint exists: grep -rn "rfi_responses|rfiResponses|response_number|responseNumber" artifacts/api-server/src/routes/rfis.ts | head -10
Check 3 — Multiple responses shown in frontend: grep -n "rfiResponses|responseNumber|response_number|Response 1|additionalResponses" artifacts/bimlog/src/pages/project/RfisTab.tsx | head -10
Check 4 — Close RFI restricted to project_admin only: grep -n "project_admin|projectAdmin|isAdmin|admin.*close|close.*admin" artifacts/bimlog/src/pages/project/RfisTab.tsx | head -10
Check 5 — Word export has real checkbox fields: grep -n "CheckBox|checkBox|checkbox|SymbolRun|fieldCode|FORMCHECKBOX" artifacts/api-server/src/routes/rfis.ts | head -10
Show exact output of all 5 checks. Nothing else.