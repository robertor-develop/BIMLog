Build the BIMLog Super Admin panel. This is a protected page at route /admin accessible only to users where is_super_admin is true in the users table. Add is_super_admin boolean column defaulting to false to the users table and run migration immediately. Set Roberto Rodriguez's account — email robertor@rryasociados.com — is_super_admin to true immediately via a direct database update.
Add isSuperAdmin middleware to the backend that checks the requesting user's is_super_admin field. Return 403 if not super admin.
Add a small Admin link in the navbar visible only when the logged-in user has is_super_admin true. Style it as a subtle red or orange badge so it is clearly a special access link.
Build the admin panel as a tabbed interface with these 8 tabs:
Tab 1 — Platform Overview. Show these stat cards: Total Users, Total Companies, Total Projects, Total Files Processed, Total RFIs, Total Submittals, Active Projects, Files Last 24 Hours, RFIs Last 7 Days. Below the stats show a live activity feed of the last 50 events across all projects — same format as the project activity log but showing all projects with the project name as an additional column.
Tab 2 — Users. Table showing all users: Full Name, Email, Company, Role badge, Date Joined, Last Active, Projects count. Search bar at top. Actions per row: Reset Password button that opens a modal to enter a new password, Change Role dropdown, Deactivate toggle, Delete button with confirmation. Add a Create User button at top right that opens a form with Full Name, Email, Password, Company Name, Role.
Tab 3 — Companies. Table showing all companies: Company Name, Number of Users, Number of Projects, Date Created, Logo if available. Actions: Edit company name and details, Delete company with confirmation warning showing how many users will be affected.
Tab 4 — Projects. Table showing all projects: Project Name, Code, Company, Status badge, Members count, Files count, RFIs count, Submittals count, Created Date. Actions: View Project button linking to the project, Archive Project, Delete Project with confirmation, Transfer Ownership — opens modal to select new owner from users list.
Tab 5 — Email Log. Table showing all emails sent by the platform: To email, Subject, Trigger Type badge, Timestamp, Status badge — Delivered green, Bounced red, Failed red, Pending gray. Filter by date range, trigger type, and status. Add a new table called email_log to the database with columns id, to_email, subject, trigger_type, status, error_message, sent_at. Run migration immediately. Update the sendEmail function in the email service to log every send attempt to this table with the result.
Tab 6 — Activity Feed. Full platform activity — every action across every project. Same columns as project activity log plus Project Name and Company Name. Filter by project, company, user, action type, date range. Pagination — 50 rows per page.
Tab 7 — Feature Flags. A list of toggle switches for platform features that can be enabled or disabled globally or per company. Initial flags: AI Pre-Submission Check enabled, AI Name Suggestion enabled, Audit Certificate enabled, Email Notifications enabled, Rapid Approval Detection enabled, Procurement Before Approval Warning enabled. Store flags in a new table called feature_flags with columns id, flag_name, enabled, applies_to — global or company_id, updated_at, updated_by. Run migration immediately. Show each flag as a row with the flag name, description, current status toggle, and scope selector.
Tab 8 — Admin Actions Log. Every action taken through this admin panel is logged — who did it, what they did, to which record, and when. Table with columns: Admin Name, Action, Target Type, Target ID, Details, Timestamp. This log is immutable — no delete button. Add a new table called admin_actions_log with columns id, admin_user_id, admin_email, action, target_type, target_id, details jsonb, created_at. Run migration immediately. Every destructive action — delete user, delete project, reset password, change role, archive project — must be logged to this table before the action is executed.
Also add an Information dropdown to the navbar visible to all logged-in and non-logged-in users. Position it between Help and the avatar in the logged-in navbar, and between Help and Get Started in the non-logged-in navbar. When clicked show a dropdown with these links: How It Works linking to /setup-guide, Pricing scrolling to pricing section on landing page, Privacy Policy linking to /privacy, Terms of Service linking to /terms, Platform Disclaimer linking to /disclaimer, Data Retention Policy linking to /data-retention, Contact linking to mailto:info@ignitesmart.ai, About linking to ignitesmart.ai. Style the dropdown consistently with the rest of the navbar.
Then run TypeScript check on both frontend and backend with zero errors. Then rebuild and redeploy. Show exact output of these checks:
Check 1 — is_super_admin column exists: SELECT column_name FROM information_schema.columns WHERE table_name = 'users' AND column_name = 'is_super_admin';
Check 2 — Roberto set as super admin: SELECT email, is_super_admin FROM users WHERE email = 'robertor@rryasociados.com';
Check 3 — Admin route exists: grep -rn "/admin|AdminPanel|isSuperAdmin" artifacts/bimlog/src/pages/ | head -10
Check 4 — isSuperAdmin middleware exists: grep -n "isSuperAdmin|is_super_admin|superAdmin" artifacts/api-server/src/routes/auth.ts artifacts/api-server/src/middlewares/ | head -10
Check 5 — Email log table exists: SELECT EXISTS (SELECT FROM information_schema.tables WHERE table_name = 'email_log');
Check 6 — Feature flags table exists: SELECT EXISTS (SELECT FROM information_schema.tables WHERE table_name = 'feature_flags');
Check 7 — Admin actions log table exists: SELECT EXISTS (SELECT FROM information_schema.tables WHERE table_name = 'admin_actions_log');
Check 8 — Information dropdown in navbar: grep -n "Information|info.*dropdown|dropdown.*info|How It Works|Privacy Policy" artifacts/bimlog/src/components/layout/Navbar.tsx | head -10
Show exact output of all 8 checks. Nothing else.