READ THIS BEFORE TOUCHING ANYTHING.

YOU WILL DO EXACTLY WHAT IS WRITTEN HERE. NOTHING MORE. NOTHING LESS.
DO NOT ADD FEATURES. DO NOT IMPROVE THINGS. DO NOT REFACTOR OTHER FILES.
DO NOT TOUCH ANY FILE NOT EXPLICITLY LISTED BELOW.
DO NOT PUBLISH UNTIL ALL CHECKS PASS.
PRINT ALL OUTPUT AS PLAIN TEXT IN THE CHAT. NO COLLAPSED BOXES. NO SUMMARIES. EVERY LINE RAW.
IF YOU DO ANYTHING NOT IN THIS INSTRUCTION YOU HAVE FAILED.

═══════════════════════════════════════════════════════════
STEP 1 — ADD OPENAI API KEY TO USER PROFILE (BACKEND)
═══════════════════════════════════════════════════════════

In the Drizzle schema, find the users table.
Add one column:
  openai_api_key: text, nullable

Push schema to DB.

Check 1 — confirm column exists:
SELECT column_name FROM information_schema.columns
WHERE table_name = 'users' AND column_name = 'openai_api_key';

Print raw output in chat.

═══════════════════════════════════════════════════════════
STEP 2 — ADD OPENAI KEY TO PROFILE ROUTE
═══════════════════════════════════════════════════════════

Find the profile/user update route in artifacts/api-server/src/routes/auth.ts
or wherever PATCH /auth/profile or PATCH /users/:id is handled.

When the user updates their profile, if body.openai_api_key is present:
- If it is an empty string, save null
- If it is a non-empty string, save it as-is (user owns their key)

When GET /auth/me is called, return openai_api_key as:
- null if not set
- "configured" if set (never return the actual key to the frontend for security)

Add a new endpoint:
POST /auth/openai-key
Body: { key: string }
Auth: authMiddleware required
- Validates key is non-empty string starting with "sk-"
- Saves to users table for req.user.userId
- Returns { success: true }

DELETE /auth/openai-key
Auth: authMiddleware required
- Sets openai_api_key = null for req.user.userId
- Returns { success: true }

Check 2:
grep -n "openai_api_key\|openai-key" artifacts/api-server/src/routes/auth.ts | head -20

Print raw output in chat.

═══════════════════════════════════════════════════════════
STEP 3 — ADD OPENAI KEY FIELD TO PROFILE PAGE (FRONTEND)
═══════════════════════════════════════════════════════════

File: artifacts/bimlog/src/pages/Profile.tsx

Find the section where API token is shown (there is already an API token section).
After that section, add a new card for OpenAI API Key.

The card must look exactly like this:

Title: "OpenAI API Key"
Subtitle: "Required for audio transcription in Meeting Minutes. Your key is used only for your account — you pay OpenAI directly at $0.006/minute (~$0.36 per hour of audio)."

If key is NOT configured (user.openai_api_key === null):
  Show an input field with placeholder "sk-..."
  Show a "Save Key" button
  Show a note in gray: "Get your key at platform.openai.com/api-keys"

If key IS configured (user.openai_api_key === "configured"):
  Show a green badge: "✓ Configured"
  Show a "Remove Key" button in red outline
  Do NOT show the actual key

On Save Key:
  POST /auth/openai-key with { key: inputValue }
  On success: show green toast "OpenAI API key saved"
  Refresh user data

On Remove Key:
  DELETE /auth/openai-key
  On success: show toast "OpenAI API key removed"
  Refresh user data

Check 3:
grep -n "openai\|OpenAI\|openai_api_key" artifacts/bimlog/src/pages/Profile.tsx | head -20

Print raw output in chat.

═══════════════════════════════════════════════════════════
STEP 4 — ADD AUDIO TRANSCRIPTION ENDPOINT (BACKEND)
═══════════════════════════════════════════════════════════

In artifacts/api-server/src/routes/meeting_minutes.ts

Add new endpoint:
POST /projects/:projectId/meetings/transcribe-audio

Accepts multipart file upload (field name: "audio")
Supported formats: .mp3, .mp4, .m4a, .wav, .webm, .ogg
Max file size: 25MB (Whisper API limit)

LOGIC:
1. Get the logged-in user's openai_api_key from the users table
   If null → return 400 { error: "no_openai_key", message: "OpenAI API key not configured" }

2. Send the audio file to OpenAI Whisper API:
   POST https://api.openai.com/v1/audio/transcriptions
   Headers: Authorization: Bearer {user's openai_api_key}
   Body: multipart form with file + model: "whisper-1"

3. Get transcript text back from Whisper

4. Send transcript to Claude API with this prompt:
   System: "You are a construction project coordinator assistant.
   Extract structured meeting information from this transcript.
   Return ONLY valid JSON, no markdown, no explanation."

   User: "Extract from this meeting transcript:
   {transcript}

   Return this exact JSON structure:
   {
     'title': 'meeting title or topic if mentioned',
     'agenda': ['item 1', 'item 2'],
     'attendees': [{ 'trade': '', 'company': '', 'fullName': '', 'role': '', 'email': '', 'phone': '' }],
     'rfis': [{ 'rfiNumber': '', 'description': '', 'status': 'PENDING', 'responsible': '' }],
     'deliverables': [{ 'floor': '', 'description': '', 'plumbing': '', 'hvac': '', 'fireProt': '', 'electrical': '', 'other': '', 'coordinator': '', 'deadline': '' }],
     'viewpoints': [{ 'floor': '', 'responsible': '', 'holdUps': '', 'viewpoint': '', 'description': '', 'deadline': '' }],
     'aiSummary': 'two sentence summary of the meeting'
   }
   For deliverable status fields use only: PENDING, COMPLETE, N/A, or empty string.
   For deadlines use MM-DD-YY format if mentioned.
   If information is not mentioned, use empty string or empty array."

5. Parse Claude response as JSON
6. Return the parsed JSON to the frontend

Check 4:
grep -n "transcribe\|whisper\|openai_api_key" artifacts/api-server/src/routes/meeting_minutes.ts | head -20

Print raw output in chat.

═══════════════════════════════════════════════════════════
STEP 5 — ADD AUDIO UPLOAD TO MEETINGS TAB (FRONTEND)
═══════════════════════════════════════════════════════════

File: artifacts/bimlog/src/pages/project/MeetingsTab.tsx

DO NOT REWRITE THIS FILE. Make only these additions:

ADDITION A — Add these state variables after the existing useState declarations:
  const [audioUploading, setAudioUploading] = useState(false);
  const [showNoKeyModal, setShowNoKeyModal] = useState(false);
  const [audioProgress, setAudioProgress] = useState("");

ADDITION B — Add this function after the loadMeetings function:
  const handleAudioUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (!file) return;
    const allowed = [".mp3",".mp4",".m4a",".wav",".webm",".ogg"];
    const ext = "." + file.name.split(".").pop()?.toLowerCase();
    if (!allowed.includes(ext)) {
      setError("Unsupported format. Use MP3, MP4, M4A, WAV, WebM, or OGG.");
      return;
    }
    if (file.size > 25 * 1024 * 1024) {
      setError("File too large. Maximum 25MB.");
      return;
    }
    setAudioUploading(true);
    setAudioProgress("Uploading audio...");
    setError("");
    try {
      const formData = new FormData();
      formData.append("audio", file);
      const r = await fetch(`${API}/projects/${projectId}/meetings/transcribe-audio`, {
        method: "POST",
        headers: { Authorization: `Bearer ${token}` },
        body: formData,
      });
      if (!r.ok) {
        const d = await r.json();
        if (d.error === "no_openai_key") {
          setShowNoKeyModal(true);
        } else {
          setError(d.message || "Transcription failed");
        }
        return;
      }
      setAudioProgress("Extracting meeting data with AI...");
      const data = await r.json();
      if (data.title) setTitle(data.title);
      if (data.agenda?.length) setAgendaItems(data.agenda);
      if (data.attendees?.length) setAttendees(data.attendees);
      if (data.rfis?.length) setRfis(data.rfis);
      if (data.deliverables?.length) setDeliverables(data.deliverables);
      if (data.viewpoints?.length) setViewpoints(data.viewpoints);
      if (data.aiSummary) setAiSummaryText(data.aiSummary);
      setAudioProgress("");
    } catch {
      setError("Audio upload failed. Please try again.");
    } finally {
      setAudioUploading(false);
      setAudioProgress("");
      e.target.value = "";
    }
  };

ADDITION C — Add the No Key Modal just before the final return closing </div>
in the NEW MEETING FORM view (the return that starts with the form header).
Add it right before the last </div>:

  {showNoKeyModal && (
    <div style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,0.5)",
      display: "flex", alignItems: "center", justifyContent: "center", zIndex: 1000 }}>
      <div style={{ background: "white", borderRadius: 12, padding: 28,
        maxWidth: 480, width: "90%", boxShadow: "0 20px 60px rgba(0,0,0,0.2)" }}>
        <div style={{ fontSize: 18, fontWeight: 800, marginBottom: 8, color: "#111827" }}>
          OpenAI API Key Required
        </div>
        <div style={{ fontSize: 14, color: "#6B7280", lineHeight: 1.6, marginBottom: 20 }}>
          Audio transcription uses OpenAI Whisper. You need to add your own
          OpenAI API key in your Profile to use this feature.
          <br /><br />
          <strong>Cost:</strong> ~$0.006 per minute of audio (~$0.36 per hour).
          You pay OpenAI directly — BIMLog never charges for transcription.
          <br /><br />
          <strong>How to get a key:</strong> Go to{" "}
          <a href="https://platform.openai.com/api-keys" target="_blank"
            rel="noreferrer" style={{ color: "#2563EB" }}>
            platform.openai.com/api-keys
          </a>
        </div>
        <div style={{ display: "flex", gap: 10 }}>
          <button className="btn btn-primary" onClick={() => { setShowNoKeyModal(false); window.location.href = "/profile"; }}>
            Go to Profile → Add Key
          </button>
          <button className="btn btn-outline" onClick={() => setShowNoKeyModal(false)}>
            Cancel
          </button>
        </div>
      </div>
    </div>
  )}

ADDITION D — In the NEW MEETING FORM view, find the meeting info row card
(the white card with Meeting #, Title, Date, Time, Location fields).
Add an "Upload Audio" button AFTER that card and BEFORE the Agenda section.
Insert this block between the info card and the first SectionHeader:

  <div style={{ background: "#F0F7FF", border: "1px solid #BFDBFE",
    borderRadius: 10, padding: 16, marginBottom: 12,
    display: "flex", alignItems: "center", justifyContent: "space-between" }}>
    <div>
      <div style={{ fontWeight: 700, fontSize: 14, color: "#1E40AF", marginBottom: 2 }}>
        Upload Meeting Recording
      </div>
      <div style={{ fontSize: 12, color: "#3B82F6", lineHeight: 1.5 }}>
        Upload an audio file and AI will auto-fill this entire form.
        Supports MP3, MP4, M4A, WAV. Max 25MB.
        {audioProgress && <span style={{ display: "block", marginTop: 4, fontWeight: 600 }}>{audioProgress}</span>}
      </div>
    </div>
    <label style={{ cursor: audioUploading ? "not-allowed" : "pointer" }}>
      <input type="file" accept=".mp3,.mp4,.m4a,.wav,.webm,.ogg"
        onChange={handleAudioUpload} disabled={audioUploading}
        style={{ display: "none" }} />
      <div className="btn btn-primary" style={{
        opacity: audioUploading ? 0.6 : 1,
        display: "flex", alignItems: "center", gap: 6, pointerEvents: audioUploading ? "none" : "auto"
      }}>
        {audioUploading ? "Processing..." : "Upload Audio"}
      </div>
    </label>
  </div>

Check 5:
grep -n "handleAudioUpload\|showNoKeyModal\|audioUploading\|audioProgress\|transcribe-audio" artifacts/bimlog/src/pages/project/MeetingsTab.tsx | head -20

Print raw output in chat.

═══════════════════════════════════════════════════════════
STEP 6 — ADD TO SETUP GUIDE
═══════════════════════════════════════════════════════════

File: artifacts/bimlog/src/pages/SetupGuide.tsx

Find where features or integrations are documented.
Add a new section or card titled "Audio Transcription — Meeting Minutes"
with this exact content:

Title: "Audio Transcription — Meeting Minutes"
Body: "Upload a recording of your coordination meeting and BIMLog will
automatically fill in the attendees, agenda, RFIs, deliverables,
viewpoints, and action items using AI.

Requires your own OpenAI API key — add it once in your Profile.
Cost: approximately $0.006 per minute of audio (~$0.36 per hour).
You pay OpenAI directly. BIMLog does not charge for transcription.

Supported formats: MP3, MP4, M4A, WAV, WebM, OGG. Maximum 25MB per file.

To get started: Profile → OpenAI API Key → paste your key → Save."

Check 6:
grep -n "Audio\|Transcription\|OpenAI\|Whisper" artifacts/bimlog/src/pages/SetupGuide.tsx | head -10

Print raw output in chat.

═══════════════════════════════════════════════════════════
FINAL CHECKS
═══════════════════════════════════════════════════════════

Check 7 — TypeScript errors:
cd artifacts/bimlog && npx tsc --noEmit 2>&1 | wc -l

Check 8 — New DB column confirmed:
SELECT column_name FROM information_schema.columns
WHERE table_name = 'users' AND column_name = 'openai_api_key';

Check 9 — All new endpoints exist:
grep -n "openai-key\|transcribe-audio" artifacts/api-server/src/routes/auth.ts artifacts/api-server/src/routes/meeting_minutes.ts

REBUILD BOTH FRONTEND AND BACKEND.
RESTART API SERVER.
PUBLISH.
PRINT ALL CHECK OUTPUT AS PLAIN TEXT IN THE CHAT. NO COLLAPSED BOXES.