I am attaching an image showing exactly how the Name Generator and Convention Builder should look. Build both pages to match this image exactly. Here are the specific requirements:
Name Generator — build it exactly like the image shows. It must have a title "BIM file name generator" at the top with subtitle "Builds structured file names following ISO 19650 / BS 1192 conventions". It must have these labeled input fields in a grid layout: PROJECT CODE as a free text input, ORIGINATOR as a free text input, VOLUME / SYSTEM as a dropdown, LEVEL / LOCATION as a dropdown, TYPE / DOCUMENT CLASS as a dropdown, ROLE / DISCIPLINE as a dropdown, STATUS / SUITABILITY as a dropdown, REVISION as a text input, SOFTWARE / FORMAT as a dropdown, SEQUENCE NO as a text input. Below the inputs show a Generated file name box with the assembled name in monospace and colored token chips separated by dots showing each field value. Below that show Copy file name, Save to list, and Reset buttons. At the bottom show a Naming structure bar with colored chips showing Project, Originator, Volume, Level, Type, Role, Seq, Status, Rev, ext separated by dots.
Convention Builder — must have the title Naming Convention Builder. Must have separator selector and active toggle with Enforcing uploads badge. Must have a LIVE PREVIEW panel that shows colored token chips assembled into the full file name in large monospace. Must have a Naming Fields section with numbered rows — each row has a position number badge, a field label input, and a comma separated allowed values input that shows the values as small blue monospace chips below the input as you type. Must have Add Field button, Discard changes button, and Save Convention button. Unsaved changes amber badge in the top right when changes are made.
Replace artifacts/bimlog/src/pages/project/NameGenerator.tsx and artifacts/bimlog/src/pages/project/ConventionBuilder.tsx with completely rewritten versions matching these requirements exactly. Then rebuild and redeploy. Do not change anything else.