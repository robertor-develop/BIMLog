Build the Profile section. This is a new page at route /profile accessible from the sidebar. Add a Profile link in the sidebar below the user avatar and name at the bottom left. Style it consistently with the rest of the sidebar navigation.
The Profile page has 7 sections:
Section 1 — Personal Profile. Fields: Full Name text input pre-populated from current user, Job Title text input, Phone text input, Email read-only showing current user email with note "Contact support to change your email". Profile photo upload — circular avatar, accepts jpg and png, stored as base64 in a new column called avatar_url text nullable on the users table. Run migration immediately. Save button calls PATCH /api/v1/users/me.
Section 2 — Company Profile. Fields: Company Name text input pre-populated from current user company, Company Website text input, Company Address text input, Company Phone text input. Company Logo upload — accepts jpg and png, stored in a new column called company_logo_url text nullable on the companies table. Run migration immediately. Save button calls PATCH /api/v1/users/me/company.
Section 3 — Signature. A signature pad where the user draws their signature using mouse or touch. Use an HTML5 canvas element. Below it a Clear button and a Save Signature button. Store the signature as base64 PNG in a new column called signature_url text nullable on the users table. Run migration immediately. The signature must appear in individual RFI and Submittal Word and PDF exports in the Authorized By section — if a signature exists show the image above the name line, if not show just the blank underline.
Section 4 — API Token. Show the user's current API token in a masked password-type input. Add a Generate New Token button that creates a cryptographically random token using crypto.randomBytes(32).toString('hex') and stores it in a new column called api_token text nullable on the users table. Run migration immediately. Show a Copy button next to the token field. Add a note: "Use this token with the BIMLog Sync Agent for automated file validation."
Section 5 — Notification Preferences. Toggle switches for: File naming violation detected, RFI response overdue, Submittal response overdue, New file uploaded to project, New RFI created, New Submittal created, Rapid approval detected, Procurement before approval detected. Store as JSON in a new column called notification_preferences jsonb nullable on the users table. Run migration immediately. Save button calls PATCH /api/v1/users/me.
Section 6 — Change Password. Fields: Current Password, New Password, Confirm New Password. Validate that new password and confirm match before sending. Save button calls PATCH /api/v1/users/me/password. Show success or error message inline.
Section 7 — BIMLog Performance Score. Read only. Show a large circular score from 0 to 100. Below it a breakdown table with: Naming Compliance Rate — calculated as percentage of files this user uploaded that passed validation, RFI Response Time — average days to respond to RFIs assigned to this user, Submittal First-Pass Rate — percentage of submittals this user submitted that were approved without revision, Files Submitted On Time — percentage of files uploaded before deadline. Add note: "Score updates based on your activity across all projects." Calculate all metrics from real data in the database.
Add PATCH /api/v1/users/me endpoint to update fullName, jobTitle, phone, avatarUrl, signatureUrl, notificationPreferences. Add PATCH /api/v1/users/me/company endpoint to update companyName, website, address, phone, logoUrl on the companies table. Add PATCH /api/v1/users/me/password endpoint to verify current password and update to new password. Add GET /api/v1/users/me/performance-score endpoint that calculates and returns all 4 metrics.
The Profile page must be fully bilingual EN/ES using the existing i18n system. Style it consistently with the rest of the platform — same navy headers, same card layout, same fonts.
Then run TypeScript check on both frontend and backend with zero errors. Then rebuild and redeploy. Show exact output of these checks:
Check 1 — Profile route exists: grep -rn "/profile|ProfilePage|Profile.*page" artifacts/bimlog/src/pages/ | head -5
Check 2 — New user columns migrated: SELECT column_name FROM information_schema.columns WHERE table_name = 'users' AND column_name IN ('avatar_url', 'signature_url', 'api_token', 'notification_preferences') ORDER BY column_name;
Check 3 — Company logo column migrated: SELECT column_name FROM information_schema.columns WHERE table_name = 'companies' AND column_name = 'company_logo_url';
Check 4 — PATCH me endpoint exists: grep -n "patch.*me|PATCH.*me|router.patch.*me|users/me" artifacts/api-server/src/routes/auth.ts | head -10
Check 5 — Signature canvas exists: grep -rn "canvas|signature.*pad|SignaturePad|clearSignature|saveSignature" artifacts/bimlog/src/pages/Profile.tsx | head -10
Check 6 — Performance score endpoint exists: grep -n "performance-score|performanceScore|naming.*compliance|compliance.*rate" artifacts/api-server/src/routes/auth.ts | head -10
Check 7 — Profile link in sidebar: grep -rn "/profile|Profile.*link|sidebar.*profile|profile.*sidebar" artifacts/bimlog/src/pages/ artifacts/bimlog/src/components/ | head -10
Show exact output of all 7 checks. Nothing else.