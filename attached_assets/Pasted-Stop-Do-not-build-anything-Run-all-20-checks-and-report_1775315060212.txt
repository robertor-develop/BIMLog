Stop. Do not build anything.
Run all 20 checks and report only. Do not fix anything.

Check 1 — Unused imports in main pages:
grep -rn "^import" \
artifacts/bimlog/src/pages/ --include="*.tsx" | \
grep -v "node_modules" | sort | uniq -d | head -30

Check 2 — Console warnings and errors in frontend:
grep -rn "console.warn\|console.error\|TODO\|FIXME\|HACK\|temp\|temporary" \
artifacts/bimlog/src/ --include="*.tsx" --include="*.ts" | \
grep -v "node_modules" | head -30

Check 3 — Dead routes in backend:
grep -rn "TODO\|FIXME\|HACK\|temp\|unused\|dead" \
artifacts/api-server/src/routes/ --include="*.ts" | head -20

Check 4 — Any remaining hardcoded fake data anywhere:
grep -rn "270 Park\|Harbor View\|Callie\|HV-2026\
|Tom Davis\|Maria Sanchez\|DDS Mechanical\
|Ana Reyes\|Sofia Vargas\|Carlos Mendoza\|Alejandro" \
artifacts/ --include="*.tsx" --include="*.ts" | \
grep -v "node_modules" | head -20

Check 5 — Frontend TypeScript errors:
cd artifacts/bimlog && npx tsc --noEmit 2>&1 | wc -l

Check 6 — Backend TypeScript errors:
cd artifacts/api-server && npx tsc --noEmit 2>&1 | wc -l

Check 7 — API endpoints called from frontend 
but missing from backend:
grep -rn "api/v1" \
artifacts/bimlog/src/ --include="*.tsx" --include="*.ts" | \
grep -v "node_modules" | \
sed 's/.*api\/v1//' | \
sed 's/[`"'"'"'].*//' | \
sort | uniq | head -50

Check 8 — Routes registered in index.ts:
cat artifacts/api-server/src/routes/index.ts

Check 9 — Fetch calls with no error handling:
grep -rn "await fetch" \
artifacts/bimlog/src/ --include="*.tsx" --include="*.ts" | \
grep -v "node_modules" | \
grep -v "\.ok\|catch\|try\|then" | head -20

Check 10 — Missing env variables:
grep -rn "process.env\|import.meta.env" \
artifacts/api-server/src/ --include="*.ts" | \
grep -v "node_modules" | head -20

Check 11 — Schema tables all exported:
grep -n "export.*Table\|export const.*table" \
lib/db/src/schema/index.ts | head -40

Check 12 — Sidebar routes all valid:
grep -n "setLocation\|navigate\|href" \
artifacts/bimlog/src/components/layout/MasterSidebar.tsx | \
head -30

Check 13 — ProjectSidebar routes all valid:
grep -n "setLocation\|navigate\|href" \
artifacts/bimlog/src/components/layout/ProjectSidebar.tsx | \
head -40

Check 14 — All routes in App.tsx have matching pages:
grep -n "Route path\|component=" \
artifacts/bimlog/src/App.tsx | head -40

Check 15 — All new tab components exist:
find artifacts/bimlog/src/pages/project -name "*.tsx" | sort

Check 16 — All backend route files exist:
find artifacts/api-server/src/routes -name "*.ts" | sort

Check 17 — TotalControl defensive coding:
grep -n "??" \
artifacts/bimlog/src/pages/TotalControl.tsx | wc -l

Check 18 — Notification bell wired:
grep -n "notifications\|unread\|bell" \
artifacts/bimlog/src/components/layout/MasterSidebar.tsx | \
head -10

Check 19 — Search wired:
grep -n "search\|Search" \
artifacts/bimlog/src/components/layout/MasterSidebar.tsx | \
head -10

Check 20 — No seed files remain:
find artifacts/api-server/src -name "*seed*" | sort
grep -rn "270 Park\|Harbor View\|NYC-270\|HV-2026" \
artifacts/api-server/src/ --include="*.ts" | \
grep -v "node_modules" | head -10

Paste exact output of all 20 checks. Nothing else.