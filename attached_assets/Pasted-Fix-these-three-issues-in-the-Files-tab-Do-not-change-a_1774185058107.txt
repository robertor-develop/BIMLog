Fix these three issues in the Files tab. Do not change anything else.
Fix 1 — AI name suggestion must read the actual file content. When the user clicks Ask AI — before calling the suggest-name endpoint, read the actual file bytes using FileReader and convert the first portion to base64. Send both the fileName AND the fileContent as base64 in the POST body to suggest-name. In the suggest-name endpoint on the backend — if fileContent is provided and the file is a PDF, extract the text using pdf-parse on the base64 decoded buffer. Send the extracted text along with the file name and convention fields to the Claude API. The Claude prompt must be: "The user is uploading a file to a construction project called [project name]. The active naming convention has these fields: [fields and allowed values]. The file name is [fileName]. The first 500 words of the file content are: [extracted text]. Based on the actual content of this document, suggest the most appropriate compliant file name following the convention. Also determine if this document is relevant to a construction project — if the content appears to be completely unrelated to construction or this project, say so clearly." Return both the suggested name and a content_relevant boolean. If content_relevant is false — show a red warning banner that says "Warning: This document does not appear to be related to this construction project. Please confirm you are uploading the correct file." This is the content mismatch detection we already built on the backend — now wire it to the frontend suggestion flow.
Fix 2 — Customize Name must not open a blank tab. Remove the window.open call entirely. Instead when Customize Name is clicked — navigate within the same tab to the Name Generator page using setLocation from wouter. The Name Generator already exists at /projects/:projectId/name-generator. Use setLocation(/projects/${projectId}/name-generator). The user will go to the Name Generator, generate the correct name, copy it, then use the browser back button to return to Files. This is simpler and correct.
Fix 3 — Rejected files must appear in the file list immediately. The current issue is that when a file fails naming validation the API returns a 422 error and the file is NOT saved to the database, so invalidateQueries has nothing new to show. Fix this so that when a file fails validation — the API saves the file record to the database with status "rejected" before returning the 422 error response. The file must be saved with all fields including fileName, uploadedByUserId, projectId, status set to "rejected", documentRelationship, fileHash, and the validation error details stored in a field. Then the frontend invalidateQueries will find the new record and show it in the list with a red Rejected badge. Run the database migration if any new columns are needed.
Then run TypeScript check on both frontend and backend. Then rebuild and redeploy. Show exact output of these checks:
Check 1 — suggest-name endpoint reads file content: grep -n "fileContent|pdf-parse|extractText|content_relevant|unrelated" artifacts/api-server/src/routes/files.ts | head -10
Check 2 — Customize Name uses setLocation not window.open: grep -n "setLocation|window.open|Customize|name-generator" artifacts/bimlog/src/pages/project/FilesTab.tsx | head -8
Check 3 — rejected files saved to database: grep -n "status.*rejected|rejected.*status|saveRejected|insert.*rejected" artifacts/api-server/src/routes/files.ts | head -10
Show exact output of all 3 checks. Nothing else.