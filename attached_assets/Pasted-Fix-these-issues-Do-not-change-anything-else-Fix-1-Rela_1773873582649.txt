Fix these issues. Do not change anything else.
Fix 1 — Related RFI field is still a free text input in the submittal form. Find the field in SubmittalsTab.tsx that currently renders the Related RFI ID input. Replace it with a dropdown. When the submittal form opens fetch all RFIs for this project from GET /api/v1/projects/:projectId/rfis. Populate a select dropdown where each option shows the RFI number and subject — for example RFI-0001: This is a test Houston we have a problem. Include a blank first option that says None — no linked RFI. Store the selected RFI id as the linked_rfi_id value. If the fetch returns no RFIs show a single disabled option that says No RFIs on this project yet.
Fix 2 — The Customize Name button in FilesTab.tsx currently exists but does not navigate anywhere useful. Find it and change its onClick to navigate to the Name Generator page for this project. Use the existing wouter router useLocation hook — call setLocation with the path /projects/ followed by the projectId followed by /name-generator. Import useLocation from wouter if not already imported.
Fix 3 — Verify and fix Convention Builder live preview. The actual file is not ConventionBuilderTab.tsx — find the correct file name for the Convention Builder page. Check if a live preview section already exists showing a sample file name built from the active convention. If it does not exist add it at the top of the convention fields list — a card with light blue background, heading Live Name Preview, a sample file name in monospace built by joining the first allowed value from each field with the active separator, and colored field chips below it. Update in real time when fields change.
Fix 4 — Verify and fix Name Generator recent names. The actual file is not NameGeneratorTab.tsx — find the correct file name for the Name Generator page. Check if a Recent Names section already exists showing the last 5 generated names for the current session. If it does not exist add it below the Copy file name button — show the last 5 generated names in a list with monospace text, a copy button per row, and a relative timestamp like 2 minutes ago. Session only, no persistence.
Fix 5 — Activity log FILE BEFORE and FILE AFTER columns. Find the correct activity log frontend file. Check if the FILE BEFORE and FILE AFTER table columns are currently rendering values. If they are empty find where rename and replace events are logged in the files route and ensure file_name_before and file_name_after are stored when a file is renamed or replaced. Then ensure the activity log frontend renders these values in the table columns in monospace small text. Also find the correct activity log frontend file and add a date range filter — two date inputs labeled From and To next to the existing action type dropdown filter. When set they filter the log to events between those dates inclusive.
After all fixes run TypeScript check on both frontend and backend. Then rebuild and redeploy. Show me the exact output of these verification checks:
Check 1 — grep -n "rfi|Rfi|linked_rfi|rfiList|rfiOptions" artifacts/bimlog/src/pages/project/SubmittalsTab.tsx | grep -i "select|option|dropdown|fetch|load" | head -10
Check 2 — grep -n "setLocation|name-generator|Customize|customize" artifacts/bimlog/src/pages/project/FilesTab.tsx | head -10
Check 3 — grep -rn "livePreview|Live Name Preview|sampleName|live.*preview" artifacts/bimlog/src/pages/project/ | head -10
Check 4 — grep -rn "recentNames|Recent Names|lastGenerated|sessionNames" artifacts/bimlog/src/pages/project/ | head -10
Check 5 — grep -rn "fileNameBefore|file_name_before|dateFrom|dateTo|date.*filter|fromDate|toDate" artifacts/bimlog/src/pages/project/ | head -10