Modify ONLY artifacts/api-server/src/routes/files.ts. Do not touch any other file.
Fix 1 — Correct the pdf-parse import. Find line 13 which reads: import { PDFParse } from "pdf-parse"; Replace it with: import pdfParse from "pdf-parse";
Fix 2 — Correct the extraction call inside processFileFromDisk. Find this entire block: const parser = new PDFParse({ data: buffer, verbosity: 0 }); const result = await parser.getText(); await parser.destroy(); extractedText = result.text?.trim() || null; Replace it with: const result = await pdfParse(buffer); extractedText = result.text?.trim() || null;
Fix 3 — Fix the hardcoded uploadedById. Change the signature of processFileFromDisk from: async function processFileFromDisk(fileId: number, filePath: string, fileName: string, projectId: number): Promise<void> To: async function processFileFromDisk(fileId: number, filePath: string, fileName: string, projectId: number, uploadedById: number): Promise<void> Then find: await runContentVerification(fileId, projectId, fileName, extractedText, 0); Replace with: await runContentVerification(fileId, projectId, fileName, extractedText, uploadedById); Then find BOTH call sites of processFileFromDisk in the POST route and add the real uploader's user ID as the 5th argument. The uploader ID is already available in the POST route as the authenticated user — use the same user ID value already being written to the files table uploadedById field when the file row is inserted.
Fix 4 — Harden the CVR prompt. Find the entire prompt string inside runContentVerification starting with: You are a BIM document coordinator assistant. Replace the entire content string with this exact text: You are a BIM document integrity checker. Your ONLY job is to compare a file name against extracted document content and return one of exactly three results. File name: ${fileName} Extracted content (first 500 words): ${snippet} You MUST return exactly one of these three results — no exceptions, no other values: "match" — the content is consistent with what the file name describes "possible_mismatch" — the content is partially related but has inconsistencies, OR the content is too short or generic to confirm a match "clear_mismatch" — the content is clearly unrelated to what the file name describes Rules: If extracted content is very short under 100 words or generic, return "possible_mismatch" — never any other value. If the file name contains a project code, discipline, or document type that matches content keywords, lean toward "match". If the file name suggests one document type but content describes something completely different, return "clear_mismatch". You must ALWAYS return one of the three values above — never "not_applicable", never any other string. Respond with ONLY this JSON object — no markdown, no backticks, no explanation: {"result": "match", "reason": "one sentence explanation"}
Fix 5 — Add result normalization safety net. After the existing check for if (!parsedRaw || !parsedRaw.result) block ends, and BEFORE the final db.update that stores the result, add: const validResults = ["match", "possible_mismatch", "clear_mismatch"]; if (!validResults.includes(parsedRaw.result)) { parsedRaw.result = "possible_mismatch"; parsedRaw.reason = "Result normalized — AI returned unexpected value: " + parsedRaw.result; }
Then rebuild and run these checks and show exact output only:
Check 1 — Import fixed: grep -n "pdf-parse" artifacts/api-server/src/routes/files.ts | head -5
Check 2 — New extraction call exists: grep -n "pdfParse(buffer)" artifacts/api-server/src/routes/files.ts
Check 3 — uploadedById in signature and call: grep -n "uploadedById" artifacts/api-server/src/routes/files.ts | head -10
Check 4 — Prompt hardened: grep -n "document integrity checker|ALWAYS return|not_applicable.*never" artifacts/api-server/src/routes/files.ts | head -5
Check 5 — Upload one of the real ADENDA PDFs and run: SELECT id, file_name, content_verification_result, hash_comparison_note FROM files ORDER BY id DESC LIMIT 3;
Show exact output of all 5 checks. Nothing else.