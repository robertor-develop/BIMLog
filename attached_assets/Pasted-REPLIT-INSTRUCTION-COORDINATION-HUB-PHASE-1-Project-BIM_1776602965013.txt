REPLIT INSTRUCTION — COORDINATION HUB PHASE 1
Project: BIMLog v1.9 — April 2026
CRITICAL RULES — READ BEFORE TOUCHING ANYTHING
Do NOT redesign or refactor any existing module outside this scope. Do NOT use mock data, preview data, or temporary databases. Everything runs against live production DB and live API only. Do NOT introduce new AI libraries. Anthropic is already installed and configured — use it. Do NOT introduce new file parsing libraries. pdf-parse, multer, and xlsx are already in artifacts/api-server/. Do NOT create a parallel convention system. The existing convention from GET /api/v1/projects/:projectId/conventions is the source of truth. If anything is unclear, stop and ask — do not improvise. After each step, show exact file diffs in BEFORE / AFTER format.
WHAT WE ARE BUILDING
A new top-level project module called Coordination Hub. This is BIMLog's market-entry product: the coordinator-controlled intake space where all project participants upload their files and BIMLog automatically reads, understands, and renames them according to the active naming convention — acting as a smart AI middleware.
The flow is: 1. Any project member uploads a file. 2. BIMLog reads the filename and extracts document text/content from PDF, DOCX, or XLSX. 3. BIMLog uses the active naming convention plus project context plus document content together. 4. Claude proposes the corrected compliant filename automatically. 5. If normal confidence — user confirms. 6. If severe ambiguity or conflict — escalate with strong red warning, let user decide. 7. User confirms or manually corrects within allowed convention values only. 8. Everything is logged to the database. 9. User downloads the renamed file or queues it for sync.
STEP 1 — NEW DATABASE TABLE
Create a new file: lib/db/src/schema/coordination_intake_events.ts
import { pgTable, serial, integer, text, timestamp, boolean } from "drizzle-orm/pg-core";
export const coordinationIntakeEventsTable = pgTable("coordination_intake_events", {
id:                    serial("id").primaryKey(),
projectId:             integer("project_id").notNull(),
uploaderId:            integer("uploader_id").notNull(),
uploaderCompany:       text("uploader_company"),
originalFilename:      text("original_filename").notNull(),
proposedFilename:      text("proposed_filename"),
finalFilename:         text("final_filename"),
fileType:              text("file_type"),
fileSizeBytes:         integer("file_size_bytes"),
detectedDiscipline:    text("detected_discipline"),
detectedDocType:       text("detected_doc_type"),
detectedLevel:         text("detected_level"),
detectedOriginator:    text("detected_originator"),
aiConfidence:          text("ai_confidence"),
aiSummary:             text("ai_summary"),
aiExtractedKeywords:   text("ai_extracted_keywords"),
warningsTriggered:     boolean("warnings_triggered").default(false),
warningDetail:         text("warning_detail"),
userAction:            text("user_action"),
manualFieldsChanged:   text("manual_fields_changed"),
destinationAction:     text("destination_action"),
conventionId:          integer("convention_id"),
conventionSnapshot:    text("convention_snapshot"),
createdAt:             timestamp("created_at").defaultNow().notNull(),
});
Export this from the schema index. Run the migration so the table exists in production DB.
STEP 2 — NEW API ROUTE FILE
Create: artifacts/api-server/src/routes/coordination.ts
Use the same multer setup already in conventions.ts: multer with memoryStorage and 50MB file limit. Accept fields named pdf, docx, xlsx, sample. For PDF use pdf-parse exactly as conventions.ts does. Use Anthropic at process.env.AI_INTEGRATIONS_ANTHROPIC_API_KEY with baseURL process.env.AI_INTEGRATIONS_ANTHROPIC_BASE_URL and model claude-sonnet-4-6.
Expose three endpoints:
GET /projects/:projectId/coordination/events — returns recent intake events for this project from coordination_intake_events ordered by createdAt desc, limit 50.
POST /projects/:projectId/coordination/intake — accepts multipart file upload, reads filename, extracts text content if PDF/DOCX/XLSX, loads the active convention via direct DB query on namingConventionsTable and namingFieldsTable where projectId matches, loads project name and code from projectsTable, sends everything to Claude with this system prompt structure:
You are BIMLog's coordination intelligence engine. Your job is to analyze an uploaded construction document and propose a corrected compliant filename based on the project's active naming convention. You must use all available signals together: the original filename, the extracted document text, the project context, and the convention rules. Never guess randomly — reason from evidence. Return only valid JSON.
The user prompt must include: original filename, extracted text (first 3000 chars if available), project name, project code, convention separator, and for each field: label and array of allowedValues.
The AI must return this exact JSON shape:
{ "proposedFields": [ { "fieldLabel": "string", "proposedValue": "string", "confidence": "high|medium|low", "reasoning": "string" } ], "proposedFilename": "string", "overallConfidence": "high|medium|low", "severe": true or false, "severeReason": "string or null", "aiSummary": "2-3 sentence summary of document content", "detectedDiscipline": "string or null", "detectedDocType": "string or null", "detectedLevel": "string or null", "detectedOriginator": "string or null", "keywords": ["array", "of", "strings"] }
This endpoint does NOT write to DB yet. It returns the AI analysis result plus the original filename and file buffer reference (store buffer in memory temporarily, keyed by a short UUID returned to client).
POST /projects/:projectId/coordination/confirm — receives: the UUID from intake, userAction (accepted or manually_corrected or rejected), finalFilename, any manualFieldsChanged as JSON, destinationAction (downloaded or queued_sync). Writes the full record to coordination_intake_events. If destinationAction is downloaded, retrieves the buffer and serves the file as an attachment with Content-Disposition set to the finalFilename. If queued_sync, just writes the log and returns 200.
Register coordination.ts in the main Express router exactly as conventions.ts is registered.
STEP 3 — ADD NAVIGATION ENTRY
File: artifacts/bimlog/src/components/layout/ProjectSidebar.tsx
Add GitMerge to the existing lucide-react import line.
In NAV_ITEMS, add this as the FIRST item in the Project section, before analytics:
{ id: "coordination", label: "Coordination Hub", icon: GitMerge, section: "Project" },
STEP 4 — ADD ROUTING IN PROJECTDETAIL
File: artifacts/bimlog/src/pages/ProjectDetail.tsx
Add import: import { CoordinationHub } from "./project/CoordinationHub";
Add tab case with the existing ones: {tab === "coordination" && <CoordinationHub projectId={projectId} canWrite={canWrite} />}
STEP 5 — BUILD THE COORDINATIONHUB COMPONENT
Create: artifacts/bimlog/src/pages/project/CoordinationHub.tsx
The component receives projectId and canWrite as props. It has three zones.
ZONE A — HEADER STRIP
Fetch convention using useGetConvention(projectId) from @workspace/api-client-react. Show the active convention as color-coded chips using the exact same CHIP_COLORS array and chip style from NameGenerator.tsx — copy it directly, do not reinvent it. Show separator between chips. If convention.isActive is false or fields are empty, show a blocking red banner: "No active convention — this project cannot use Coordination Hub until a coordinator sets up the naming convention" with a link to /projects/:id/convention. Do not show the upload zone if no active convention.
ZONE B — UPLOAD / INTAKE AREA
Drag-and-drop zone. On file drop or browse: call POST /api/v1/projects/:projectId/coordination/intake with the file as multipart. While loading: show "BIMLog is reading your document…" with a spinner. On response: hide the upload zone and show the REVIEW PANEL.
ZONE C — INTAKE QUEUE
Below the upload zone, fetch and display recent events from GET /api/v1/projects/:projectId/coordination/events. Each row shows: original filename in muted strikethrough style, arrow, final filename in bold colored green for high confidence / yellow for medium / red for low or severe, a badge for userAction (Auto-accepted in green / Manually corrected in yellow / Rejected in red), uploader company, and timestamp. If no events yet, show a muted empty state: "No files processed yet — upload the first file above."
THE REVIEW PANEL
When AI analysis returns, show this panel replacing the upload zone:
Section 1 — ORIGINAL vs PROPOSED. Show original filename in muted monospace. Below it show the proposed name broken into field chips. Each chip uses the CHIP_COLORS color for its index position matching the convention field order. Below each chip show the field label in tiny uppercase. Below that show the AI reasoning in tiny muted italic text. Chips where confidence is low get an orange border and a small warning icon.
Section 2 — SEVERE WARNING. If severe is true, wrap the entire panel in a red left border. Show a red banner at top: "BIMLog detected a serious conflict — review carefully before confirming." Show severeReason text. Disable the confirm button until user checks a checkbox labeled "I have reviewed this warning and confirm the proposed name is correct." Log that checkbox was checked.
Section 3 — MANUAL CORRECTION. Under each chip show a small Edit link. When clicked, open a searchable dropdown — copy the exact SearchableSelect component from NameGenerator.tsx, do not rewrite it — showing only the allowedValues for that field. User cannot type free-form values. Changed chips get an orange border.
Section 4 — AI UNDERSTANDING. Show a box labeled "What BIMLog understood" containing: aiSummary text, detected discipline / doc type / level / originator if not null, keywords as small gray chips.
Section 5 — ACTIONS. Three buttons: primary "Confirm & Download" — calls /confirm with userAction accepted or manually_corrected depending on whether user changed any fields, destinationAction downloaded, serves file download. Secondary "Queue for Sync" — calls /confirm with destinationAction queued_sync, no download, shows success message. Ghost "Reject" — calls /confirm with userAction rejected, returns to upload state.
STEP 6 — API CLIENT HOOKS
In the existing api-client-react package, following the exact same pattern as useGetConvention and useUpsertConvention, add:
A function or hook to POST to /projects/:projectId/coordination/intake with multipart form data. A function or hook to POST to /projects/:projectId/coordination/confirm with JSON body. A hook to GET /projects/:projectId/coordination/events.
STEP 7 — DO NOT TOUCH
Do not modify ConventionBuilder.tsx. Do not modify NameGenerator.tsx. Do not modify FilesTab.tsx. Do not modify any existing route in conventions.ts. Do not modify any existing DB table. Do not modify auth middleware. You may read and copy patterns from these files. You may not change them.
STEP 8 — VALIDATION
After implementation confirm each of these by showing the actual code path:
Navigation: Coordination Hub appears first in Project section. Route: /projects/:id/coordination renders CoordinationHub component. Convention load: useGetConvention fetches from correct endpoint. No convention guard: upload blocked when convention.isActive is false. File upload: POST /coordination/intake accepts multipart file. PDF extraction: pdf-parse used for PDF files. AI call: claude-sonnet-4-6 called with filename plus content plus convention plus project context. Proposed name: returns field-by-field proposal with confidence per field. Chip display: each field shown as color-coded chip with label and reasoning. Severe warning: red border plus checkbox gate when severe is true. Manual correction: searchable dropdown constrained to allowedValues only. Confirm writes log: coordination_intake_events row written with all fields. Download: file served with corrected filename as Content-Disposition attachment. Queue: destinationAction queued_sync logged, no download triggered. Events list: recent events shown in queue below upload zone.
FINAL OUTPUT
At the end provide only this:
COORDINATION HUB PHASE 1 — READY
New top-level Coordination Hub module added: yes/no
AI uses filename plus content plus convention plus project context: yes/no
Auto-proposed compliant rename implemented: yes/no
Field-by-field chip display with confidence: yes/no
Severe warning escalation with checkbox gate: yes/no
Manual correction constrained to allowed values only: yes/no
Confirm writes full log to coordination_intake_events: yes/no
Download with corrected filename implemented: yes/no
Queue action logged with queued_sync status: yes/no
Recent events queue visible below upload zone: yes/no
Existing modules untouched: yes/no
Root cause this solves: Subcontractors and trades upload files with wrong or chaotic names. BIMLog intercepts, understands, and normalizes every file automatically before it enters the coordination workflow.
Exact files created: lib/db/src/schema/coordination_intake_events.ts — artifacts/api-server/src/routes/coordination.ts — artifacts/bimlog/src/pages/project/CoordinationHub.tsx
Exact files modified: artifacts/bimlog/src/components/layout/ProjectSidebar.tsx — artifacts/bimlog/src/pages/ProjectDetail.tsx — lib/api-client-react/src (new hooks) — lib/db/src/schema/index.ts
Do only this. Nothing more.