Fix the AI Assist button in the RFI response section only. Do not change anything else.
The AI Assist button currently ignores the text the user typed in the response field and generates a response based on the original RFI content from the database. This is wrong.
Change the behavior to this: When the user clicks AI Assist — take exactly what the user typed in the Official Response text area as the input. If the field is empty — use the RFI question text as context and generate a professional response draft. If the field has text — treat that text as the user's rough draft and rewrite it professionally, fixing spelling, improving technical language, and making it suitable for a formal AEC RFI response. Do not pull content from previous RFIs or previous responses. Do not copy content from RFI-0001 or any other RFI. The only context allowed is the current RFI subject, the current RFI question, and whatever the user typed in the response field.
The prompt sent to the AI must be exactly this structure: "You are responding to an AEC construction RFI. The RFI subject is [rfi.subject]. The question asked is [rfi.question stripped of markdown]. The user has drafted the following response: [user typed text]. Rewrite this draft as a professional formal RFI response. Fix spelling and grammar. Improve technical language. Keep all the user's actual content and intent — do not add new technical details that the user did not mention. Do not reference any other RFIs or previous responses. Output only the rewritten response text with no preamble."
If the user typed nothing in the response field use this prompt instead: "You are responding to an AEC construction RFI. The RFI subject is [rfi.subject]. The question asked is [rfi.question stripped of markdown]. Draft a professional formal response acknowledging the question and requesting the specific clarifications or information needed to provide a complete answer. Keep it concise. Output only the response text with no preamble."
Then run TypeScript check on backend only. Then restart API server only. Show exact output of this one check:
grep -n "user.*typed|userDraft|draftText|rough.*draft|response.*field|field.*response|user.*wrote|currentDraft" artifacts/api-server/src/routes/rfis.ts | head -10