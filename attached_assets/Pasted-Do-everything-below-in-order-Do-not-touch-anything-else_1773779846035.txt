Do everything below in order. Do not touch anything else that is currently working.
Problem 1 — Fix all broken buttons and links throughout the app. Go through every button and link in the entire app that currently does nothing when clicked and fix them. Specifically: the Add Integration button in the Integrations tab, any help links inside project pages, any contact links, any download links. Every button must do something. If it is not built yet it should show a clear message explaining what it does and who to contact.
Problem 2 — Build the BIMLog Sync Agent as a real desktop installer. Do the following in order:
First check if electron-builder can run by running: cd artifacts/sync-agent && npx electron-builder --version
Then replace the entire artifacts/sync-agent/package.json with this exact content: name bimlog-sync-agent, version 1.0.0, productName BIMLog Sync Agent, description BIMLog folder watcher and naming validator by IgniteSmart, main main.js, scripts start electron dot, build:win npx electron-builder --win --x64, build:mac npx electron-builder --mac --x64, build section with appId ai.ignitesmart.bimlog-sync-agent, productName BIMLog Sync Agent, copyright Copyright 2026 IgniteSmart, output directory dist, files main.js preload.js settings.html package.json, win target nsis arch x64, mac target dmg arch x64, nsis oneClick false allowToChangeInstallationDirectory true, dependencies chokidar 3.6.0, devDependencies electron 29.0.0 electron-builder 24.13.3
Then create an assets folder inside artifacts/sync-agent. Create a simple icon — a 256x256 image with blue background and white letter B. Save it as assets/icon.png. Convert to assets/icon.ico for Windows and assets/icon.icns for Mac using any available tool. If no conversion tool is available create empty placeholder files with those names so the build does not fail.
Then run these commands in exact order and show me the full output of each one:
cd artifacts/sync-agent
npm install
npm install --save-dev electron-builder
npx electron-builder --win --x64
If the Windows build succeeds find the .exe file inside artifacts/sync-agent/dist and copy it to artifacts/sync-agent/releases/BIMLog-Sync-Agent-Setup-Windows.exe
If the build fails because this is a Linux server and cannot build Windows binaries natively — install Wine and the required Windows cross-compilation tools by running: apt-get install -y wine wine64 && npm install --save-dev electron-builder && npx electron-builder --win --x64 --linux
Then update artifacts/api-server/src/routes/downloads.ts. Change GET /api/v1/downloads/sync-agent-windows to serve the real .exe file from artifacts/sync-agent/releases/BIMLog-Sync-Agent-Setup-Windows.exe as a binary file download with headers content-type application/octet-stream and content-disposition attachment filename BIMLog-Sync-Agent-Setup.exe. Change GET /api/v1/downloads/sync-agent-mac to return a JSON response saying Mac installer coming soon — contact info@ignitesmart.ai and update the Download for Mac button in the frontend to show a toast message saying Mac version coming soon — contact us at info@ignitesmart.ai
Then update the Download for Windows button in the frontend to call GET /api/v1/downloads/sync-agent-windows and trigger a real file download. Make sure the browser receives the file as a download not as a page open.
Then rebuild the API server and frontend and redeploy. Confirm the Windows download endpoint returns content-type application/octet-stream. Show me the full output of every step.