Fix these 4 issues in the Files tab. Do not change anything else.
Fix 1 --- The rejected file row in the file list is not clickable and shows nothing when you try to interact with it. Make each file row in the list expandable. When a user clicks on a file row --- expand it inline to show: the full file name, the rejection reason with field-by-field breakdown, the Suggested Compliant Name blue chip with copy button, the Ask AI purple button, and the Customize Name button. The expand/collapse should work like an accordion --- click to open, click again to close. This way the user can see and act on every rejected file directly from the list without needing to re-upload.
Fix 2 --- Ask AI is not reading the actual file content. The issue is that when the user clicks Ask AI from the expanded file row in the list --- there is no file object available because the file was already uploaded. For files already in the list, Ask AI must work differently --- send the fileName and the stored validationDetails to the suggest-name endpoint and ask Claude to suggest the best compliant name based on the file name alone plus the convention fields. Do not require file bytes for files already in the list. For NEW uploads where the user just dropped a file and it was rejected --- read the file bytes from the drop event and send them to suggest-name as base64 so Claude can read the PDF content. Make sure the suggest-name endpoint handles both cases --- with fileContent for new drops, without fileContent for existing rejected files in the list.
Fix 3 --- Customize Name blanks the screen. The Name Generator at /projects/:projectId/name-generator is rendering blank when navigated to via setLocation. Check why the Name Generator page renders blank --- it likely needs the projectId from the URL params and is not getting it correctly when navigated this way. Fix the Name Generator page to always read projectId from the URL params using useParams or useRoute from wouter, not from props. Then the setLocation navigation will work correctly.
Fix 4 --- Remove the Upload File blue button from the top right corner of the Files tab header. The drag and drop zone is the only upload interface. Delete this button completely --- it is redundant and confusing.
Then run TypeScript check on both frontend and backend. Then rebuild and redeploy. Show exact output of these checks:
Check 1 --- File rows are expandable with accordion: grep -n "expand|accordion|isExpanded|toggleExpand|onClick.*row|row.*onClick" artifacts/bimlog/src/pages/project/FilesTab.tsx | head -10
Check 2 --- suggest-name handles both with and without fileContent: grep -n "fileContent|fileName.*only|without.*content|fallback" artifacts/api-server/src/routes/files.ts | head -10
Check 3 --- Name Generator reads projectId from URL params: grep -n "useParams|useRoute|params|projectId" artifacts/bimlog/src/pages/project/NameGenerator.tsx | head -10
Check 4 --- Upload File button removed from header: grep -n "Upload File|UploadFile|upload.*button|button.*upload" artifacts/bimlog/src/pages/project/FilesTab.tsx | head -10
Show exact output of all 4 checks. Nothing else.