Fix these 5 things. Do not touch anything else.
Fix 1 — Add missing database columns and run migration immediately. Add is_superseded boolean not null default false to the files table. Add convention_version text nullable to the files table. Run pnpm --filter @workspace/db run push immediately after. Confirm both columns exist.
Fix 2 — Fix the 2 raw addPage calls in the individual RFI PDF. In artifacts/api-server/src/routes/rfis.ts on lines 349 and 442 there are raw doc.addPage() calls that are not wrapped in addPageIfNeeded. Replace both of them with addPageIfNeeded calls with an appropriate height value of 100. This will eliminate the blank pages.
Fix 3 — When a new version of a file is uploaded — automatically set is_superseded to true on all previous versions of that document in the project. A previous version is any file in the same project where the base file name without extension matches the new file's base name without extension. Do this in the upload route immediately after saving the new file record.
Fix 4 — Move the suggested compliant names section in FilesTab. It is currently at line 652 which is too far down the page. Move it so it appears immediately after the field-level error breakdown chips — right after the block that shows which fields failed and what the allowed values are. The user must see the suggestion the moment they understand what went wrong — not after scrolling down.
Fix 5 — Add AI Suggest button to the suggested names section. Next to the existing suggested name chip add a second button with a purple background and sparkle icon that says Ask AI. When clicked — call POST /api/v1/projects/:projectId/files/suggest-name with the original file name in the request body. If this endpoint does not exist — create it. The endpoint loads the active convention for the project and calls the Claude API asking it to analyze the original file name and suggest the most appropriate compliant name based on what it can infer. Return the suggestion as JSON with suggestedName and explanation fields. Show the AI suggestion as a purple chip with an AI badge next to it and a copy button.
Then run TypeScript check on both frontend and backend. Then rebuild and redeploy. Show me confirmation that all 5 fixes are complete.