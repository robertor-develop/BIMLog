Do these fixes. Do not touch any project pages, RFIs, Submittals, Files, Convention Builder, Name Generator, or any other existing functionality.
Fix 1 --- Managed Connection and OAuth buttons still not working. Find the SidebarModal buttons in ProjectSidebar.tsx for Managed Connection Get Started and OAuth Connection Contact Us. The buttons are rendering but the click does nothing. Replace both buttons with anchor tags styled as buttons. Managed Connection: <a href="mailto:info@ignitesmart.ai?subject=BIMLog%20Managed%20Connection%20Request" style={{ ... same button styles ... }}>Get Started</a>. OAuth Connection: <a href="mailto:info@ignitesmart.ai?subject=BIMLog%20OAuth%20Connection%20Request" style={{ ... same button styles ... }}>Contact Us</a>. Anchor tags with href mailto always work regardless of onClick issues.
Fix 2 --- Master sidebar navigation. Replace the current Dashboard and Projects links with these items: Files — links to the most recently visited project's files tab or /dashboard if no project visited. RFIs — same pattern. Reports — links to /dashboard for now with a coming soon tooltip. Then below those show a divider and these admin-only items visible only when isSuperAdmin is true: Admin Panel — links to /admin. Total Control — links to /admin same as Admin Panel for now. Style these exactly like Urbano's sidebar --- same positioning, admin items above the profile section at the bottom left.
Fix 3 --- Completely rebuild the main dashboard content area. Replace the current boring project card list with this proper command center layout:
Row 1 --- Platform stats bar. 5 stat cards in a row: Total Active Projects, Total Files Processed, Open RFIs Across All Projects, Pending Submittals, Platform Compliance Rate. Calculate all from real data by fetching all projects and aggregating their stats.
Row 2 --- Two columns side by side. Left column --- Needs Attention. Show a list of alerts: projects with overdue RFIs highlighted red, projects with naming violations highlighted amber, projects with pending submittals highlighted blue. Each alert shows project name, what the issue is, and a Go to Project button. If nothing needs attention show a green All Clear message. Right column --- Your Pending Items. RFIs waiting for Roberto's response across all projects. Submittals waiting for review. Each item shows project name, RFI or submittal number, days outstanding, and a View button.
Row 3 --- Two columns side by side. Left column --- Recent Activity Feed. Last 15 events across ALL projects combined, sorted by newest first. Show timestamp, action badge, user name, company, project name, and details. Right column --- Top Naming Violators. A small table showing which companies have the most naming violations across all projects. Company name, violation count, projects affected. If no violations show a clean All compliant message.
Row 4 --- Your Projects. The existing project cards exactly as they are now, just moved to the bottom of the page. Keep the New Project button. Keep all project card functionality.
Then rebuild and redeploy. Show exact output of these checks:
Check 1 --- Managed Connection uses anchor tag: grep -n "mailto.*Managed|href.*Managed|anchor.*managed" artifacts/bimlog/src/components/layout/ProjectSidebar.tsx | head -5
Check 2 --- Admin Panel and Total Control in master sidebar: grep -n "Admin Panel|Total Control|isSuperAdmin" artifacts/bimlog/src/components/layout/MasterSidebar.tsx | head -10
Check 3 --- Needs Attention section exists: grep -n "Needs Attention|needsAttention|overdue|violation" artifacts/bimlog/src/pages/Dashboard.tsx | head -10
Check 4 --- Recent Activity Feed across all projects: grep -n "Recent Activity|recentActivity|allProjects|combined" artifacts/bimlog/src/pages/Dashboard.tsx | head -10
Show exact output of all 4 checks. Nothing else.