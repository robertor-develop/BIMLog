Fix these 8 issues across the platform. Do not change anything else that is currently working.
Fix 1 — Integration connection modal missing OAuth option. When a user clicks Connect on any integration card the modal shows 3 options — Validate and Download, Managed Connection, and BIMLog Sync Agent. Add a 4th option at the bottom: OAuth Connection — with a lock icon, title OAuth Connection, description "Secure token-based direct integration. Log in through your existing platform credentials. No manual setup. No API tokens to manage. Available on Enterprise plans." with a Get Started button styled the same as the others.
Fix 2 — Integration cards missing value description. Each integration card on the Integrations page currently shows the platform name, logo, and Not connected status. Add one line of value description specific to each platform shown in subtle gray text below the platform name. Use these exact descriptions: Procore — "Sync RFIs, submittals, and documents bidirectionally. Every action recorded in BIMLog audit trail." Autodesk ACC — "Connect model files and coordination data. Naming validation on every upload from ACC." OneDrive — "File storage sync with naming validation gateway. Every upload validated before it reaches your folders." SharePoint — "Document library sync with convention enforcement. Non-compliant files flagged before delivery." Revit — "Model file upload gateway. Every RVT file validated against active convention on upload." Navisworks — "Composite model uploads validated. NWD and NWF files tracked with version history." Speckle — "Model data streams connected and tracked. Every version attributed and timestamped." MS Project — "Schedule data imported and monitored. Milestones and baselines tracked for delay attribution." Google Drive — "File storage sync with naming validation. Every file routed through BIMLog before delivery." Power BI — "BIMLog data exposed as a Power BI data source for custom compliance and performance dashboards." Excel — "Schedule trackers and submittal logs parsed and synced. No manual data entry." IFC — "Open BIM file uploads validated against active convention. IFC model versions tracked." Claude Anthropic — "AI-powered RFI drafting, submittal compliance checking, and report generation on demand." Gemini — "Alternative AI provider for project insights and report generation. Activate as primary or fallback."
Fix 3 — Add Integration modal shows wrong platforms. The modal that opens when clicking Add Integration currently shows platforms that already have dedicated cards on the integrations page. Remove all platforms that already appear on the integrations page from this modal. The modal should only show platforms that are NOT already listed. Replace the grid with these unlisted platforms only: Autodesk BIM 360, Aconex, Primavera P6, Bluebeam Revu, Fieldwire, PlanGrid, Smartsheet, Trimble Connect, Dropbox, Box, Egnyte, Prostream, e-Builder, Kahua, Newforma. Keep the Other free text field at the bottom exactly as it is. Keep the Contact us button exactly as it is.
Fix 4 — API docs button is dead. The API docs button at the bottom of the Integrations page currently does nothing. Make it open a modal with the title BIMLog REST API. The modal should show: a header section with text "BIMLog exposes a full REST API for integration with any platform that supports standard HTTP authentication. Base URL: https://bim-log-ignite.replit.app/api/v1 — Authentication: Bearer token via JWT — contact info@ignitesmart.ai to request API credentials." Then show a table with these endpoints listed: GET /projects — List all projects for authenticated user. POST /projects — Create a new project. GET /projects/:id/files — List all files for a project. POST /projects/:id/files — Upload and validate a file. GET /projects/:id/rfis — List all RFIs. POST /projects/:id/rfis — Create a new RFI. GET /projects/:id/rfis/:id/audit-certificate — Generate audit certificate PDF. GET /projects/:id/submittals — List all submittals. POST /projects/:id/submittals — Create a new submittal. GET /projects/:id/submittals/:id/audit-certificate — Generate submittal audit certificate. GET /projects/:id/activity — Get full activity log. Webhooks available on Enterprise — contact info@ignitesmart.ai. Full documentation coming soon at ignitesmart.ai/api-docs.
Fix 5 — Convention Builder missing live preview. At the top of the Convention Builder page above the field list add a live preview section. Show a card with a light blue background and the heading Live Name Preview. Inside it show a sample file name built by taking the first allowed value from each field in the current convention and joining them with the active separator. Below the sample name show the field tokens as colored chips exactly like the Name Generator does — each field value in its color with the field label as a tooltip. Update this preview in real time whenever the user edits any field label, allowed values, or separator. Add a small gray subtitle under the heading that says "This is what a correctly named file will look like with your current convention settings."
Fix 6 — Name Generator missing recent names. Below the Copy file name and Save to list buttons add a section called Recent Names with a clock icon. This section shows the last 5 file names generated in the current session as a list. Each row shows the full file name in monospace, a Copy button, and a small timestamp showing how long ago it was generated — e.g. "2 minutes ago." When the page first loads this section is hidden. It appears as soon as the first name is generated. Session only — does not persist across page reloads.
Fix 7 — Team Add Member must support email invite. The Add Member button currently only allows adding users who already exist in the system. Change the Add Member modal to have two tabs at the top. Tab 1 called Existing User — shows the current search or select from project users. Tab 2 called Invite by Email — shows a form with fields: Email address required, Full name required, Company name required, Role dropdown with the 4 role options. When submitted this creates a pending invitation record in the database — add a new table called project_invitations with columns id, project_id, invited_by_user_id, email, full_name, company_name, role, status defaulting to pending, created_at, accepted_at. Run the migration immediately. Show pending invitations in the Team tab below the active members list in a section called Pending Invitations with columns Email, Name, Company, Role, Invited, and a Resend and Cancel button per row. When an invited person signs up with that email they are automatically added to the project with the assigned role and the invitation marked accepted.
Fix 8 — Activity Log improvements. Two changes only. First — populate the FILE BEFORE and FILE AFTER columns. When a file rename or replace event is logged store the file name before the change in a column called file_name_before and the name after in file_name_after in the activity log table. Check if these columns already exist — if they do populate them properly on rename events. If they do not exist add them and run migration. Show the values in the FILE BEFORE and FILE AFTER columns in the activity log table in monospace small text. Second — add a date range filter. Next to the existing All Actions dropdown filter add two date inputs — From date and To date. When set they filter the activity log to show only events between those dates. Keep the existing search bar and action type filter working alongside the date filter.
Then run TypeScript check on both frontend and backend. Then rebuild and redeploy. Show me confirmation that all 8 fixes are complete with zero TypeScript errors.