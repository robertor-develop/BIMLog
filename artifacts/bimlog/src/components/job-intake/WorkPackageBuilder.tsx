import { Layers3, Plus, Trash2 } from "lucide-react";

export function WorkPackageBuilder({ items, setItems, tt }: { items:any[]; setItems:(updater:(items:any[])=>any[])=>void; tt:(en:string,es:string)=>string }) {
  const dimensions=["building","floor","zone","discipline","system","phase","deliverable","task","milestone"];
  const dimensionLabel=(value:string)=>({ building:tt("Building","Edificio"),floor:tt("Floor","Piso"),zone:tt("Zone","Zona"),discipline:tt("Discipline","Disciplina"),system:tt("System","Sistema"),phase:tt("Phase","Fase"),deliverable:tt("Deliverable","Entregable"),task:tt("Task","Tarea"),milestone:tt("Milestone","Hito") } as Record<string,string>)[value] ?? value;
  const update=(itemIndex:number, packageIndex:number, patch:any)=>setItems((current)=>current.map((item,index)=>index===itemIndex?{...item,workPackages:(item.workPackages||[]).map((row:any,rowIndex:number)=>rowIndex===packageIndex?{...row,...patch}:row)}:item));
  const updateTask=(itemIndex:number, packageIndex:number, taskIndex:number, patch:any)=>setItems((current)=>current.map((item,index)=>index===itemIndex?{...item,workPackages:(item.workPackages||[]).map((row:any,rowIndex:number)=>rowIndex===packageIndex?{...row,tasks:(row.tasks||[]).map((task:any,index:number)=>index===taskIndex?{...task,...patch}:task)}:row)}:item));
  const addPackage=(itemIndex:number)=>setItems((current)=>current.map((candidate,index)=>{ if(index!==itemIndex) return candidate; const id=`WP-${crypto.randomUUID()}`; return {...candidate,workPackages:[...(candidate.workPackages||[]),{id,packageCode:id.slice(0,11),title:"",dimensionType:"deliverable",dimensionValue:"",packageType:"deliverable",tasks:[]}]}; }));
  const addTask=(itemIndex:number,packageIndex:number)=>{ const id=`TASK-${crypto.randomUUID()}`; update(itemIndex,packageIndex,{tasks:[...(items[itemIndex].workPackages?.[packageIndex]?.tasks||[]),{id,taskCode:id.slice(0,13),name:"",plannedHours:"0.00"}]}); };
  return <section className="ji-row"><h3><Layers3 size={16}/> {tt("Work-package decomposition", "Descomposición en paquetes de trabajo")}</h3><p>{tt("Work packages are optional. Split a Contract Item only when part of it needs separate scheduling, responsibility, progress, or delivery control. A package may contain several operational tasks when the work needs that detail.", "Los paquetes de trabajo son opcionales. Divida una Partida de Contrato solo cuando una parte necesite control separado de calendario, responsabilidad, avance o entrega. Un paquete puede contener varias tareas operativas cuando el trabajo necesite ese detalle.")}</p>{items.map((item,itemIndex)=><div className="ji-row" key={item.id}><strong>{item.name||item.id}</strong><small>{tt("Owning Contract Item", "Partida de Contrato propietaria")}: {item.id}</small>{(item.workPackages||[]).map((row:any,packageIndex:number)=><div className="ji-row" key={row.id}><div className="ji-grid three"><label>{tt("Package", "Paquete")}<input value={row.title||""} onChange={(e)=>update(itemIndex,packageIndex,{title:e.target.value})}/></label><label>{tt("Stable package code", "Código estable del paquete")}<input value={row.packageCode||row.id} readOnly/></label><label>{tt("Break down by", "Dividir por")}<select value={row.dimensionType||"deliverable"} onChange={(e)=>update(itemIndex,packageIndex,{dimensionType:e.target.value})}>{dimensions.map(value=><option key={value} value={value}>{dimensionLabel(value)}</option>)}</select></label><label>{tt("Value", "Valor")}<input value={row.dimensionValue||""} placeholder={row.dimensionType==="floor"?tt("Example: Level 3","Ejemplo: Piso 3"):undefined} onChange={(e)=>update(itemIndex,packageIndex,{dimensionValue:e.target.value})}/></label><button type="button" aria-label={tt("Remove work package", "Eliminar paquete de trabajo")} onClick={()=>setItems((current)=>current.map((candidate,index)=>index===itemIndex?{...candidate,workPackages:(candidate.workPackages||[]).filter((_:any,i:number)=>i!==packageIndex)}:candidate))}><Trash2 size={14}/></button></div><div className="ji-lock"><strong>{tt("Operational tasks in this package", "Tareas operativas de este paquete")}</strong><p>{tt("Add separate tasks only when they need their own assignment or progress control. Example: CELLAR_PB_SH_PRE_R0V0.", "Agregue tareas separadas solo cuando necesiten su propia asignación o control de avance. Ejemplo: CELLAR_PB_SH_PRE_R0V0.")}</p>{(row.tasks||[]).map((task:any,taskIndex:number)=><div className="ji-grid three" key={task.id}><label>{tt("Task name", "Nombre de tarea")}<input value={task.name||""} onChange={(event)=>updateTask(itemIndex,packageIndex,taskIndex,{name:event.target.value})}/></label><label>{tt("Stable task code", "Código estable de tarea")}<input value={task.taskCode||task.id} readOnly/></label><label>{tt("Planned hours", "Horas planificadas")}<input type="number" min="0" step="0.01" value={task.plannedHours||"0.00"} onChange={(event)=>updateTask(itemIndex,packageIndex,taskIndex,{plannedHours:event.target.value})}/></label><button type="button" aria-label={tt("Remove task", "Eliminar tarea")} onClick={()=>update(itemIndex,packageIndex,{tasks:(row.tasks||[]).filter((_:any,index:number)=>index!==taskIndex)})}><Trash2 size={14}/></button></div>)}<button type="button" onClick={()=>addTask(itemIndex,packageIndex)}><Plus size={14}/> {tt("Add operational task", "Agregar tarea operativa")}</button></div></div>)}<button type="button" onClick={()=>addPackage(itemIndex)}><Plus size={14}/> {tt("Add Work Package", "Agregar paquete de trabajo")}</button></div>)}</section>;
}
