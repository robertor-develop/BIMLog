import assert from "node:assert/strict";import React from "react";import{renderToStaticMarkup}from"react-dom/server";import{IntakeReleaseReadiness}from"./IntakeReleaseReadiness";
const data={identity:{jobName:"River",jobCode:"R1"},relationships:{participants:[{id:"A"}],engagements:[]},commercial:{contracts:[]},apuDrafts:[],workPackages:[],resourcePlans:[]};const en=renderToStaticMarkup(<IntakeReleaseReadiness data={data} documents={[]} tt={(a)=>a}/>);assert.match(en,/Migration preview/);assert.match(en,/does not migrate, approve, activate, post, push or publish/);const es=renderToStaticMarkup(<IntakeReleaseReadiness data={data} documents={[]} tt={(_a,b)=>b}/>);assert.match(es,/Vista previa de migración/);assert.match(es,/Sin acción oculta/);console.log("Intake release-readiness UI behavior: PASS");
