import { useEffect, useMemo, useState } from "react";
import { Link, useRoute } from "wouter";
import { useAuthStore } from "@/store/auth";
import { useI18n } from "@/lib/i18n";
import { PrintPdfButton } from "@/components/PrintPdfButton";
import { FinancialProjectShell } from "@/components/layout/FinancialProjectShell";
import { ContractItemWorkflowPanel } from "@/components/commercial/ContractItemWorkflowPanel";
import { ContractPaymentApplicationsPanel } from "@/components/commercial/ContractPaymentApplicationsPanel";

const API_BASE = (import.meta.env.VITE_API_URL as string | undefined) ?? "";
type ContractItemDraft = { displayName: string; quantity: string; unit: string; unitRate: string; apuPlanVersion: string; workflowTemplate: string; industryTemplate: string };
type SovDraft = { stableLineId: string; budgetSnapshotLineId: string; projectCostNodeId: string; description: string; amount: string; scheduleItemPlacementId: string; contractItem?: ContractItemDraft };
const emptyLine = (apu?: any): SovDraft => ({ stableLineId: `ITEM-${Date.now()}`, budgetSnapshotLineId: "", projectCostNodeId: "", description: "", amount: "0.00", scheduleItemPlacementId: "", contractItem: { displayName: "", quantity: "1.00", unit: "Hours", unitRate: apu?.sellingPrice ?? "", apuPlanVersion: apu?.version ? String(apu.version) : "", workflowTemplate: "bim-submittal", industryTemplate: "bim-services" } });
const itemValue = (quantity: string, rate: string) => {
  const decimal = /^\d{1,24}(?:\.\d{0,6})?$/;
  if (!decimal.test(quantity) || !decimal.test(rate)) return "";
  const scaled = (value: string) => { const [whole, fraction = ""] = value.split("."); return BigInt(whole) * 1_000_000n + BigInt(fraction.padEnd(6, "0")); };
  const product = (scaled(quantity) * scaled(rate) + 500_000n) / 1_000_000n;
  const whole = product / 1_000_000n, fraction = (product % 1_000_000n).toString().padStart(6, "0").replace(/0+$/, "");
  return fraction ? `${whole}.${fraction}` : `${whole}`;
};
const emptyAmendmentLine = (): SovDraft => ({ stableLineId: `AMD-${Date.now()}`, budgetSnapshotLineId: "", projectCostNodeId: "", description: "", amount: "0.00", scheduleItemPlacementId: "" });
type ContractSort = "created_desc" | "legal_asc" | "legal_desc" | "counterparty_asc" | "status_asc" | "value_desc" | "value_asc" | "approved_desc" | "executed_desc";
type ContractColumn = "legalNumber" | "title" | "counterparty" | "status" | "type" | "perspective" | "originalValue" | "currentCommitment" | "currency" | "approvedAt" | "executedAt";
type ContractSection = "summary" | "filters" | "contracts";
const defaultColumns: ContractColumn[] = ["legalNumber", "title", "counterparty", "status", "type", "perspective", "originalValue", "currentCommitment", "currency"];
const defaultSections: ContractSection[] = ["summary", "filters", "contracts"];
const statusOptions = ["all", "draft", "submitted", "under_review", "approved", "executed", "returned", "rejected", "withdrawn", "superseded", "terminated", "voided", "closed"];
const typeOptions = ["all", "subcontract", "purchase_order", "consultant_agreement", "owner_prime", "other_commitment"];
const perspectiveOptions = ["all", "downstream", "upstream"];
const sortOptions: ContractSort[] = ["created_desc", "legal_asc", "legal_desc", "counterparty_asc", "status_asc", "value_desc", "value_asc", "approved_desc", "executed_desc"];
const detailStyles = `.fc-detail{flex-basis:100%;width:100%;margin-top:8px;padding:16px;border:1px solid #cbd9e7;border-radius:10px;background:#f8fbff}.fc-detail h3,.fc-detail h4{margin:0 0 8px}.fc-detail h4{margin-top:18px}.fc-detail-heading{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.fc-detail-heading p{margin:0}.fc-table-wrap{overflow-x:auto}.fc-table{width:100%;border-collapse:collapse;background:#fff}.fc-table th,.fc-table td{padding:9px;text-align:left;border-bottom:1px solid #e4e9ef;font-size:12px;vertical-align:top}.fc-table th{background:#edf4fa;color:#405162}.fc-table small,.fc-amendments span,.fc-amendments code{display:block;margin-top:3px}.fc-amendment-form{margin-top:16px;padding:14px;border:1px solid #d5dfeb;border-radius:8px;background:#fff}.fc-amendment-line{display:grid;grid-template-columns:1.2fr .8fr 1.3fr .7fr auto;gap:8px;align-items:end;margin-top:10px}.fc-amendment-line label{display:grid;gap:4px;font-size:12px;color:#556170}.fc-amendment-line input,.fc-amendment-line select{min-width:0;padding:8px;border:1px solid #cbd4de;border-radius:6px}.fc-amendments{display:grid;gap:8px}.fc-amendments article{display:flex;justify-content:space-between;gap:12px;padding:12px;border:1px solid #dce3ea;border-radius:8px;background:#fff}.fc-amendments .fc-actions{margin-top:0}.fc-history{display:grid;gap:0;margin:0;padding:0;list-style:none}.fc-history li{display:grid;grid-template-columns:180px 1fr 1fr;gap:10px;padding:9px 0;border-bottom:1px solid #dce3ea;font-size:12px}.fc-history time{color:#64707d}@media(max-width:720px){.fc-detail{padding:12px}.fc-detail-heading,.fc-amendments article{display:block}.fc-detail-heading>button{margin-top:10px}.fc-amendment-line,.fc-history li{grid-template-columns:1fr}.fc-amendment-line button{width:100%}}`;
const contractItemStyles = `.fc-contract-items-heading{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;margin-top:22px;padding:14px;border-radius:10px;background:#eef6ff;border:1px solid #bfd9f4}.fc-contract-items-heading h3{margin:0 0 5px}.fc-contract-items-heading p{margin:0;max-width:760px;font-size:12px;line-height:1.5}.fc-contract-items-heading>span{white-space:nowrap;padding:7px 9px;border-radius:99px;background:#fff;color:#164e7a;font-size:12px;font-weight:700}.fc-contract-item{margin-top:12px;padding:14px;border:1px solid #ccd9e7;border-radius:10px;background:#fbfdff}.fc-item-title{display:flex;justify-content:space-between;gap:12px;margin-bottom:12px}.fc-item-title span{color:#2563eb;font-size:12px;font-weight:700}.fc-item-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.fc-item-grid label{display:grid;gap:4px;font-size:12px;color:#556170}.fc-item-grid input,.fc-item-grid select{min-width:0;padding:8px;border:1px solid #cbd4de;border-radius:6px;background:#fff}.fc-item-grid input[readonly]{background:#f1f5f9;font-weight:700}@media(max-width:900px){.fc-item-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.fc-contract-items-heading{display:block}.fc-contract-items-heading>span{display:inline-block;margin-top:10px}}@media(max-width:600px){.fc-item-grid{grid-template-columns:1fr}.fc-contract-items-heading{padding:12px}}`;
const paymentStyles = `.fc-payments{margin-top:22px;padding:16px;border:1px solid #cbd9e7;border-radius:10px;background:#f8fbff}.fc-payments h4{margin-top:0}.fc-payments .fc-error{display:grid;gap:8px}.fc-payment-form{display:grid;gap:14px;padding:14px;border:1px solid #d5dfeb;border-radius:8px;background:#fff}.fc-payment-lines{display:grid;gap:8px}.fc-payment-lines label{display:grid;grid-template-columns:minmax(0,1fr) 150px;gap:12px;align-items:center}.fc-payment-lines span,.fc-payment-lines small{display:block}.fc-payment-lines small{margin-top:3px;color:#64707d}.fc-payment-lines input{min-width:0;padding:8px;border:1px solid #cbd4de;border-radius:6px}.fc-payment-list{display:grid;gap:10px}.fc-payment-list article{padding:14px;border:1px solid #dce3ea;border-radius:8px;background:#fff}.fc-payment-list article>div:first-child{display:flex;align-items:center;gap:10px;flex-wrap:wrap}.fc-payment-list article p{flex-basis:100%;margin:0;color:#64707d}.fc-payment-list dl{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.fc-payment-list dt{font-size:11px;color:#64707d}.fc-payment-list dd{margin:3px 0 0;font-weight:700}.fc-payment-list code{display:block;overflow-wrap:anywhere}.fc-payment-empty,.fc-payment-unavailable{color:#64707d}@media(max-width:600px){.fc-payment-lines label,.fc-payment-list dl{grid-template-columns:1fr}.fc-payment-lines input{width:100%;box-sizing:border-box}.fc-payments{padding:12px}}`;
const paymentAdvancedStyles = `.fc-payment-notice{padding:10px;border:1px solid #86c99a;border-radius:7px;background:#f0fff4}.fc-payment-total{display:flex;justify-content:space-between;gap:12px;padding:10px;border-top:1px solid #d5dfeb}.fc-payment-attribution{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin:10px 0;font-size:12px}.fc-payment-list details{margin:10px 0}.fc-payment-list summary{cursor:pointer;font-weight:700}.fc-payment-reason{display:grid;gap:4px;margin-top:10px;font-size:12px}.fc-payment-reason input{box-sizing:border-box;width:100%;min-width:0;padding:8px;border:1px solid #cbd4de;border-radius:6px}@media(max-width:390px){.fc-payments{width:100%;min-width:0;box-sizing:border-box;padding:10px}.fc-payments .fc-detail-heading{display:block}.fc-payments .fc-detail-heading>button,.fc-payment-form>button,.fc-payment-list .fc-actions button{width:100%;margin-top:8px}.fc-payment-attribution,.fc-payment-list dl,.fc-payment-lines label{grid-template-columns:minmax(0,1fr)}.fc-payment-total{display:grid}.fc-payment-list article{min-width:0;padding:10px}.fc-payment-list code{word-break:break-all}}`;
const human = (value: string) => value.replace(/_/g, " ").replace(/\b\w/g, (letter) => letter.toUpperCase());
const decimal = (value: unknown) => Number(String(value ?? "0").replace(/,/g, ""));
const safePdfFileName = (value: string | null, fallback: string) => {
  const leaf = (value || "").trim().split(/[\\/]/).pop() || fallback;
  const safe = leaf.replace(/[\u0000-\u001F\u007F<>:"/\\|?*]+/g, "-").replace(/^\.+/, "").trim().slice(0, 160);
  return /\.pdf$/i.test(safe) ? safe : `${safe || fallback}.pdf`;
};
const safeDownloadFileName = (value: string | null, fallback: string, extension: "pdf" | "xlsx") => {
  const leaf = (value || "").trim().split(/[\\/]/).pop() || fallback;
  const safe = leaf.replace(/[\u0000-\u001F\u007F<>:"/\\|?*]+/g, "-").replace(/^\.+/, "").trim().slice(0, 160);
  return new RegExp(`\\.${extension}$`, "i").test(safe) ? safe : `${safe || fallback}.${extension}`;
};
const contentDispositionFileName = (header: string | null) => {
  if (!header) return null;
  const encoded = header.match(/filename\*\s*=\s*(?:"([^"]*)"|([^;]*))/i);
  if (encoded) {
    try { return decodeURIComponent((encoded[1] ?? encoded[2] ?? "").replace(/^UTF-8''/i, "")); } catch { return encoded[1] ?? encoded[2] ?? null; }
  }
  return header.match(/filename\s*=\s*"([^"]+)"/i)?.[1] ?? header.match(/filename\s*=\s*([^;]+)/i)?.[1] ?? null;
};

export function FinancialContractWorkspace() {
  const { token } = useAuthStore(), { language, tt } = useI18n();
  const lang = language;
  const [, params] = useRoute("/projects/:id/financial/contracts");
  const projectId = Number(params?.id);
  const [data, setData] = useState<any>(null), [budget, setBudget] = useState<any>(null), [snapshot, setSnapshot] = useState<any>(null), [apu, setApu] = useState<any>(null);
  const [error, setError] = useState(""), [loading, setLoading] = useState(true), [busy, setBusy] = useState(""), [showCreate, setShowCreate] = useState(false);
  const [exportError, setExportError] = useState("");
  const [search, setSearch] = useState("");
  const [statusFilter, setStatusFilter] = useState("all");
  const [counterpartyFilter, setCounterpartyFilter] = useState("all");
  const [typeFilter, setTypeFilter] = useState("all");
  const [perspectiveFilter, setPerspectiveFilter] = useState("all");
  const [currencyFilter, setCurrencyFilter] = useState("all");
  const [dateField, setDateField] = useState("none");
  const [dateFrom, setDateFrom] = useState("");
  const [dateTo, setDateTo] = useState("");
  const [valueMin, setValueMin] = useState("");
  const [valueMax, setValueMax] = useState("");
  const [sort, setSort] = useState<ContractSort>("created_desc");
  const [selectedColumns, setSelectedColumns] = useState<ContractColumn[]>(defaultColumns);
  const [selectedSections, setSelectedSections] = useState<ContractSection[]>(defaultSections);
  const [form, setForm] = useState({ legalNumber: "", perspective: "downstream", contractType: "subcontract", counterpartyName: "", title: "", currency: "USD", originalValue: "", budgetSnapshotId: "", paymentTerms: "", reviewerId: "", approverId: "", executorId: "", managerId: "" });
  const [lines, setLines] = useState<SovDraft[]>([emptyLine()]);
  const [openContractId, setOpenContractId] = useState("");
  const [contractDetail, setContractDetail] = useState<any>(null);
  const [detailLoading, setDetailLoading] = useState(false);
  const [showAmendment, setShowAmendment] = useState(false);
  const [workflowItemId, setWorkflowItemId] = useState("");
  const [amendmentForm, setAmendmentForm] = useState({ legalNumber: "", title: "", amountDelta: "" });
  const [amendmentLines, setAmendmentLines] = useState<SovDraft[]>([emptyAmendmentLine()]);
  const headers = useMemo(() => ({ Authorization: `Bearer ${token}`, "Content-Type": "application/json" }), [token]);
  const api = async (path: string, options?: RequestInit) => { const response = await fetch(`${API_BASE}/api/v1${path}`, { ...options, headers: { ...headers, ...(options?.headers ?? {}) } }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body?.error?.[language] ?? tt("Financial contract access was denied.", "Se denegó el acceso al contrato financiero.")); return body; };
  const load = async () => { setLoading(true); setError(""); try { const [contracts, budgets, apuResponse] = await Promise.all([api(`/projects/${projectId}/financial/contracts`), api(`/projects/${projectId}/financial/workspace`), api(`/projects/${projectId}/financial/apu`)]); const savedApu = apuResponse?.data?.plan ?? null; setData(contracts); setBudget(budgets); setApu(savedApu); setLines((current) => current.length === 1 && !current[0].contractItem?.apuPlanVersion ? [emptyLine(savedApu)] : current); const selected = form.budgetSnapshotId || budgets.snapshots?.[0]?.id || ""; if (selected) { setForm((current) => ({ ...current, budgetSnapshotId: selected, currency: budgets.snapshots?.find((s: any) => s.id === selected)?.currency ?? current.currency })); setSnapshot(await api(`/projects/${projectId}/financial/snapshots/${selected}`)); } } catch (e) { setData(null); setBudget(null); setSnapshot(null); setApu(null); setError(e instanceof Error ? e.message : String(e)); } finally { setLoading(false); } };
  useEffect(() => { if (projectId && token) void load(); }, [projectId, token, language]);
  const selectSnapshot = async (id: string) => { const meta = budget?.snapshots?.find((s: any) => s.id === id); setForm({ ...form, budgetSnapshotId: id, currency: meta?.currency ?? form.currency }); setLines([emptyLine(apu)]); if (id) setSnapshot(await api(`/projects/${projectId}/financial/snapshots/${id}`)); };
  const chooseBudgetLine = (index: number, id: string) => { const item = snapshot?.snapshot?.lines?.find((line: any) => line.id === id); setLines(lines.map((line, i) => i === index ? { ...line, budgetSnapshotLineId: id, projectCostNodeId: item?.project_cost_node_id ?? "", description: line.description || item?.description || "", contractItem: line.contractItem ? { ...line.contractItem, displayName: line.contractItem.displayName || item?.description || "" } : line.contractItem } : line)); };
  const updateContractItem = (index: number, patch: Partial<ContractItemDraft>) => setLines((current) => current.map((line, i) => {
    if (i !== index || !line.contractItem) return line;
    const contractItem = { ...line.contractItem, ...patch };
    const amount = itemValue(contractItem.quantity, contractItem.unitRate);
    return { ...line, description: contractItem.displayName || line.description, amount, contractItem };
  }));
  useEffect(() => {
    const total = lines.reduce((sum, line) => sum + (Number.isFinite(Number(line.amount)) ? Number(line.amount) : 0), 0).toFixed(2);
    setForm((current) => current.originalValue === total ? current : { ...current, originalValue: total });
  }, [lines]);
  const initialGrants = () => [{ value: form.reviewerId, permission: "review" }, { value: form.approverId, permission: "approve" }, { value: form.executorId, permission: "execute" }, { value: form.managerId, permission: "manage" }, { value: form.reviewerId, permission: "view" }, { value: form.approverId, permission: "view" }, { value: form.executorId, permission: "view" }, { value: form.managerId, permission: "view" }].filter((item) => item.value).map((item) => ({ userId: Number(item.value), permission: item.permission }));
  const create = async () => { setBusy("create"); setError(""); try { await api(`/projects/${projectId}/financial/contracts`, { method: "POST", body: JSON.stringify({ ...form, initialGrants: initialGrants(), commercialMetadata: { retainage: null, tax: null, bond: null, insurance: null }, lines: lines.map((line, index) => ({ ...line, scheduleItemPlacementId: line.scheduleItemPlacementId || null, sortOrder: index })) }) }); setShowCreate(false); setLines([emptyLine(apu)]); await load(); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(""); } };
  const act = async (contract: any, action: string) => { setBusy(contract.id); setError(""); try { let path = `/projects/${projectId}/financial/contracts/${contract.id}/versions/${contract.versionId}/actions`, body: any = { action, expectedRevision: contract.revision }; if (action === "approve") { path = `/projects/${projectId}/financial/contracts/${contract.id}/versions/${contract.versionId}/approve`; body = { expectedRevision: contract.revision, confirmationFingerprint: contract.contentFingerprint, overBudgetReason: window.prompt(tt("If this exceeds budget or aggregate limits, enter the required exception reason.", "Si esto excede el presupuesto o los límites agregados, indique el motivo obligatorio de la excepción.")) || undefined }; } else if (action === "execute") { const signedFileId = window.prompt(tt("Authenticated signed-document file ID", "ID del archivo firmado autenticado")); if (!signedFileId) return; path = `/projects/${projectId}/financial/contracts/${contract.id}/versions/${contract.versionId}/execute`; body = { expectedRevision: contract.revision, confirmationFingerprint: contract.contentFingerprint, signedFileId }; } else if (["return", "reject", "withdraw"].includes(action)) body.reason = window.prompt(tt("Controlled reason", "Motivo controlado")) || "Workflow decision recorded"; await api(path, { method: "POST", body: JSON.stringify(body) }); await load(); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(""); } };
  const download = async (contract: any, format: "pdf" | "xlsx") => { setBusy(`${contract.id}-${format}`); try { const response = await fetch(`${API_BASE}/api/v1/projects/${projectId}/financial/contracts/${contract.id}/export.${format}`, { headers: { Authorization: `Bearer ${token}` } }); if (!response.ok) throw new Error(tt("Export was denied.", "Se denegó la exportación.")); const url = URL.createObjectURL(await response.blob()), link = document.createElement("a"); link.href = url; link.download = safeDownloadFileName(contentDispositionFileName(response.headers.get("Content-Disposition")), `contract-${contract.legalNumber}.${format}`, format); link.click(); URL.revokeObjectURL(url); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(""); } };
  const toggleContractDetail = async (contract: any) => {
    if (openContractId === contract.id) { setOpenContractId(""); setContractDetail(null); setShowAmendment(false); return; }
    setOpenContractId(contract.id); setContractDetail(null); setShowAmendment(false); setDetailLoading(true); setError("");
    try { const response = await api(`/projects/${projectId}/financial/contracts/${contract.id}`); setContractDetail(response.detail); }
    catch (e) { setOpenContractId(""); setError(e instanceof Error ? e.message : String(e)); }
    finally { setDetailLoading(false); }
  };
  const chooseAmendmentBudgetLine = (index: number, id: string) => { const item = snapshot?.snapshot?.lines?.find((line: any) => line.id === id); setAmendmentLines(amendmentLines.map((line, i) => i === index ? { ...line, budgetSnapshotLineId: id, projectCostNodeId: item?.project_cost_node_id ?? "", description: line.description || item?.description || "" } : line)); };
  const createAmendment = async (contract: any) => {
    setBusy(`${contract.id}-amendment`); setError("");
    try {
      await api(`/projects/${projectId}/financial/contracts/${contract.id}/amendments`, { method: "POST", body: JSON.stringify({ ...amendmentForm, currency: contract.currency, budgetSnapshotId: contract.budgetSnapshotId, lines: amendmentLines.map((line, index) => ({ ...line, amountDelta: line.amount, scheduleItemPlacementId: line.scheduleItemPlacementId || null, sortOrder: index })) }) });
      setShowAmendment(false); setAmendmentForm({ legalNumber: "", title: "", amountDelta: "" }); setAmendmentLines([emptyAmendmentLine()]);
      await load(); const response = await api(`/projects/${projectId}/financial/contracts/${contract.id}`); setContractDetail(response.detail);
    } catch (e) { setError(e instanceof Error ? e.message : String(e)); }
    finally { setBusy(""); }
  };
  const toggleAmendmentForm = async (contract: any) => {
    if (showAmendment) { setShowAmendment(false); return; }
    setBusy(`${contract.id}-amendment-setup`); setError("");
    try {
      if (contract.budgetSnapshotId && snapshot?.id !== contract.budgetSnapshotId) setSnapshot(await api(`/projects/${projectId}/financial/snapshots/${contract.budgetSnapshotId}`));
      const seededLines = (contractDetail?.lines ?? []).map((line: any, index: number) => ({ stableLineId: `AMD-${Date.now()}-${index + 1}`, budgetSnapshotLineId: line.budgetSnapshotLineId, projectCostNodeId: line.projectCostNodeId, description: line.description, amount: "0.00", scheduleItemPlacementId: line.schedule?.placementId ? String(line.schedule.placementId) : "" }));
      setAmendmentLines(seededLines.length ? seededLines : [emptyAmendmentLine()]);
      setShowAmendment(true);
    } catch (e) { setError(e instanceof Error ? e.message : String(e)); }
    finally { setBusy(""); }
  };
  const actOnAmendment = async (contract: any, amendment: any, action: string) => {
    setBusy(amendment.id); setError("");
    try {
      let path = `/projects/${projectId}/financial/contracts/${contract.id}/amendments/${amendment.id}/versions/${amendment.versionId}/actions`, body: any = { action, expectedRevision: amendment.revision };
      if (action === "approve") { path = `${path.replace(/\/actions$/, "")}/approve`; body = { expectedRevision: amendment.revision, confirmationFingerprint: amendment.contentFingerprint, overBudgetReason: window.prompt(tt("If required, enter the budget exception reason.", "Si es necesario, indique el motivo de la excepcion presupuestaria.")) || undefined }; }
      else if (action === "execute") { const signedFileId = window.prompt(tt("Authenticated signed-document file ID", "ID del archivo firmado autenticado")); if (!signedFileId) return; path = `${path.replace(/\/actions$/, "")}/execute`; body = { expectedRevision: amendment.revision, confirmationFingerprint: amendment.contentFingerprint, signedFileId }; }
      else if (["return", "reject", "withdraw"].includes(action)) body.reason = window.prompt(tt("Controlled reason", "Motivo controlado")) || "Workflow decision recorded";
      await api(path, { method: "POST", body: JSON.stringify(body) }); const response = await api(`/projects/${projectId}/financial/contracts/${contract.id}`); setContractDetail(response.detail); await load();
    } catch (e) { setError(e instanceof Error ? e.message : String(e)); }
    finally { setBusy(""); }
  };
  const contracts = data?.contracts ?? [];
  const counterparties = Array.from(new Set(contracts.map((contract: any) => contract.counterpartyName).filter(Boolean))).sort();
  const currencies = Array.from(new Set(contracts.map((contract: any) => contract.currency).filter(Boolean))).sort();
  const filteredContracts = [...contracts].filter((contract: any) => {
    const dateValue = dateField === "approved" ? contract.approvedAt : dateField === "executed" ? contract.executedAt : "";
    const amount = contract.status === "executed" ? decimal(contract.currentCommitment) : decimal(contract.originalValue);
    const query = search.trim().toLowerCase();
    if (statusFilter !== "all" && contract.status !== statusFilter) return false;
    if (counterpartyFilter !== "all" && contract.counterpartyName !== counterpartyFilter) return false;
    if (typeFilter !== "all" && contract.contractType !== typeFilter) return false;
    if (perspectiveFilter !== "all" && contract.perspective !== perspectiveFilter) return false;
    if (currencyFilter !== "all" && contract.currency !== currencyFilter) return false;
    if (dateField !== "none" && (!dateValue || (dateFrom && dateValue.slice(0, 10) < dateFrom) || (dateTo && dateValue.slice(0, 10) > dateTo))) return false;
    if (valueMin && amount < Number(valueMin)) return false;
    if (valueMax && amount > Number(valueMax)) return false;
    if (!query) return true;
    return [contract.bimlogId, contract.legalNumber, contract.title, contract.counterpartyName, contract.status, contract.perspective, contract.contractType, contract.currency, contract.originalValue, contract.currentCommitment, contract.approvedAt?.slice(0, 10), contract.executedAt?.slice(0, 10)].filter(Boolean).join(" ").toLowerCase().includes(query);
  }).sort((a: any, b: any) => {
    if (sort === "legal_asc") return String(a.legalNumber).localeCompare(String(b.legalNumber));
    if (sort === "legal_desc") return String(b.legalNumber).localeCompare(String(a.legalNumber));
    if (sort === "counterparty_asc") return String(a.counterpartyName).localeCompare(String(b.counterpartyName));
    if (sort === "status_asc") return String(a.status).localeCompare(String(b.status)) || String(a.legalNumber).localeCompare(String(b.legalNumber));
    if (sort === "value_asc") return decimal(a.currentCommitment || a.originalValue) - decimal(b.currentCommitment || b.originalValue);
    if (sort === "value_desc") return decimal(b.currentCommitment || b.originalValue) - decimal(a.currentCommitment || a.originalValue);
    if (sort === "approved_desc") return String(b.approvedAt ?? "").localeCompare(String(a.approvedAt ?? ""));
    if (sort === "executed_desc") return String(b.executedAt ?? "").localeCompare(String(a.executedAt ?? ""));
    return 0;
  });
  const filteredExecutedTotal = filteredContracts.filter((contract: any) => contract.status === "executed").reduce((sum: number, contract: any) => sum + decimal(contract.currentCommitment), 0);
  const activeSummary = [
    `${tt("Status", "Estado")}: ${statusFilter === "all" ? tt("All", "Todos") : human(statusFilter)}`,
    `${tt("Counterparty", "Contraparte")}: ${counterpartyFilter === "all" ? tt("All", "Todas") : counterpartyFilter}`,
    `${tt("Type", "Tipo")}: ${typeFilter === "all" ? tt("All", "Todos") : human(typeFilter)}`,
    `${tt("Perspective", "Perspectiva")}: ${perspectiveFilter === "all" ? tt("All", "Todas") : human(perspectiveFilter)}`,
    `${tt("Currency", "Moneda")}: ${currencyFilter === "all" ? tt("All", "Todas") : currencyFilter}`,
    `${tt("Search", "Busqueda")}: ${search.trim() || tt("None", "Ninguna")}`,
    `${tt("Date", "Fecha")}: ${dateField === "none" ? tt("Not filtered", "Sin filtro") : `${human(dateField)} ${dateFrom || ".."} - ${dateTo || ".."}`}`,
    `${tt("Value", "Valor")}: ${valueMin || ".."} - ${valueMax || ".."}`,
    `${tt("Sort", "Orden")}: ${human(sort)}`,
    `${tt("Rows", "Filas")}: ${filteredContracts.length}/${contracts.length}`,
  ];
  const toggleColumn = (column: ContractColumn) => setSelectedColumns((current) => current.includes(column) ? current.filter((item) => item !== column) : [...current, column]);
  const toggleSection = (section: ContractSection) => setSelectedSections((current) => current.includes(section) ? current.filter((item) => item !== section) : [...current, section]);
  const downloadCurrentView = async () => {
    setBusy("current-view-pdf"); setExportError("");
    if (!selectedColumns.length || !selectedSections.length) {
      setExportError(tt("Select at least one PDF field and one PDF section before exporting.", "Seleccione al menos un campo PDF y una seccion PDF antes de exportar."));
      setBusy("");
      return;
    }
    const params = new URLSearchParams({ lang: language, status: statusFilter, counterparty: counterpartyFilter, contract_type: typeFilter, perspective: perspectiveFilter, currency: currencyFilter, search: search.trim(), date_field: dateField, date_from: dateFrom, date_to: dateTo, value_min: valueMin, value_max: valueMax, sort, columns: selectedColumns.join(","), sections: selectedSections.join(",") });
    try {
      const response = await fetch(`${API_BASE}/api/v1/projects/${projectId}/financial/contracts/current-view.pdf?${params.toString()}`, { headers: { Authorization: `Bearer ${token}` } });
      if (!response.ok) { const body = await response.json().catch(() => ({})); throw new Error(body?.error?.[language] ?? body?.error?.en ?? tt("Contracts current-view PDF export failed.", "Fallo la exportacion PDF de vista actual de contratos.")); }
      const url = URL.createObjectURL(await response.blob()), link = document.createElement("a");
      link.href = url; link.download = safePdfFileName(contentDispositionFileName(response.headers.get("Content-Disposition")), "contracts-current-view.pdf"); link.click(); URL.revokeObjectURL(url);
    } catch (e) { setExportError(e instanceof Error ? e.message : String(e)); } finally { setBusy(""); }
  };
  if (loading && !data) {
    return <FinancialProjectShell projectId={projectId} activeTab="contracts"><main style={{ maxWidth: 760, margin: "24px auto", padding: 24, border: "1px solid #D7DEE8", borderRadius: 12, background: "#FFFFFF" }}><h1>{tt("Contracts & Commitments", "Contratos y Compromisos")}</h1><p>{tt("Loading controlled financial records...", "Cargando registros financieros controlados...")}</p></main></FinancialProjectShell>;
  }
  if (error && !data) {
    return <FinancialProjectShell projectId={projectId} activeTab="contracts"><main role="alert" style={{ maxWidth: 760, margin: "24px auto", padding: 24, border: "1px solid #F0B4B4", borderRadius: 12, background: "#FFF7F7" }}><h1>{tt("Financial access denied", "Acceso financiero denegado")}</h1><p>{error}</p><button type="button" onClick={() => void load()}>{tt("Retry", "Reintentar")}</button></main></FinancialProjectShell>;
  }
  return <FinancialProjectShell projectId={projectId} activeTab="contracts"><div className="fc-page"><style>{styles}{detailStyles}{contractItemStyles}{paymentStyles}{paymentAdvancedStyles}</style>
    <header className="fc-header"><div><Link href={`/projects/${projectId}/financial/budget`}>← {tt("Project Budget", "Presupuesto del Proyecto")}</Link><h1>{tt("Contracts & Commitments", "Contratos y Compromisos")}</h1><p>{tt("Exact, versioned contract terms and schedules of values", "Términos y SOV exactos y versionados")}</p></div><button onClick={() => setShowCreate(!showCreate)}>{showCreate ? tt("Close", "Cerrar") : tt("New contract", "Nuevo contrato")}</button></header>
    <section className="fc-boundary">{tt("Operational project control only. Approval is separate from signed-document execution. No accounting posting, invoice payment, bank movement, external portal, or automatic AI.", "Solo control operativo del proyecto. La aprobación está separada de la ejecución con documento firmado. Sin asientos contables, pago de facturas, movimientos bancarios, portal externo ni IA automática.")}</section>
    {error && <div className="fc-error" role="alert">{error}</div>}
    {exportError && <div className="fc-error" role="alert">{exportError}</div>}
    {showCreate && <section className="fc-panel"><h2>{tt("Controlled contract draft", "Borrador de contrato controlado")}</h2><div className="fc-form">
      <label>{tt("Legal number", "Número legal")}<input value={form.legalNumber} onChange={(e) => setForm({ ...form, legalNumber: e.target.value })}/></label>
      <label>{tt("Perspective", "Perspectiva")}<select value={form.perspective} onChange={(e) => setForm({ ...form, perspective: e.target.value })}><option value="downstream">{tt("Downstream commitment", "Compromiso descendente")}</option><option value="upstream">{tt("Owner / prime", "Propietario / principal")}</option></select></label>
      <label>{tt("Type", "Tipo")}<select value={form.contractType} onChange={(e) => setForm({ ...form, contractType: e.target.value })}><option value="subcontract">{tt("Subcontract", "Subcontrato")}</option><option value="purchase_order">{tt("Purchase order", "Orden de compra")}</option><option value="consultant_agreement">{tt("Consultant agreement", "Acuerdo de consultoría")}</option><option value="owner_prime">{tt("Owner / prime contract", "Contrato propietario / principal")}</option><option value="other_commitment">{tt("Other commitment", "Otro compromiso")}</option></select></label>
      <label>{tt("Counterparty", "Contraparte")}<input value={form.counterpartyName} onChange={(e) => setForm({ ...form, counterpartyName: e.target.value })}/></label>
      <label>{tt("Title", "Título")}<input value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })}/></label>
      <label>{tt("Approved budget snapshot", "Instantánea de presupuesto aprobada")}<select value={form.budgetSnapshotId} onChange={(e) => void selectSnapshot(e.target.value)}><option value="">—</option>{budget?.snapshots?.map((s: any) => <option key={s.id} value={s.id}>v{s.budgetVersion} · {s.currentTotal} {s.currency}</option>)}</select></label>
      <label>{tt("Calculated contract value", "Valor contractual calculado")}<input inputMode="decimal" value={form.originalValue} readOnly/></label>
      <label>{tt("ISO currency", "Moneda ISO")}<input value={form.currency} readOnly/></label>
      <label className="fc-wide">{tt("Payment terms (metadata only)", "Términos de pago (solo metadatos)")}<input value={form.paymentTerms} onChange={(e) => setForm({ ...form, paymentTerms: e.target.value })}/></label>
    </div><h3>{tt("Internal record grants", "Permisos internos del registro")}</h3><p>{tt("Enter current project-member IDs. These grants never replace financial authority.", "Ingrese IDs de miembros vigentes. Estos permisos nunca sustituyen la autoridad financiera.")}</p><div className="fc-form"><label>{tt("Reviewer user ID", "ID del revisor")}<input inputMode="numeric" value={form.reviewerId} onChange={(e) => setForm({ ...form, reviewerId: e.target.value })}/></label><label>{tt("Approver user ID", "ID del aprobador")}<input inputMode="numeric" value={form.approverId} onChange={(e) => setForm({ ...form, approverId: e.target.value })}/></label><label>{tt("Executor user ID", "ID del ejecutor")}<input inputMode="numeric" value={form.executorId} onChange={(e) => setForm({ ...form, executorId: e.target.value })}/></label><label>{tt("Record manager user ID", "ID del administrador del registro")}<input inputMode="numeric" value={form.managerId} onChange={(e) => setForm({ ...form, managerId: e.target.value })}/></label></div>
      <div className="fc-contract-items-heading"><div><h3>{tt("Contract Items", "Partidas de Contrato")}</h3><p>{tt("For BIM Services each item is displayed as a Submittal. Quantity × the selected saved APU unit rate calculates its contractual value and freezes the exact APU snapshot.", "Para Servicios BIM cada partida se muestra como Submittal. Cantidad × la tarifa unitaria del APU guardado calcula su valor contractual y congela la instantánea exacta del APU.")}</p></div><span>{apu ? `APU v${apu.version} · ${apu.sellingPrice} ${apu.currency}` : tt("Save an APU plan before creating Contract Items", "Guarde un plan APU antes de crear Partidas de Contrato")}</span></div>
      {lines.map((line, index) => <div className="fc-contract-item" key={line.stableLineId}><div className="fc-item-title"><strong>{tt("Contract Item", "Partida de Contrato")} {index + 1}</strong><span>{tt("Display: Submittal", "Nombre visible: Submittal")}</span></div><div className="fc-item-grid"><label>{tt("Budget line", "Línea presupuestaria")}<select value={line.budgetSnapshotLineId} onChange={(e) => chooseBudgetLine(index, e.target.value)}><option value="">—</option>{snapshot?.snapshot?.lines?.map((item: any) => <option key={item.id} value={item.id}>{item.project_code} · {item.description} · {String(item.amount)}</option>)}</select></label><label>{tt("Contract Item ID", "ID de Partida")}<input value={line.stableLineId} onChange={(e) => setLines(lines.map((x, i) => i === index ? { ...x, stableLineId: e.target.value } : x))}/></label><label className="fc-wide">{tt("Contract Item / Submittal name", "Nombre de Partida / Submittal")}<input value={line.contractItem?.displayName ?? ""} onChange={(e) => updateContractItem(index, { displayName: e.target.value })}/></label><label>{tt("Quantity", "Cantidad")}<input inputMode="decimal" value={line.contractItem?.quantity ?? ""} onChange={(e) => updateContractItem(index, { quantity: e.target.value })}/></label><label>{tt("Unit", "Unidad")}<input value={line.contractItem?.unit ?? ""} onChange={(e) => updateContractItem(index, { unit: e.target.value })}/></label><label>{tt("APU unit rate", "Tarifa unitaria APU")}<input inputMode="decimal" value={line.contractItem?.unitRate ?? ""} readOnly/></label><label>{tt("Calculated item value", "Valor calculado de partida")}<input value={line.amount} readOnly/></label><label>{tt("APU snapshot", "Instantánea APU")}<input value={line.contractItem?.apuPlanVersion ? `v${line.contractItem.apuPlanVersion}` : "—"} readOnly/></label><label>{tt("Workflow template", "Plantilla de flujo")}<select value={line.contractItem?.workflowTemplate ?? "bim-submittal"} onChange={(e) => updateContractItem(index, { workflowTemplate: e.target.value })}><option value="bim-submittal">{tt("BIM: Phase → Revision → Version → Task", "BIM: Fase → Revisión → Versión → Tarea")}</option><option value="generic">{tt("Generic configurable workflow", "Flujo genérico configurable")}</option></select></label><label>{tt("Optional Schedule item ID", "ID opcional del elemento de Schedule")}<input inputMode="numeric" value={line.scheduleItemPlacementId} onChange={(e) => setLines(lines.map((x, i) => i === index ? { ...x, scheduleItemPlacementId: e.target.value } : x))}/></label></div>{lines.length > 1 && <button onClick={() => setLines(lines.filter((_, i) => i !== index))}>{tt("Remove Contract Item", "Quitar Partida")}</button>}</div>)}
      <div className="fc-actions"><button disabled={!apu} onClick={() => setLines([...lines, emptyLine(apu)])}>{tt("Add Contract Item", "Agregar Partida de Contrato")}</button><button className="primary" disabled={busy === "create" || !apu} onClick={() => void create()}>{tt("Create contract with frozen APU items", "Crear contrato con partidas APU congeladas")}</button></div>
    </section>}
    <section className="fc-export-view" aria-label={tt("Contracts current-view controls", "Controles de vista actual de contratos")}>
      <div className="fc-export-top">
        <div><h2>{tt("Contracts current view", "Vista actual de contratos")}</h2><p>{tt("Operational filters stay visible and govern both the records below and Print PDF.", "Los filtros operativos permanecen visibles y controlan los registros y el PDF.")}</p></div>
        <PrintPdfButton
          lang={lang}
          loading={busy === "current-view-pdf"}
          disabled={busy !== "" || !data}
          configurationInvalid={!selectedColumns.length || !selectedSections.length}
          disabledReason={tt("Select at least one PDF field and one PDF section.", "Seleccione al menos un campo PDF y una seccion PDF.")}
          currentViewSummary={activeSummary}
          options={<div style={{ display: "grid", gap: 14 }}>
            <div className="fc-checks" style={{ marginTop: 0 }}><strong>{tt("PDF fields", "Campos PDF")}</strong>{(["legalNumber", "title", "counterparty", "status", "type", "perspective", "originalValue", "currentCommitment", "currency", "approvedAt", "executedAt"] as ContractColumn[]).map((column) => <label key={column}><input type="checkbox" checked={selectedColumns.includes(column)} onChange={() => toggleColumn(column)}/>{human(column)}</label>)}</div>
            <div className="fc-checks" style={{ marginTop: 0 }}><strong>{tt("PDF sections", "Secciones PDF")}</strong>{(["summary", "filters", "contracts"] as ContractSection[]).map((section) => <label key={section}><input type="checkbox" checked={selectedSections.includes(section)} onChange={() => toggleSection(section)}/>{human(section)}</label>)}</div>
            {(!selectedColumns.length || !selectedSections.length) && <p className="fc-validation" role="alert">{tt("Select at least one field and one section.", "Seleccione al menos un campo y una seccion.")}</p>}
          </div>}
          onClick={() => void downloadCurrentView()}
        />
      </div>
      <div className="fc-filter-grid"><label>{tt("Search", "Busqueda")}<input value={search} onChange={(e) => setSearch(e.target.value)} placeholder={tt("Search number, title, company, status...", "Buscar numero, titulo, empresa, estado...")}/></label><label>{tt("Status", "Estado")}<select value={statusFilter} onChange={(e) => setStatusFilter(e.target.value)}>{statusOptions.map((item) => <option key={item} value={item}>{item === "all" ? tt("All statuses", "Todos los estados") : human(item)}</option>)}</select></label><label>{tt("Counterparty", "Contraparte")}<select value={counterpartyFilter} onChange={(e) => setCounterpartyFilter(e.target.value)}><option value="all">{tt("All counterparties", "Todas las contrapartes")}</option>{counterparties.map((item: any) => <option key={item} value={item}>{item}</option>)}</select></label><label>{tt("Type", "Tipo")}<select value={typeFilter} onChange={(e) => setTypeFilter(e.target.value)}>{typeOptions.map((item) => <option key={item} value={item}>{item === "all" ? tt("All types", "Todos los tipos") : human(item)}</option>)}</select></label><label>{tt("Perspective", "Perspectiva")}<select value={perspectiveFilter} onChange={(e) => setPerspectiveFilter(e.target.value)}>{perspectiveOptions.map((item) => <option key={item} value={item}>{item === "all" ? tt("All perspectives", "Todas las perspectivas") : human(item)}</option>)}</select></label><label>{tt("Currency", "Moneda")}<select value={currencyFilter} onChange={(e) => setCurrencyFilter(e.target.value)}><option value="all">{tt("All currencies", "Todas las monedas")}</option>{currencies.map((item: any) => <option key={item} value={item}>{item}</option>)}</select></label><label>{tt("Date field", "Campo de fecha")}<select value={dateField} onChange={(e) => setDateField(e.target.value)}><option value="none">{tt("No date filter", "Sin filtro de fecha")}</option><option value="approved">{tt("Approved date", "Fecha de aprobacion")}</option><option value="executed">{tt("Executed date", "Fecha de ejecucion")}</option></select></label><label>{tt("From", "Desde")}<input type="date" value={dateFrom} onChange={(e) => setDateFrom(e.target.value)} disabled={dateField === "none"}/></label><label>{tt("To", "Hasta")}<input type="date" value={dateTo} onChange={(e) => setDateTo(e.target.value)} disabled={dateField === "none"}/></label><label>{tt("Minimum value", "Valor minimo")}<input inputMode="decimal" value={valueMin} onChange={(e) => setValueMin(e.target.value)} placeholder="0.00"/></label><label>{tt("Maximum value", "Valor maximo")}<input inputMode="decimal" value={valueMax} onChange={(e) => setValueMax(e.target.value)} placeholder="0.00"/></label><label>{tt("Sort", "Orden")}<select value={sort} onChange={(e) => setSort(e.target.value as ContractSort)}>{sortOptions.map((item) => <option key={item} value={item}>{human(item)}</option>)}</select></label></div>
      <p className="fc-active-summary">{activeSummary.join(" | ")}</p>
    </section>
    <section className="fc-summary"><div><span>{tt("Visible contracts", "Contratos visibles")}</span><strong>{filteredContracts.length}/{contracts.length}</strong></div><div><span>{tt("Executed commitment in view", "Compromiso ejecutado en vista")}</span><strong>{filteredExecutedTotal.toFixed(2)}</strong></div><div><span>{tt("Currencies in view", "Monedas en vista")}</span><strong>{Array.from(new Set(filteredContracts.map((contract: any) => contract.currency))).join(", ") || "-"}</strong></div></section>
    <main className="fc-cards">{filteredContracts.map((contract: any) => <article key={contract.id} className="fc-card"><div className="fc-card-head"><div><small>{contract.bimlogId}</small><h2>{contract.legalNumber} · {contract.title}</h2><p>{contract.counterpartyName} · {contract.perspective} · {contract.contractType}</p></div><span className={`fc-status ${contract.status}`}>{contract.status}</span></div><div className="fc-money"><span>{tt("Original", "Original")}<b>{contract.originalValue} {contract.currency}</b></span><span>{tt("Executed amendments", "Enmiendas ejecutadas")}<b>{contract.executedAmendmentTotal}</b></span><span>{tt("Current commitment", "Compromiso actual")}<b>{contract.currentCommitment}</b></span></div><code>{contract.contentFingerprint}</code><div className="fc-actions">
      {contract.status === "draft" && <button disabled={busy === contract.id} onClick={() => void act(contract, "submit")}>{tt("Submit", "Enviar")}</button>}
      {contract.status === "submitted" && <><button disabled={busy === contract.id} onClick={() => void act(contract, "start_review")}>{tt("Start review", "Iniciar revisión")}</button><button disabled={busy === contract.id} onClick={() => void act(contract, "withdraw")}>{tt("Withdraw", "Retirar")}</button></>}
      {contract.status === "under_review" && <><button disabled={busy === contract.id} onClick={() => void act(contract, "approve")}>{tt("Confirm exact approval", "Confirmar aprobación exacta")}</button><button disabled={busy === contract.id} onClick={() => void act(contract, "return")}>{tt("Return", "Devolver")}</button><button disabled={busy === contract.id} onClick={() => void act(contract, "reject")}>{tt("Reject", "Rechazar")}</button></>}
      {contract.status === "approved" && <button disabled={busy === contract.id} onClick={() => void act(contract, "execute")}>{tt("Attest signed execution", "Atestar ejecución firmada")}</button>}
      <button aria-expanded={openContractId === contract.id} disabled={detailLoading && openContractId === contract.id} onClick={() => void toggleContractDetail(contract)}>{openContractId === contract.id ? tt("Hide contract details", "Ocultar detalles del contrato") : tt("View contract details", "Ver detalles del contrato")}</button>
      <div className="fc-export-panel" aria-label={tt("Contract export actions", "Acciones de exportación del contrato")}>
        <div>
          <strong>{tt("Generate contract outputs", "Generar salidas del contrato")}</strong>
          <p>{tt("PDF is the formal contract report for sharing and record retention. XLSX is the workbook for analysis and reconciliation.", "PDF es el reporte formal del contrato para compartir y conservar el registro. XLSX es el libro para análisis y conciliación.")}</p>
        </div>
        <div className="fc-export-actions">
          <button
            disabled={busy !== ""}
            onClick={() => void download(contract, "pdf")}
            title={tt("Generate formal contract PDF report", "Generar reporte PDF formal del contrato")}
            aria-label={tt(`Generate PDF report for contract ${contract.legalNumber}`, `Generar reporte PDF para el contrato ${contract.legalNumber}`)}
          >
            {busy === `${contract.id}-pdf` ? tt("Generating PDF...", "Generando PDF...") : tt("Generate PDF Report", "Generar reporte PDF")}
          </button>
          <button
            disabled={busy !== ""}
            onClick={() => void download(contract, "xlsx")}
            title={tt("Download contract workbook for analysis", "Descargar libro del contrato para análisis")}
            aria-label={tt(`Download XLSX workbook for contract ${contract.legalNumber}`, `Descargar libro XLSX para el contrato ${contract.legalNumber}`)}
          >
            {busy === `${contract.id}-xlsx` ? tt("Preparing XLSX...", "Preparando XLSX...") : tt("Download XLSX Workbook", "Descargar libro XLSX")}
          </button>
        </div>
      </div>
      {openContractId === contract.id && <section className="fc-detail" aria-label={tt("Contract detail workspace", "Espacio de detalles del contrato")}>
        {detailLoading && <p>{tt("Loading contract details...", "Cargando detalles del contrato...")}</p>}
        {!detailLoading && contractDetail && <>
          <div className="fc-detail-heading"><div><h3>{tt("Contract detail workspace", "Espacio de detalles del contrato")}</h3><p>{tt("Schedule of Values, amendments, and immutable activity history in one place.", "Programa de Valores, enmiendas e historial inmutable en un solo lugar.")}</p></div>{contract.status === "executed" && <button disabled={busy !== ""} onClick={() => void toggleAmendmentForm(contract)}>{showAmendment ? tt("Close amendment", "Cerrar enmienda") : tt("New amendment", "Nueva enmienda")}</button>}</div>
          <h4>{tt("Contract Items / Schedule of Values", "Partidas de Contrato / Programa de Valores")}</h4>
          <div className="fc-table-wrap"><table className="fc-table"><thead><tr><th>{tt("Code", "Código")}</th><th>{tt("Contract Item", "Partida de Contrato")}</th><th>{tt("Quantity × Rate", "Cantidad × Tarifa")}</th><th>{tt("Contract value", "Valor contractual")}</th><th>{tt("APU snapshot", "Instantánea APU")}</th><th>{tt("Workflow", "Flujo")}</th></tr></thead><tbody>{contractDetail.lines.map((line: any) => <tr key={line.stableLineId}><td>{line.projectCode}<small>{line.projectName}</small></td><td>{line.contractItem?.displayName || line.description}<small>{line.stableLineId} · {line.contractItem?.industryTemplate === "bim-services" ? "Submittal" : "Contract Item"}</small></td><td>{line.contractItem?.quantity ?? "1"} {line.contractItem?.unit ?? "LS"} × {line.contractItem?.unitRate ?? line.amount} {contract.currency}</td><td>{line.amount} {contract.currency}<small>{tt("Approved budget", "Presupuesto aprobado")}: {line.budgetAmount}</small></td><td>{line.contractItem?.apuPlanVersion ? `v${line.contractItem.apuPlanVersion}` : tt("Legacy line", "Línea heredada")}<small>{line.contractItem?.apuFingerprint?.slice(0, 12) ?? "—"}</small></td><td>{human(line.contractItem?.workflowTemplate ?? "legacy")}<small>{line.schedule ? `${line.schedule.sourceType} #${line.schedule.sourceId}` : tt("Not linked", "Sin vínculo")}</small><button onClick={() => setWorkflowItemId(workflowItemId === line.stableLineId ? "" : line.stableLineId)}>{workflowItemId === line.stableLineId ? tt("Close workflow", "Cerrar flujo") : tt("Open workflow", "Abrir flujo")}</button></td></tr>)}</tbody></table></div>
          {contractDetail.lines.map((line: any) => workflowItemId === line.stableLineId && <ContractItemWorkflowPanel key={line.stableLineId} projectId={projectId} contractId={contract.id} stableLineId={line.stableLineId} displayName={line.contractItem?.displayName || line.description} token={token!} language={language} tt={tt}/>)}
          {showAmendment && <div className="fc-amendment-form"><h4>{tt("Amendment workflow", "Flujo de enmienda")}</h4><p>{tt("Create an exact signed delta against the approved budget. Line deltas must reconcile to the amendment total.", "Cree un delta firmado exacto contra el presupuesto aprobado. Los deltas de linea deben conciliar con el total de la enmienda.")}</p><div className="fc-form"><label>{tt("Legal amendment number", "Numero legal de enmienda")}<input value={amendmentForm.legalNumber} onChange={(e) => setAmendmentForm({ ...amendmentForm, legalNumber: e.target.value })}/></label><label>{tt("Title", "Titulo")}<input value={amendmentForm.title} onChange={(e) => setAmendmentForm({ ...amendmentForm, title: e.target.value })}/></label><label>{tt("Exact amendment delta", "Delta exacto de la enmienda")}<input inputMode="decimal" value={amendmentForm.amountDelta} onChange={(e) => setAmendmentForm({ ...amendmentForm, amountDelta: e.target.value })}/></label><label>{tt("Currency", "Moneda")}<input value={contract.currency} readOnly/></label></div>{amendmentLines.map((line, index) => <div className="fc-amendment-line" key={line.stableLineId}><label>{tt("Budget line", "Linea presupuestaria")}<select value={line.budgetSnapshotLineId} onChange={(e) => chooseAmendmentBudgetLine(index, e.target.value)}><option value="">-</option>{snapshot?.snapshot?.lines?.map((item: any) => <option key={item.id} value={item.id}>{item.project_code} - {item.description}</option>)}</select></label><label>{tt("Delta line ID", "ID de linea delta")}<input value={line.stableLineId} onChange={(e) => setAmendmentLines(amendmentLines.map((item, i) => i === index ? { ...item, stableLineId: e.target.value } : item))}/></label><label>{tt("Description", "Descripcion")}<input value={line.description} onChange={(e) => setAmendmentLines(amendmentLines.map((item, i) => i === index ? { ...item, description: e.target.value } : item))}/></label><label>{tt("Signed delta", "Delta firmado")}<input inputMode="decimal" value={line.amount} onChange={(e) => setAmendmentLines(amendmentLines.map((item, i) => i === index ? { ...item, amount: e.target.value } : item))}/></label>{amendmentLines.length > 1 && <button onClick={() => setAmendmentLines(amendmentLines.filter((_, i) => i !== index))}>{tt("Remove", "Quitar")}</button>}</div>)}<div className="fc-actions"><button onClick={() => setAmendmentLines([...amendmentLines, emptyAmendmentLine()])}>{tt("Add delta line", "Agregar linea delta")}</button><button className="primary" disabled={busy !== ""} onClick={() => void createAmendment(contract)}>{tt("Create amendment draft", "Crear borrador de enmienda")}</button></div></div>}
          <h4>{tt("Amendments", "Enmiendas")}</h4>
          <div className="fc-amendments">{contractDetail.amendments.length === 0 ? <p>{tt("No amendments recorded.", "No hay enmiendas registradas.")}</p> : contractDetail.amendments.map((amendment: any) => <article key={amendment.id}><div><strong>{amendment.legalNumber} - {amendment.title}</strong><span>{amendment.amountDelta} {amendment.currency} - {human(amendment.status)}</span><code>{amendment.contentFingerprint}</code></div><div className="fc-actions">{amendment.status === "draft" && <button disabled={busy === amendment.id} onClick={() => void actOnAmendment(contract, amendment, "submit")}>{tt("Submit", "Enviar")}</button>}{amendment.status === "submitted" && <><button disabled={busy === amendment.id} onClick={() => void actOnAmendment(contract, amendment, "start_review")}>{tt("Start review", "Iniciar revision")}</button><button disabled={busy === amendment.id} onClick={() => void actOnAmendment(contract, amendment, "withdraw")}>{tt("Withdraw", "Retirar")}</button></>}{amendment.status === "under_review" && <><button disabled={busy === amendment.id} onClick={() => void actOnAmendment(contract, amendment, "approve")}>{tt("Approve exact delta", "Aprobar delta exacto")}</button><button disabled={busy === amendment.id} onClick={() => void actOnAmendment(contract, amendment, "return")}>{tt("Return", "Devolver")}</button><button disabled={busy === amendment.id} onClick={() => void actOnAmendment(contract, amendment, "reject")}>{tt("Reject", "Rechazar")}</button></>}{amendment.status === "approved" && <button disabled={busy === amendment.id} onClick={() => void actOnAmendment(contract, amendment, "execute")}>{tt("Attest signed execution", "Atestar ejecucion firmada")}</button>}</div></article>)}</div>
          {contract.status === "executed" ? <ContractPaymentApplicationsPanel projectId={projectId} contract={contract} lines={contractDetail.lines} token={token} language={language} tt={tt}/> : <section className="fc-payments fc-payment-unavailable"><h4>{tt("Payment applications", "Solicitudes de pago")}</h4><p>{tt("Payment applications become available only after governed contract execution.", "Las solicitudes de pago están disponibles solo después de la ejecución controlada del contrato.")}</p></section>}
          <h4>{tt("Immutable activity history", "Historial de actividad inmutable")}</h4>
          <ol className="fc-history">{contractDetail.history.map((event: any, index: number) => <li key={`${event.occurredAt}-${index}`}><time>{new Date(event.occurredAt).toLocaleString()}</time><strong>{human(event.eventType)}</strong><span>{event.beforeState && event.afterState ? `${human(event.beforeState)} -> ${human(event.afterState)}` : event.reasonCode}</span></li>)}</ol>
        </>}
      </section>}
    </div></article>)}{data && contracts.length === 0 && <div className="fc-empty">{tt("No accessible contract records. An authorized preparer may create one.", "No hay contratos accesibles. Un preparador autorizado puede crear uno.")}</div>}{data && contracts.length > 0 && filteredContracts.length === 0 && <div className="fc-empty">{tt("No contracts match the current filters.", "Ningun contrato coincide con los filtros actuales.")}</div>}</main>
  </div></FinancialProjectShell>;
}

const styles = `.fc-page{min-height:100vh;background:#f4f6f8;color:#17212b;padding:24px;overflow-x:hidden}.fc-page>*{max-width:1180px;margin-left:auto;margin-right:auto}.fc-header{display:flex;justify-content:space-between;align-items:end;gap:16px}.fc-header h1{margin:8px 0 4px;font-size:28px}.fc-header p,.fc-panel p,.fc-card p{color:#64707d}.fc-page button,.fc-page input,.fc-page select{font:inherit}.fc-page button{border:1px solid #c7d1dc;border-radius:7px;padding:8px 12px;background:#fff;color:#164e7a;cursor:pointer}.fc-page button.primary{background:#164e7a;color:#fff}.fc-page button:disabled{opacity:.5;cursor:not-allowed}.fc-boundary{margin-top:18px;padding:12px 14px;border-left:4px solid #bf7a13;background:#fff8e8}.fc-error,.fc-validation{margin-top:12px;padding:12px;background:#fff0f0;color:#9b2525;border-radius:8px}.fc-panel,.fc-export-view{margin-top:14px;padding:18px;background:#fff;border:1px solid #dce3ea;border-radius:10px}.fc-export-top{display:flex;align-items:flex-start;justify-content:space-between;gap:14px;margin-bottom:12px}.fc-export-top h2{margin:0 0 4px;font-size:18px}.fc-export-top p,.fc-active-summary{margin:0;color:#64707d;font-size:12px;line-height:1.45}.fc-filter-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.fc-checks{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-top:12px;font-size:12px;color:#556170}.fc-checks strong{color:#17212b}.fc-checks label{display:inline-flex;align-items:center;gap:5px}.fc-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.fc-form label,.fc-line label,.fc-filter-grid label{display:grid;gap:4px;font-size:12px;color:#556170}.fc-form input,.fc-form select,.fc-line input,.fc-line select,.fc-filter-grid input,.fc-filter-grid select{min-width:0;padding:8px;border:1px solid #cbd4de;border-radius:6px;background:#fff}.fc-wide{grid-column:1/-1}.fc-line{display:grid;grid-template-columns:1.2fr .8fr 1.4fr .7fr .8fr auto;gap:8px;padding:10px 0;border-top:1px solid #edf0f3;align-items:end}.fc-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}.fc-export-panel{flex-basis:100%;display:grid;gap:10px;max-width:560px;margin-top:4px;padding:12px;border:1px solid #ccdff1;border-radius:10px;background:#f8fbff}.fc-export-panel strong{display:block;color:#123f68;font-size:13px}.fc-export-panel p{margin:4px 0 0;font-size:12px;line-height:1.45}.fc-export-actions{display:flex;gap:8px;flex-wrap:wrap}.fc-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:14px}.fc-summary>div{padding:14px;background:#fff;border:1px solid #dce3ea;border-radius:9px}.fc-summary span{display:block;color:#65717e;font-size:12px}.fc-summary strong{display:block;margin-top:5px;font-size:20px;font-variant-numeric:tabular-nums}.fc-cards{display:grid;gap:12px;margin-top:14px}.fc-card{padding:17px;background:#fff;border:1px solid #dce3ea;border-radius:10px}.fc-card-head{display:flex;justify-content:space-between;gap:16px}.fc-card h2{margin:5px 0;font-size:18px}.fc-card small,.fc-card code{color:#6b7682;overflow-wrap:anywhere}.fc-status{align-self:start;padding:5px 9px;background:#eef2f6;border-radius:99px;font-size:11px;text-transform:uppercase}.fc-status.executed{background:#e7f5ec;color:#21633a}.fc-status.approved{background:#eaf2ff;color:#20578b}.fc-money{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:12px 0}.fc-money span{font-size:11px;color:#67727f}.fc-money b{display:block;font-size:16px;color:#17212b;margin-top:3px;font-variant-numeric:tabular-nums}.fc-empty{padding:40px;text-align:center;background:#fff;border:1px solid #dce3ea;border-radius:10px;color:#697582}@media(max-width:720px){.fc-page{padding:12px}.fc-header{display:block}.fc-header>button{margin-top:12px}.fc-export-top{display:block}.fc-export-top>button{margin-top:10px;width:100%}.fc-filter-grid,.fc-form,.fc-summary,.fc-money{grid-template-columns:1fr}.fc-wide{grid-column:auto}.fc-line{grid-template-columns:1fr;padding:14px 0}.fc-card-head{display:block}.fc-status{display:inline-block;margin-top:6px}.fc-actions button,.fc-export-actions button{flex:1 1 auto}.fc-export-panel{max-width:none}.fc-card code{display:block}.fc-page h1{font-size:23px}}`;
export const financialContractWorkspaceStyles = styles + detailStyles + contractItemStyles + paymentStyles + paymentAdvancedStyles;
