# BIMLog Feedback pass-through relay and Roberto receiver

Release identity: `v1.60.35.09-F`. Google Drive is excluded. PostgreSQL is the lifecycle/backlog authority; the receiver filesystem is a verified custody projection.

## One feedback, one package

Each authorized feedback export is assembled from a fresh PostgreSQL snapshot. The ZIP contains a canonical JSON manifest, a human-readable PDF, and only evidence with a completed clean scanner receipt. The PDF carries submission metadata, submitter, timestamps, message, status history, evidence inventory, authenticated download links, and bounded embedded PNG/JPEG evidence. Customer packages redact internal history, reasons, and submitter email; internal packages and the PostgreSQL-derived follow-up CSV require super-admin authority plus an export reason. Package reads are bounded, hash-verified, and fail closed on missing storage, scanner, or integrity proof.

## Single protocol

All receiver traffic uses HTTPS and refuses redirects. Configuration pins the destination origin and leaf certificate or SPKI SHA-256 fingerprint. Every request is HMAC-SHA256 signed over a versioned, length-delimited canonical form containing exact method, path and sorted query, audience, key ID, UTC timestamp, nonce, request ID, company and project scope, feedback stable ID, asset/object ID, exact byte count, SHA-256, and body digest. The receiver accepts active and explicitly bounded grace keys, signs only with the active key, enforces clock skew, and atomically consumes nonces bound to the complete request digest. Secrets, roots, content, and unsanitized names are never logged.

Endpoints are capability/health, idempotent object delivery, immutable receipt retrieval, exact-byte readback verification, and custody status. A receipt canonically binds protocol version, feedback ID, asset/object ID, bytes, SHA-256, destination identity, received timestamp, nonce/request identity, and receiver key ID/signature. A successful HTTP status without a valid receipt is failure. Lost responses replay the same idempotency identity. A valid receipt prevents byte resend. Temporary deletion requires both a valid receipt and independent readback/hash confirmation where advertised. Final deletion evidence uses a distinct canonical domain and binds request/scope/object identity, bytes/hash, destination, policy, approver/times, command ID/digest, job/version, fencing token, hold-snapshot/no-hold proof, absence time, and prepared-journal hash; historical verification is signed-time bound.

## Relay state machine

The accepted temporary-custody order is `queued -> transferring -> receipt-verified -> cleanup-pending -> delivered`. Direct `receipt-verified -> delivered` is allowed only when no temporary bytes exist and verified readback makes cleanup unnecessary. `transferring -> delivered`, `delivered -> receipt-verified`, and cleanup before a verified receipt are forbidden. Terminal/exception states are `manual-review`, `held`, and `expired`. Only governed clean scanner output may enter transfer; audio transcription follows clean custody and linked consent. Quarantined or rejected evidence never becomes permanent receiver custody. Transitions are versioned, transactional, idempotent, and audited.

Direct mode streams to a healthy configured receiver and retains no permanent BIMLog/Replit bytes. Queue mode exists only through the private encrypted `TemporaryRelayStore` port, with explicit configuration, TTL, byte/count quotas, opaque keys, no public URL, and compare-and-delete semantics. No approved adapter means fail closed with a visible unavailable/pending state. Retry count, exponential delay, concurrency lease, poison classification, and dead-letter/manual review are bounded and persisted. Cancellation stops unstarted work; holds defeat expiry and deletion. Partial transfer, hash mismatch, stale/forged receipt, unknown outcome, delete failure, or failed absence proof never discard bytes silently.

## Roberto-controlled receiver projection

The configured root must be independently approved and must not be a workspace, application, build, cache, or temporary directory. Every existing component is checked for links/reparse points and physical containment.

The exact operator-approved root on Roberto's current machine is `F:/BIMLog/Feedback`. Other servers configure `<BIMLogDataRoot>/Feedback`; the receiver never creates or selects a root implicitly. The root must remain outside repositories, worktrees, evidence, application, build, cache, and temporary custody.

The implemented custody core uses opaque hashes of company, project, feedback, and object identity beneath `01-Active`, with immutable request/deletion indexes under `99-System`. Release purge is reachable only through a canonical signed `FeedbackPurgeCommand` plus an asynchronous current-authority port. An injected trusted clock is freshly sampled at every authority and mutation boundary; caller operation-start time never decides later validity. The receiver revalidates signature, command identity, revocation/expiry, database-current command, receipt/readback bindings, and its request fence immediately before prepared journal, generation, unlink, absence receipt, final journal, and return. Deletion and absence timestamps are post-operation clock samples with fail-closed monotonic ordering. A prepared journal is a durable recovery obligation; restart alone cannot mint a deletion receipt without current database authority, while a newly authorized command may adopt the exact prepared identity and finish the obligation. Adoption preserves the predecessor authenticated-journal SHA-256, original unlink command ID/digest, finalization command ID/digest, and a domain-separated adoption timestamp/reason in both the successor journal and signed deletion receipt. Divergent custody is rejected before adoption is written. Finalized proof remains retrievable after later expiry/revocation only by directly matching the original signed request against the immutable authenticated request index; retrieval performs no nonce recovery, locking, or filesystem mutation and never reauthorizes deletion. The operator-facing projection remains the target below and is not yet claimed as implemented:

`<BIMLogDataRoot>/Feedback/{01-Active,02-Resolved-Retention,03-Legal-Hold,90-Quarantine,91-Transfer-Failures,99-System}/<CompanySlug>/<ProjectCode-or-General>/<YYYY>/<FB-StableId>/`

Each ticket has `README.txt`, `manifest.json`, and `originals/`, `screenshots/`, `audio/`, `documents/`, `transcripts/`, `receipts/`. Byte filenames are opaque: UTC timestamp plus short content hash. Sanitized display names exist only in `manifest.json`. Manifests are restore-safe, versioned, hash-bound, and contain no credentials or host paths. Atomic projection moves preserve ticket/object IDs and receipts.

## Cleanup and retention proposal

Durations are configuration surfaced for Roberto approval, never embedded as governing constants. Proposed defaults are temporary relay 7 days maximum; transfer failure/manual review and quarantine 30 days; resolved evidence 90 days after both customer and internal verified closure; legal hold has no automatic deletion.

Cleanup is dry-run first. It never auto-deletes Active, Hold, or unknown-outcome custody. Eligibility requires resolved and dual verified closure, no hold, elapsed policy, authenticated destination receipt, matching hash/readback, and operator approval for material purge. Execution produces a byte inventory and immutable deletion receipt, removes exact approved objects, verifies absence, and retains PostgreSQL metadata, hashes, decisions, audit, and deletion receipt. Quota/age dashboards, per-project review/export indexes, duplicate-content detection, and restore verification are required acceptance surfaces.

## Required executable acceptance

Implemented local evidence covers canonical request/receipt/readback/deletion signing, rotation and effective revocation, replay/skew, filesystem-persisted nonce reserve/commit/rollback across authority instance restart, idempotent duplicate and lost-response resolution, native loopback TLS CA/hostname/leaf/SPKI enforcement, bounded streaming and deadlines, opaque filesystem custody, request conflicts, concurrent delivery, staging recovery, containment/reparse denial, hold-aware prepared→unlink→absence→signed-final purge, backup inventory, and isolated exact-byte restore. Nonce capability plaintext is never persisted: v2 nonce records retain only its SHA-256 proof. Delivery, readback, and purge now use the same request-identity lock namespace and revalidate fences at mutation/read boundaries; nonce operations use the same owner/fence/heartbeat mechanism. Synchronous purge/readback use a bounded 30-second lease rather than claiming a heartbeat. Cross-process delivery-versus-purge/recovery/backup and PID-reuse proof remain required before this fence work is accepted. The TLS harness still requires a governed certificate fixture source; ambient OpenSSL is not treated as an available dependency.

The local candidate now includes keyed canonical v3 request indexes and prepared/final deletion journals, strict refusal plus quarantine of unauthenticated legacy authority files, authenticated monotonic custody generations, request-fenced recovery, and a signed generation-consistent v2 backup manifest with bounded asynchronous copy and exact restore verification. Legacy unauthenticated backup/restore entry points have been removed. Upload stages carry boot/process/instance/fence/lease ownership; recovery is globally serialized and preserves live stages while reclaiming only ownerless-expired or proven-dead custody. The portable Windows TLS proof accepts only an absolute governed OpenSSL executable whose SHA-256 and exact version match required authority values. The registered Git OpenSSL identity is pinned; an alternate fixture requires all three explicit path, SHA-256, and version settings. There is no ambient executable fallback and no dependency is installed.

Renewable async request-fenced readback and canonical-command purge are implemented; legacy direct purge methods fail closed. Deterministic tests now exercise revoked, expired, signing-key-denied, authority-outage, job-version, fence, hold-snapshot, and canonical tamper denial; lost-response replay; a real blocked-copy generation change; backup abort and clean retry; partial-restore cleanup and exact retry; PID-reuse takeover; live-owner refusal; and owner-publication grace. Real child-process termination and restart is proven after nonce reservation, object write, generation write, authenticated index write, and prepared deletion-journal write. Write/file-fsync/rename/unlink faults and a genuine post-unlink directory-fsync fault prove no false deletion receipt and exact authority-revalidated restart. Scanner timeout cancellation, admission release, slowloris body-idle cleanup, and fail-closed exact TLS fixture executable hash/version identity are also executable. Sustained multiprocess resource saturation, encrypted temporary-store quotas/TTL, persisted transfer leases, poison/dead-letter handling, operator-facing projection moves, bilingual customer status, mounted receiver HTTP endpoints, and deployed receiver availability remain pending and must not be inferred from the current fixtures. Real receiver URLs, production certificates/keys, provider storage, deployment, and customer data remain external gates.
