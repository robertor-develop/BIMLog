import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
process.env.PROD_DATABASE_URL ||= "postgresql://source-gate.invalid/bimlog";
const { evaluateStaffingScenario,redactScenarioEvaluation } = await import("./team-resource-planning-service");
const { createTeamResourceMutationRateLimit } = await import("../middlewares/team-resource-planning-rate-limit");

const root=path.resolve(import.meta.dirname,"../../../..");
const read=(file:string)=>fs.readFileSync(path.join(root,file),"utf8");
const migration=read("artifacts/api-server/src/lib/team-resource-planning-migration.ts"),schema=read("lib/db/src/schema/team-resource-planning.ts"),service=read("artifacts/api-server/src/lib/team-resource-planning-service.ts"),route=read("artifacts/api-server/src/routes/team-performance.ts"),page=read("artifacts/bimlog/src/components/team-performance/ResourceSchedulingPanel.tsx"),workspace=read("artifacts/bimlog/src/pages/TeamPerformanceWorkspace.tsx"),manual=read("artifacts/bimlog/src/lib/help-content.ts"),navbar=read("artifacts/bimlog/src/components/layout/Navbar.tsx"),help=read("artifacts/bimlog/src/pages/HelpCenter.tsx");
for(const table of ["team_capacity_profile_versions","team_staffing_scenario_versions","team_staffing_application_events"])assert.match(migration,new RegExp(`CREATE TABLE IF NOT EXISTS ${table}`));
for(const name of ["team_capacity_profile_versions_company_id_fkey","team_capacity_profile_versions_user_id_fkey","team_capacity_profile_versions_supersedes_id_fkey","team_capacity_profile_versions_created_by_id_fkey","team_capacity_profile_version_positive_chk","team_capacity_profile_content_object_chk","team_staffing_scenario_versions_company_id_fkey","team_staffing_scenario_versions_project_id_fkey","team_staffing_scenario_versions_supersedes_id_fkey","team_staffing_scenario_versions_created_by_id_fkey","team_staffing_scenario_version_positive_chk","team_staffing_scenario_content_object_chk","team_staffing_scenario_evaluation_object_chk","team_staffing_application_events_event_key_key","team_staffing_application_events_scenario_version_id_fkey","team_staffing_application_events_project_id_fkey","team_staffing_application_events_actor_user_id_fkey","team_staffing_application_reason_chk"])assert.match(schema,new RegExp(name));
for(const name of ["team_capacity_profile_latest_idx","team_staffing_scenario_project_latest_idx","team_staffing_application_project_time_idx"])assert.match(schema,new RegExp(`${name}[\\s\\S]{0,180}\\.desc\\(\\)\\.nullsFirst\\(\\)`));
assert.match(service,/daysBetween/);assert.match(service,/weeklyCapacityHours/);assert.match(service,/CAPACITY_EXCEEDED/);assert.match(service,/internalCost/);assert.match(service,/billingValue/);assert.match(service,/EXISTS\(SELECT 1 FROM project_members viewer/);assert.match(service,/BEGIN/);assert.match(service,/ROLLBACK/);assert.match(service,/TASK_VERSION_CONFLICT/);assert.match(service,/event_key/);assert.match(route,/resource-planning\/evaluate/);assert.match(route,/scenarios\/:scenarioVersionId\/apply/);
assert.doesNotMatch(service,/weeklyCapacityHours:40/);assert.doesNotMatch(service,/workingDays:\[1,2,3,4,5\]/);assert.match(service,/CAPACITY_PROFILE_REQUIRED/);assert.match(service,/const experience=new Map<string,number>\(\);[\s\S]*data\.evidence/);
assert.match(service,/TEAM_RESOURCE_PROFILE_SELF_ONLY/);assert.match(service,/allowRates:false,existingRates/);assert.match(service,/canManageRates:false/);assert.match(service,/internalHourlyRate:data\.auth\.canViewRates/);
assert.match(service,/project_company_binding_versions/);assert.match(service,/TEAM_RESOURCE_COMPANY_MISMATCH/);assert.match(service,/job_activation_resource_assignments[\s\S]*direct_load/);assert.match(service,/NOT EXISTS\(SELECT 1 FROM job_activation_resource_assignments/);
assert.match(service,/TEAM_RESOURCE_DIRECT_TASK_ONLY/);assert.match(service,/if\(item\.assignmentId\) throw[\s\S]{0,260}TEAM_RESOURCE_DIRECT_TASK_ONLY/);assert.match(service,/plannedHoursAdvisory/);assert.doesNotMatch(service,/SET user_id=\$4,person_name/);
assert.match(service,/TEAM_RESOURCE_ASSIGNMENT_OUTSIDE_SCENARIO/);assert.match(service,/item\.startDate<startDate\|\|item\.endDate>endDate/);
assert.match(service,/basisFingerprint/);assert.match(service,/TEAM_RESOURCE_SCENARIO_BASIS_STALE/);assert.match(service,/storedScenarioKey/);assert.match(service,/storedEventKey/);assert.match(service,/WHERE project_id=\$1 AND event_key=\$2/);assert.match(service,/scenario_version_id "scenarioVersionId",reason,result/);assert.match(service,/TEAM_RESOURCE_EVENT_KEY_DIVERGENT_REPLAY/);assert.match(service,/String\(replay\.scenarioVersionId\)!==scenarioId\|\|String\(replay\.reason\)!==reason/);assert.match(service,/company_id=\$2 AND project_id=\$3 FOR UPDATE/);
const applyBegin=service.indexOf('await connection.query("BEGIN");',service.indexOf("export async function applyScenario")),applyLock=service.indexOf("bimlog:team-staffing-apply",applyBegin),applyReplay=service.indexOf('SELECT scenario_version_id "scenarioVersionId",reason,result',applyBegin);assert.ok(applyBegin>=0&&applyLock>applyBegin&&applyReplay>applyLock,"apply must take the project/event transaction lock before replay lookup");assert.match(service,/pg_advisory_xact_lock\(hashtext\('bimlog:team-staffing-apply'\),hashtext\(\$1\)\)/);
assert.doesNotMatch(migration,/DROP TABLE|TRUNCATE|DELETE FROM/i);
const missingProfile=evaluateStaffingScenario({startDate:"2026-08-17",endDate:"2026-08-21",assignments:[{taskId:"task-1",assignmentId:null,expectedAssignmentVersion:null,userId:9,plannedHours:8,startDate:"2026-08-17",endDate:"2026-08-21",category:"coordination",reason:"verified planning evidence",expectedTaskVersion:1}],profiles:new Map(),commitments:new Map([[9,4]]),experience:new Map()});
assert.equal(missingProfile.people[0]?.capacityHours,null);assert.equal(missingProfile.people[0]?.availableHours,null);assert.equal(missingProfile.people[0]?.utilization,null);assert.equal(missingProfile.totals.internalCost,null);assert.equal(missingProfile.totals.billingValue,null);assert.ok(missingProfile.warnings.includes("CAPACITY_PROFILE_REQUIRED"));
const persistedEvaluation={people:[{userId:9,internalCost:800,billingValue:1200}],totals:{scenarioHours:8,internalCost:800,billingValue:1200},warnings:[]};
const redactedEvaluation=redactScenarioEvaluation(persistedEvaluation,false);assert.equal(redactedEvaluation.people[0].internalCost,null);assert.equal(redactedEvaluation.people[0].billingValue,null);assert.equal(redactedEvaluation.totals.internalCost,null);assert.equal(redactedEvaluation.totals.billingValue,null);assert.deepEqual(redactScenarioEvaluation(persistedEvaluation,true),persistedEvaluation);assert.equal(persistedEvaluation.people[0].internalCost,800);
let clock=1_000,nextCalls=0,statusCode=0,responseBody:any=null;const headers=new Map<string,string>();const limiter=createTeamResourceMutationRateLimit({operation:"test",limit:2,windowMs:1_000,now:()=>clock,persistence:"memory"});const request:any={user:{userId:7},params:{projectId:"42"}};const response:any={setHeader:(name:string,value:string)=>headers.set(name,value),status:(value:number)=>{statusCode=value;return response;},json:(value:any)=>{responseBody=value;return response;}};const next=()=>{nextCalls+=1;};await limiter(request,response,next);await limiter(request,response,next);await limiter(request,response,next);assert.equal(nextCalls,2);assert.equal(statusCode,429);assert.equal(responseBody.code,"TEAM_RESOURCE_RATE_LIMITED");assert.equal(headers.get("Retry-After"),"1");clock=2_000;await limiter(request,response,next);assert.equal(nextCalls,3);
assert.match(page,/What-if staffing scenario/);assert.match(page,/Programaci.n de Recursos/);assert.doesNotMatch(page,/window\.print/);assert.match(workspace,/PrintPdfButton/);assert.match(workspace,/surface:\s*"team-performance"/);assert.match(page,/Review & apply/);assert.match(manual,/Resource Scheduling/);assert.match(manual,/Applying fails atomically/);assert.match(navbar,/v1\.60\.33\.06/);assert.match(help,/v1\.60\.33\.06/);assert.match(help,/PrintPdfButton/);assert.match(help,/Complete manual/);
console.log(JSON.stringify({status:"PASS",tests:["explicit-capacity-only","verified-experience","viewer-authorized-cross-project-commitments","rate-redaction","immutable-scenarios","stale-basis-refusal","direct-task-only-atomic-apply","project-safe-idempotent-event","task-version-conflict","non-destructive-migration","bilingual-ui","csv","print-pdf","complete-manual","filtered-manual","global-print","release-version"]}));
