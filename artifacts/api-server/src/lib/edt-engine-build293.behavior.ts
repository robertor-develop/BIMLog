import assert from "node:assert/strict";import{transitionTimeEntry}from"./edt-engine-economic-service";import type{EdtTransactionClient,EdtTransactionHost}from"./edt-engine-transaction";
const calls:string[]=[];const client:EdtTransactionClient={async query<Row>(sql:string){calls.push(sql);if(sql.includes("SELECT e.*"))return{rows:[{id:"t1",status:"draft",optimistic_version:1,user_id:30,project_id:11,intake_id:"i1",work_item_id:"w1",task_id:"task1",assignment_id:"a1",hours:"2.5"}] as Row[]};if(sql.startsWith("UPDATE job_activation_time_entries"))return{rows:[],rowCount:1};return{rows:[]};}};const host:EdtTransactionHost={async connect(){return client}};
const result=await transitionTimeEntry({actor:{grants:["TIME_SUBMIT"],actorUserId:30,actorCompanyId:7,actorProjectIds:[11],eligibleRole:"DRAFTER"},companyId:7,projectId:11,entryId:"t1",expectedVersion:1,decision:"submit",budgetAccountId:"b1",pool:"direct_production",amount:"250.00",reason:"Submit completed drafting time",evidence:{sheet:"weekly"}},host);
assert.equal(result.status,"submitted");assert.ok(calls.some(sql=>sql.includes("job_activation_budget_ledger_entries")));assert.ok(calls.includes("COMMIT"));
console.log("EDT_ENGINE_BUILD293_RESULT=PASS");
